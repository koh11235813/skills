# Provenance ledger

Evidence for every load-bearing claim in `SKILL.md` and `references/mechanism.md`, so the next update is a diff against this table rather than a re-reading of the prose.

**Pinned:** codex-rs `main` @ `421082e79d`, 2026-09-24. Prompt composition probed against `codex-cli 0.158.0-alpha.8` (main `53446f90a5`, 17 commits before the pin); exec behavior still from `0.155.0-alpha.2.3`. The binaries and the source tree are close but not identical, so treat a disagreement between them as a version gap rather than an error.

Paths are relative to `codex-rs/` in the [openai/codex](https://github.com/openai/codex) repository.

The **match on** column is what to `rg` for. It is deliberately *not* the full string as quoted in the prose: most quoted strings are `format!` templates whose placeholder names differ from the source (the docs say `unsupported call: {tool}`; the source says `format!("unsupported call: {tool_name}")`). Searching the whole template returns zero hits for claims that are perfectly correct. **Always match on the literal fragment between placeholders, or on the constant/type name.**

---

## Command classification (exec policy)

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| Banned allow-prefix list has 88 entries | mechanism §2 | `core/src/exec_policy.rs` | `BANNED_PREFIX_SUGGESTIONS` |
| Forbidden prefix never spawns | SKILL triage, mechanism §2 | `core/src/exec_policy.rs` | `rejected: policy forbids commands starting with` |
| Dangerous-rm heuristic forbids outright | SKILL triage, mechanism §2 | `core/src/exec_policy.rs` | `rm -f style commands are not permitted` |
| …but the rm string only replaces the fallback reason when a forced-rm match is still identified at rejection time, and it is never consulted at all when a policy `prompt` prefix rule is what produced the decision | SKILL triage, mechanism §2, §4 | `core/src/exec_policy.rs`; `core/src/exec_policy_tests.rs` | `derive_rejected_prompt_reason`; `prompt_is_rule` |
| Prompt suppressed under `never` | SKILL triage, mechanism §2 | `core/src/exec_policy.rs` | `approval required by policy, but AskForApproval` |
| Two Granular-specific rejections exist | mechanism §4 table | `core/src/exec_policy.rs` | `Granular.sandbox_approval is false` |
| Windows-without-backend prompts (or forbids under `never`) for every unmatched command under the downgraded managed read-only profile | SKILL Windows para, mechanism §3 | `core/src/exec_policy.rs` | `windows_managed_fs_restrictions_without_sandbox_backend` |
| Escalation request in a forbidding mode is scripted-rejected | SKILL triage, mechanism §2 | `core/src/tools/handlers/unified_exec/exec_command.rs` | `you cannot ask for escalated permissions` |
| One-shot migration strips banned saved rules | mechanism §2 | `execpolicy/src/sandbox_migration.rs` | `strip_banned_allow_rules` |
| The known-safe safelist module is gone; only the dangerous-command classifier remains | SKILL Windows para, mechanism §2, §3 | `shell-command/src/command_safety/mod.rs` | `pub mod is_dangerous_command` |
| Dangerous-command classification fails closed past wrapper depth 8 | mechanism §2 | `shell-command/src/command_safety/is_dangerous_command.rs` | `MAX_DANGEROUS_COMMAND_WRAPPER_DEPTH` |
| `approval_policy = "untrusted"` is a hard config error | SKILL mode-matrix note, SKILL outside playbook, mechanism §2 | `core/src/config/mod.rs` | `UnsupportedUntrustedApprovalPolicyError` |
| App-server config writes reject it separately | mechanism §2 | `app-server/src/config_manager_service.rs` | `is no longer supported; remove this setting` |
| The CLI approval enum no longer spells `untrusted` | SKILL mode-matrix note | `utils/cli/src/approval_mode_cli_arg.rs` | `enum ApprovalModeCliArg` |
| An untrusted project resolves to `UnlessTrusted` by default | SKILL mode-matrix note, mechanism §2 | `core/src/config/mod.rs` | `active_project.is_untrusted()` |
| An app-server `thread/start` approval policy becomes a config override and bypasses the check | SKILL mode-matrix note, mechanism §2 | `app-server/src/request_processors/thread_processor.rs`; `core/src/config/mod.rs` | `fn build_thread_config_overrides`; `approval_policy_override` |
| The v2 wire enum still exposes `"untrusted"` | mechanism §2 | `app-server-protocol/src/protocol/v2/shared.rs` | `enum AskForApproval` |
| Managed requirements pin the first allowed policy as the required default | mechanism §2 | `config/src/config_requirements.rs` | `allowed_approval_policies` |
| Under `unless-trusted` every unmatched command prompts | SKILL mode-matrix note, mechanism §2 | `core/src/exec_policy.rs` | `Projects marked untrusted require approval` |
| Unmatched-command fallback = policy × sandbox kind × sandbox-override request | mechanism §2 | `core/src/exec_policy.rs` | `requests_sandbox_override()` |
| The Windows no-backend guard needs a codex-managed, restricted, non-full-write profile | SKILL Windows para, mechanism §3 | `core/src/exec_policy.rs` | `profile_has_managed_filesystem_restrictions` |
| An explicit exec-policy allow still bypasses the sandbox | SKILL Windows para, mechanism §3 | `core/src/exec_policy.rs` | `bypass_sandbox` |
| Unquoted shell metacharacters abort literal parsing | SKILL inside playbook, mechanism §2 | `shell-command/src/bash.rs` | `is_literal_word_or_number` |
| …and the whole wrapper then becomes the policy subject | SKILL inside playbook, mechanism §2 | `core/src/exec_policy.rs` | `fn commands_for_exec_policy` |
| Cyber models / managed `ignore_rules` strip every Allow prefix rule | SKILL Layer 0, mechanism §2 | `core/src/session/turn_context.rs`; `core/src/exec_policy/model_policy.rs` | `IgnoreForCyberModel` |
| Two shipped catalog slugs carry `model_specialty: "cyber"` | SKILL Layer 0, mechanism §2 | `models-manager/models.json` | `"model_specialty": "cyber"` |
| …and no reusable prefix-rule amendment is proposed in that mode | SKILL Layer 0, mechanism §2 | `core/src/exec_policy.rs` | `auto_amendment_allowed` |
| Environments carry a restrictive-only exec-policy overlay | mechanism §2 | `core/src/session/environment.rs`; `core/src/exec_policy/model_policy.rs` | `environment command policy cannot contain allow rules` |

## Approval gate

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| Approval cache key is 7 fields (adds executable, tty) | SKILL inside playbook, mechanism §2 | `core/src/tools/runtimes/unified_exec.rs` | `UnifiedExecApprovalKey` |
| …and the lookup pairs that key with the selected environment's exec-policy fingerprint | SKILL inside playbook, mechanism §2 | `core/src/tools/approvals.rs` | `RequirementsExecPolicy::fingerprint` |
| Only `ApprovedForSession` decisions are cached; denials are not | SKILL triage, mechanism §2 | `core/src/tools/sandboxing.rs` | `ReviewDecision::ApprovedForSession` |
| Command is canonicalized before keying, while the executable field keeps the raw first token | SKILL inside playbook, mechanism §2 | `core/src/command_canonicalization.rs`; `core/src/tools/approvals.rs` | `__codex_shell_script__`; `executable: command.first().cloned()` |
| Denial reasons are propagated; the hook default is "PermissionRequest hook denied approval" and "rejected by configuration" is network-amendment-deny only | SKILL triage, mechanism §4 | `core/src/tools/approvals.rs`; `hooks/src/engine/output_parser.rs` | `PermissionRequest hook denied approval`; `rejected by configuration` |
| Guardian denial wording | SKILL triage, mechanism §4 | `core/src/guardian/review.rs` | `rejected due to unacceptable risk` |
| Human denial normalized — and *only* the exact string `rejected by user` is | SKILL triage, mechanism §4 | `core/src/tools/events.rs` | `exec command rejected by user` |
| Deserialization/client/transport failures keep the reason `approval request failed` | SKILL triage, mechanism §2 | `app-server/src/bespoke_event_handling.rs` | `approval request failed` |
| Abort tears down the whole turn — for the exec and patch approval ops only | mechanism §2 | `core/src/session/handlers.rs` | `ReviewDecision::Abort` |
| …but an MCP approval abort is converted back into a tool result (`user cancelled MCP tool call`), because MCP approvals travel through `request_user_input`, whose handler does not interrupt | SKILL triage, mechanism §2 | `core/src/mcp_tool_call.rs`; `core/src/session/handlers.rs` | `user cancelled MCP tool call` |
| MCP denial never contacts the server | SKILL triage, mechanism §4 | `core/src/mcp_tool_call.rs` | `user rejected MCP tool call` |
| Escalation, retries **and** an elicitation marked `codex_sensitive_action` all set `require_synchronous_review`, which forces the synchronous path and skips extension fast approval wherever a Guardian review is already running | mechanism §2, §3, §6 | `core/src/guardian/review.rs` | `require_synchronous_review` |
| Guardian denial circuit breaker: 3 consecutive / 10-in-50, and 1/1 for a cyber-specialty model **only** — an `auto_review.ignore_rules` slug does not select it | SKILL triage, mechanism §2 | `core/src/guardian/mod.rs`; `core/src/guardian/review.rs` | `MAX_CONSECUTIVE_CYBER_GUARDIAN_DENIALS_PER_TURN`; `MODEL_SPECIALTY_CYBER` |
| `guardian_approval` is stable and default-enabled; guardian routing additionally requires `approvals_reviewer` to be `auto_review` (default `user`) and the policy to be `on-request` or granular | SKILL off-by-default table | `features/src/lib.rs`; `protocol/src/config_types.rs`; `core/src/guardian/review.rs` | `key: "guardian_approval"`; `routes_approval_policy_to_guardian` |
| A stored grant preapproves only an exactly-matching request | mechanism §2 | `core/src/tools/handlers/mod.rs` | `fn preapproved_permission_profile` |
| Strict auto-review can be enabled mid-turn | mechanism §2 | `core/src/state/turn.rs`; `core/src/session/mod.rs` | `enable_strict_auto_review` |
| Managed requirements can force AutoReview by model slug | mechanism §2 | `config/src/requirements_layers/models.rs` | `required_on_models` |
| A network request disconnecting during approval cancels the *owning execution*, and only when one is attributed | SKILL triage, mechanism §3, §4 | `core/src/tools/network_approval.rs` | `before approval could complete`; `execution_id: owner_call` |
| …but the disconnect never cancels the approval reviewer or changes its decision | SKILL triage, mechanism §3 | `network-proxy/src/request_disconnect.rs` | `struct NetworkRequestDisconnect` |
| Codex delegates are refused unless approval policy is `never` | mechanism §5 | `core/src/codex_delegate.rs` | `Codex delegates require approval policy` |
| A required-auto-review model coerces full access to workspace-write and forces the Guardian reviewer at startup | SKILL Layer 0, mechanism §2 | `core/src/session/mod.rs` | `auto_review_required_for_model` |
| …and the validation that runs after it passes unless `guardian_approval` is off or the profile still has full-disk write, so at startup it bites only in those cases; a *later* settings change undoing either coercion is rejected the same way, with `To use model X, you need to use auto review.` | SKILL Layer 0, mechanism §2 | `core/src/session/session.rs`; `config/src/constraint.rs` | `AutoReviewRequired` |
| …and MCP *reviewer selection* returns Guardian ahead of any per-account or per-app reviewer setting, but the three skip-review shortcuts are evaluated separately and still fire, so a per-tool `approval_mode = "approve"` runs unprompted anyway | SKILL Layer 0, SKILL inside playbook, mechanism §2 | `core/src/connectors.rs`; `core/src/mcp_tool_call.rs` | `mcp_approvals_reviewer_from_layers`; `AppToolApproval::Approve` |
| `--approve-for-me` expands to exactly three config overrides | SKILL off-by-default table | `utils/cli/src/shared_options.rs` | `take_auto_review_config_overrides` |
| An `ApprovedMcpPolicyAmendment` decision writes `approval_mode = "approve"` into config.toml (server, plugin or apps variant) and degrades to a session-only memory on any write failure | SKILL inside playbook, mechanism §2 | `core/src/mcp_tool_call.rs` | `ApprovedMcpPolicyAmendment` |
| …and whether an MCP tool prompts at all is decided per tool by its approval mode, defaulting to `auto`, where the server's own `read_only_hint` decides | SKILL inside playbook, mechanism §6 | `core/src/mcp_tool_call.rs`; `config/src/mcp_types.rs` | `requires_mcp_tool_approval_for_mode` |
| Turn-scoped `strict_auto_review` routes every approval to Guardian and cancels three MCP shortcuts; the only writer is a client `request_permissions` response | mechanism §2 | `core/src/mcp_tool_call.rs`; `core/src/tools/approvals.rs`; `core/src/session/mod.rs` | `strict_auto_review_enabled` |
| Persisting an execpolicy amendment is a second approval that outlives the call, so MCP persistence is not unique | mechanism §2 | `core/src/session/handlers.rs` | `ApprovedExecpolicyAmendment` |
| A remembered MCP approval is keyed by server + `plugin_id` + `connector_id` + `link_id` + tool name, and the persistent key is that same key | SKILL inside playbook, mechanism §2 | `core/src/mcp_tool_call.rs` | `struct McpToolApprovalKey`; `persistent_mcp_tool_approval_key` |
| …`plugin_id` was added in this range and is resolved only for MCP servers the catalog attributes to a plugin, so it splits the key per owning plugin and leaves the `codex_apps` path unchanged | mechanism §2 | `core/src/mcp_tool_call.rs`; `codex-mcp/src/mcp/mod.rs` | `plugin_ids_by_mcp_server_name`; `persist_non_app_mcp_tool_approval` |
| …built only when the resolved mode is `Auto`, and never on `codex_apps` without a `connector_id`; with no key the prompt does not even offer session-remember or persist, so the call is re-prompted every time | mechanism §2 | `core/src/mcp_tool_call.rs`; `config/src/mcp_types.rs` | `session_mcp_tool_approval_key`; `allow_session_remember` |
| …but the apps persist path writes `apps.<connector_id>.tools.<tool>.approval_mode`, with no `link_id`, so a persisted approval widens from one account to the whole app; a failed write degrades to the still-account-scoped in-memory record | SKILL inside playbook, mechanism §2 | `core/src/mcp_tool_call.rs` | `persist_codex_app_tool_approval` |
| …and a persisted apps approval reloads the session user config layer, but app-tool policy reads the `McpConfig` captured when the call was prepared | mechanism §2 | `core/src/mcp_tool_call.rs`; `codex-mcp/src/binding.rs` | `reload_user_config_layer`; `Returns the configuration and approval authority captured with this client` |
| `codex_sensitive_action` requires synchronous review when routed to automatic review, and by its own definition does not opt the action into review | mechanism §6 | `protocol/src/mcp_approval_meta.rs`; `core/src/session/mcp.rs` | `SENSITIVE_ACTION_KEY` |
| The user-CUA shortcut auto-approves an empty-schema form only for a `user` reviewer on a `node_repl`/`cua_repl` `js` call under connector `node_repl` with an `mcp_tool_call` approval kind, and is disqualified by `codex_sensitive_action` or `codex_requires_user_input` — so on that one shape the marker converts a silent auto-approval into a client-forwarded form | mechanism §6 | `core/src/session/mcp.rs`; `codex-mcp/src/elicitation.rs` | `user_cua_execution`; `codex_requires_user_input` |
| Full Access is approval policy `never` plus a disabled permission profile on the thread and every selected environment; a pending or failed environment is never Full Access | SKILL outside playbook, mechanism §6 | `protocol/src/environment.rs`; `core/src/environment_selection.rs` | `fn has_full_access` |
| …and it approves any empty-schema elicitation before `require_synchronous_review` is computed, then short-circuits Guardian review a second time inside `run_guardian_review` | SKILL outside playbook, mechanism §6 | `core/src/session/mcp.rs`; `core/src/guardian/review.rs` | `has_full_access` |
| Under the default `user` reviewer a sensitive elicitation that is not also marked `codex_strict_auto_review` never reaches Guardian: routing returns before review | SKILL outside playbook, mechanism §6 | `core/src/session/mcp.rs`; `core/src/guardian/review.rs` | `routes_approval_policy_to_guardian` |
| An empty poll, a `\u{3}` to a non-TTY process, and the `write_stdin_approval` feature being off skip terminal-input review entirely | SKILL off-by-default table, mechanism §2 | `core/src/unified_exec/stdin_approval.rs` | `Feature::WriteStdinApproval` |
| A terminal whose original environment is no longer selected is refused, on both the direct and Guardian paths (different wording on each) | mechanism §2 | `core/src/unified_exec/stdin_approval.rs`; `core/src/tools/approvals.rs` | `cannot access the terminal's` |
| Denied-read and environment-owned network restrictions the running terminal cannot enforce are refused rather than prompted, and network settings are normalized out of the denied-read comparison | SKILL triage, SKILL inside playbook, mechanism §2, §4 | `core/src/unified_exec/stdin_approval.rs` | `restrictions; start a new terminal` |
| Matching permissions skip approval unless `strict_auto_review` is set; retained grants, a launch bypass and any other drift require it | mechanism §2 | `core/src/unified_exec/stdin_approval.rs`; `core/src/unified_exec/process_manager.rs` | `fn review_requirement` |
| A NUL byte in terminal input is refused before the reviewer, because manual approval shell-quotes the input | mechanism §2, §4 | `core/src/unified_exec/stdin_approval.rs` | `NUL byte and cannot be reviewed safely` |
| The formatted action plus its reason is capped at 8,000 bytes, and a formatter truncation refuses on the same string | mechanism §2, §4 | `core/src/unified_exec/process_manager.rs` | `too large to review safely` |
| The approval reason names the launch authority and serializes retained grants as JSON; it is reviewer-facing and never reaches the model | mechanism §2 | `core/src/unified_exec/stdin_approval.rs` | `fn approval_reason` |
| Every terminal-input refusal reaches the model wrapped as `write_stdin rejected: {reason}`, and an approval that ends in a harness error as `write_stdin approval failed: {err}`; neither is rewritten into the exec/patch normalized form | SKILL triage, mechanism §2, §4 | `core/src/tools/handlers/unified_exec/write_stdin.rs`; `core/src/tools/events.rs` | `write_stdin rejected: ` |
| The human prompt for a terminal write offers only `Approved` and `Abort` — no per-call decline, no `ApprovedForSession` — so every reviewed write re-prompts and a human refusal aborts the turn through the exec-approval op | SKILL triage, SKILL inside playbook, mechanism §2 | `core/src/tools/approvals.rs`; `core/src/session/mod.rs`; `core/src/session/handlers.rs` | `ExecApprovalKind::WriteStdin` |
| A terminal write needing any sandbox override is scripted-rejected under a forbidding policy before any reviewer runs, unless `strict_auto_review` is set under `never` | SKILL triage, mechanism §2 | `core/src/tools/approvals.rs`; `core/src/exec_policy.rs`; `protocol/src/models.rs` | `requests_sandbox_override` |
| By default no approval sits on the `write_stdin` path, and a running process keeps its launch-time sandbox regardless of later policy changes | SKILL inside playbook, mechanism §2 | `core/src/unified_exec/stdin_approval.rs`; `core/src/unified_exec/process_manager.rs` | `neither an earlier approval nor a policy` |

## OS sandboxing

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| **Windows downgrades workspace-write to read-only** | SKILL mode matrix, mechanism §3 | `core/src/config/permissions.rs` | `default_builtin_permission_profile_name` |
| …and a test asserts exactly that | — | `cli/src/debug_sandbox.rs` | `downgrades to read-only when the Windows sandbox is disabled` |
| Windows sandbox backend ships disabled | SKILL mode matrix, mechanism §3 | `features/src/lib.rs` | `experimental_windows_sandbox` |
| `CODEX_SANDBOX=seatbelt` is macOS-only | SKILL inside playbook, mechanism §3 | `core/src/sandboxing/mod.rs` | `CODEX_SANDBOX_ENV_VAR` |
| `CODEX_SANDBOX_NETWORK_DISABLED` on every platform | SKILL inside playbook, mechanism §3 | `core/src/spawn.rs` | `CODEX_SANDBOX_NETWORK_DISABLED` |
| Protected workspace metadata is deleted and forces exit 1 | mechanism §3 | `linux-sandbox/src/linux_run_main.rs` | `sandbox blocked creation of protected workspace metadata path` |
| Seatbelt profile is deny-by-default | mechanism §3 | `sandboxing/src/seatbelt_base_policy.sbpl` | `(deny default)` |
| Linux network syscalls return EPERM via seccomp | mechanism §3 | `linux-sandbox/src/landlock.rs` | `SeccompAction::Errno` |
| Denial detection is 7 keywords + exit codes + SIGSYS | SKILL triage, mechanism §3 | `sandboxing/src/denial.rs` | `landlock` |
| The single retry-unsandboxed gate | mechanism §3 | `core/src/tools/orchestrator.rs` | `retry without sandbox` |
| `UnlessTrusted` is the only policy the default `wants_no_sandbox_approval` accepts for every tool; `Granular` needs its `sandbox_approval` flag, and the apply_patch runtime overrides the default to accept `OnRequest` too | SKILL mode matrix, mechanism §2 | `core/src/tools/sandboxing.rs`; `core/src/tools/runtimes/apply_patch.rs`; `core/src/tools/orchestrator.rs` | `fn wants_no_sandbox_approval` |
| …and a denied-read filesystem policy blocks the unsandboxed attempt entirely | SKILL mode matrix, mechanism §2 | `core/src/tools/sandboxing.rs`; `core/src/tools/orchestrator.rs` | `fn unsandboxed_execution_allowed` |
| Blocked host wording | mechanism §3 | `core/src/tools/orchestrator.rs` | `is blocked by policy` |
| `LD_*` / `DYLD_*` stripped | mechanism §3 | `process-hardening/src/lib.rs` | `remove_env_vars_with_prefix` |
| Explicit loopback allowlist entries now connect | mechanism §3 | `network-proxy/src/connect_policy.rs` | `allows_non_public_target` |
| Missing protected paths are materialized on Linux as synthetic empty read-only mounts; macOS emits deny regexes covering paths that do not exist | mechanism §3 | `linux-sandbox/src/bwrap.rs`; `sandboxing/src/seatbelt.rs` | `append_missing_read_only_subpath_args` |
| Windows kills process trees via job objects | mechanism §3, §5 | `utils/pty/src/win/job.rs` | `JobObject` |
| `.git`/`.agents`/`.codex` carve-out is one shared list rendered by three backends | mechanism §3 | `protocol/src/permissions.rs` | `PROTECTED_METADATA_PATH_NAMES` |
| …and Windows renders it as deny ACEs, dropping any path that does not yet exist | mechanism §3 | `windows-sandbox-rs/src/allow.rs` | `add_deny_path` |
| macOS denies unlink of writable dir roots and protected ancestors | mechanism §3 | `sandboxing/src/seatbelt.rs` | `deny file-write-unlink` |
| macOS refuses *nested* symlinked writable-root components, normalizing top-level aliases such as `/tmp -> /private/tmp` instead | SKILL triage, mechanism §3 | `sandboxing/src/seatbelt.rs` | `symlinked writable roots are not supported`; `fn nested_symlink_component` |
| macOS preferences policy is appended only under full-disk read | mechanism §3 | `sandboxing/src/seatbelt.rs` | `MACOS_SEATBELT_PREFERENCES_POLICY` |
| Linux drops all capabilities | mechanism §3 | `linux-sandbox/src/bwrap.rs` | `--cap-drop` |
| Linux sandbox runs as PID 1 and reaps orphans | mechanism §3 | `linux-sandbox/src/launcher.rs` | `--as-pid-1` |
| Linux materializes synthetic empty protected dirs | mechanism §3 | `linux-sandbox/src/bwrap.rs` | `append_missing_read_only_subpath_args` |
| Project-root discovery requires a `HEAD` file inside `.git` | mechanism §3 | `config/src/loader/mod.rs` | `join("HEAD")` |
| A named list strips five auth variables from model-reachable children, explicit command overrides included | mechanism §3 | `protocol/src/shell_environment.rs` | `NON_INHERITABLE_ENV_VARS` |

## Limits and truncation

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| Exec timeout 10 s, exit forced to 124 | mechanism §3 | `core/src/exec.rs` | `DEFAULT_EXEC_COMMAND_TIMEOUT_MS` |
| stdout, stderr, and aggregate share one 1 MiB constant | mechanism §3 | `utils/pty/src/lib.rs`, `core/src/exec.rs` | `DEFAULT_OUTPUT_BYTES_CAP` |
| Unified-exec wait constants | mechanism §3 | `core/src/unified_exec/mod.rs` | `MIN_EMPTY_YIELD_TIME_MS` |
| Empty polls clamp to `[5 s, configured max]`; non-empty capped at 30 s | mechanism §3 | `core/src/unified_exec/process_manager.rs` | `clamp(MIN_EMPTY_YIELD_TIME_MS` |
| Tool-schema compaction is 4 progressive passes over a 5,000-byte threshold | SKILL default surface, mechanism §1 | `tools/src/json_schema/compaction.rs` | `LARGE_SCHEMA_COMPACTION_PASSES` |
| Model-visible tool schemas are a fixed whitelist; bounds, `pattern`, `format`, `default` are dropped at deserialization | SKILL default surface, mechanism §1 | `tools/src/json_schema/types.rs`; `tools/src/json_schema.rs` | `pub struct JsonSchema`; `min_items` |
| Nothing forwards the raw schema JSON: every wire form is `JsonSchema`-typed | SKILL default surface, mechanism §1 | `tools/src/tool_definition.rs`; `tools/src/responses_api.rs`; `tools/src/tool_spec.rs` | `input_schema: JsonSchema`; `create_tools_raw_json_for_responses_api` |
| Numeric keywords infer `number` and `format` infers `string` on an untyped node, then are discarded | mechanism §1 | `tools/src/json_schema.rs`; `tools/src/json_schema_tests.rs` | `multipleOf`; `infers_number_from_numeric_keywords` |
| An untyped node whose only keywords are discarded ones is cleared to `{}` | mechanism §1 | `tools/src/json_schema.rs` | `map.clear()` |
| `const` is rewritten into a one-element `enum` | mechanism §1 | `tools/src/json_schema.rs` | `map.remove("const")` |
| The bound drop applies on the no-compaction parse path too | mechanism §1 | `tools/src/json_schema.rs`; `ext/web-search/src/tool.rs` | `parse_tool_input_schema_without_compaction` |
| The 5,000-byte budget is measured on the normalized, already-lossy schema | mechanism §1 | `tools/src/json_schema/compaction.rs` | `compact_normalized_schema_len` |
| Compaction pass 1 strips per-parameter `description` keys inside the schema, not the tool's own description | SKILL default surface, mechanism §1 | `tools/src/json_schema/compaction.rs`; `tools/src/responses_api.rs` | `strip_schema_descriptions`; `pub description: String` |
| Per-tool `output_token_limit` is a positive token budget merged by `min`, and the merge never touches approval mode | SKILL outside playbook, mechanism §4 | `config/src/mcp_types.rs`; `core-plugins/src/loader.rs` | `restrict_output_token_limit` |
| The effective MCP truncation policy is captured from the executing call's config snapshot, falling back to the model's own policy | mechanism §4 | `core/src/tools/handlers/mcp.rs`; `codex-mcp/src/binding.rs` | `fn output_token_limit` |
| Global `tool_output_token_limit` rewrites the model's truncation policy, which is the fallback when no per-tool limit is set | SKILL outside playbook, mechanism §4 | `config/src/config_toml.rs`; `models-manager/src/model_info.rs` | `tool_output_token_limit` |
| Model-facing MCP output is cut at policy × 1.2 (the 20% serialization allowance) and the history copy is cut at the same budget via `fallback_token_limit_override` when recorded, including on resume | SKILL inside playbook, mechanism §4 | `core/src/tools/context.rs`; `core/src/context_manager/history.rs`; `history/src/lib.rs`; `protocol/src/protocol.rs` | `fallback_token_limit_override` |
| `PostToolUse` receives the whole serialized `CallToolResult` including `_meta`, while the Code Mode copy of the same result has `_meta` deliberately stripped | SKILL inside playbook, mechanism §4 | `core/src/tools/context.rs`; `tools/src/tool_output.rs`; `core/src/tools/handlers/mcp.rs` | `must not reach Code Mode` |
| A Code Mode script's yielded output is re-cut at Code Mode's own token budget, not at the per-tool MCP limit | SKILL outside playbook, mechanism §4 | `core/src/tools/code_mode/mod.rs` | `truncate_code_mode_result` |
| The MCP completion event is byte-capped at the 1 MiB `DEFAULT_OUTPUT_BYTES_CAP` and collapses an oversized result to a text preview with `structured_content` and `_meta` dropped | SKILL inside playbook, mechanism §4 | `core/src/mcp_tool_call.rs`; `utils/pty/src/lib.rs` | `truncate_mcp_tool_result_for_event` |
| A tool-lifecycle extension mutates the `CallToolResult` (content, structured content, `_meta`) after `tools/call` and before modality sanitization, the completion event and model output | mechanism §4 | `core/src/tools/lifecycle.rs`; `core/src/mcp_tool_call.rs`; `ext/extension-api/src/contributors/tool_lifecycle.rs` | `on_mcp_tool_result` |
| Hook output spills at 2,500 tokens | mechanism §4 | `hooks/src/output_spill.rs` | `Full hook output saved to` |

## Injected context

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| `project_doc_max_bytes` default is 32 KiB, total | SKILL outside playbook, mechanism §1 | `config/src/config_toml.rs` | `DEFAULT_PROJECT_DOC_MAX_BYTES` |
| AGENTS.md cut mid-file; only trace is a server-side log | SKILL outside playbook, mechanism §1 | `core/src/agents_md.rs` | `LOCAL_AGENTS_MD_FILENAME` |
| Skills catalog gets 2% of the context window | mechanism §1, §6 | `ext/skills/src/render.rs` | `SKILL_METADATA_CONTEXT_WINDOW_PERCENT` |
| Descriptions are shortened under that budget | mechanism §1, §6 | `ext/skills/src/render.rs` | `Skill descriptions were shortened` |
| Catalog budget = configured `max_context_tokens` capped at 10,000 tokens, else 2% of window, else 8,000 characters | mechanism §6 | `ext/skills/src/render.rs` | `MAX_CONFIGURED_SKILL_METADATA_TOKEN_BUDGET` |
| Ambiguous `$mention` is silently skipped | mechanism §1 | `skills/src/selection.rs` | `skill_count != 1` |
| Untrusted host values truncate at ~1,000 tokens | mechanism §1 | `context-fragments/src/additional_context.rs` | `MAX_ADDITIONAL_CONTEXT_VALUE_TOKENS` |
| Internal-context marker | mechanism §1 | `core/src/context/internal_model_context.rs` | `CONTEXT_START_MARKER` |
| The `never` policy announcement forbids `sandbox_permissions` | mechanism §1 | `prompts/templates/permissions/approval_policy/never.md` | `commands will be rejected` |
| The `on-request` announcement forbids destructive prefix rules | mechanism §1, §2 | `prompts/templates/permissions/approval_policy/on_request.md` | `NEVER provide a prefix_rule argument` |
| **Managed "force exact value" is an open set** | SKILL Layer 0, mechanism Layer 0 | `core/src/config/requirements.rs` | `allow_login_shell` |
| **Model catalog `tool_mode` is read first, with no local override** | SKILL Layer 0, mechanism Layer 0 | `core/src/tools/mod.rs` | `effective_tool_mode` |
| …and the shipped catalog uses it for seven slugs | SKILL Layer 0, mechanism Layer 0 | `models-manager/models.json` | `"tool_mode": "code_mode_only"` |
| …including the highest-priority visible preset, so an unconfigured session is in code mode | mechanism Layer 0 | `models-manager/models.json` (`gpt-6-astra`: `"visibility": "list"`, `"priority": 1`, `"tool_mode": "code_mode_only"`); `models-manager/src/manager.rs`; `protocol/src/openai_models.rs` | `mark_default_by_picker_visibility`; `show_in_picker` |
| …and that preset changed at the 2026-09-19 re-pin: `gpt-6-astra` flipped `"visibility"` from `"hide"` to `"list"`, displacing `gpt-5.6-sol` as the default and bringing its `experimental_supported_tools` (`send_user_message_async`, `clock`) into an unconfigured session | mechanism Layer 0, SKILL off-by-default table | `models-manager/models.json` (`gpt-6-astra` block) | `"experimental_supported_tools"` |
| Allowlisted bundled cleanup hooks are trusted with no recorded hash and stay enabled through per-hook disablement | SKILL Layer 0, mechanism Layer 0 | `plugin/src/bundled_hooks.rs`; `hooks/src/engine/discovery.rs` | `ALLOWLISTED_BUNDLED_HOOKS`; `fn hook_trust_status` |
| …and the match requires an `mcp_tool` handler with no matcher, with the Apps target additionally requiring an empty input and the registered connector — which local discovery cannot supply, so that entry resolves only on the executor path | mechanism Layer 0 | `plugin/src/bundled_hooks.rs`; `hooks/src/engine/discovery.rs`; `core-plugins/src/executor_hooks.rs` | `connector_openai_browser`; `/*app_connector_id*/ None`; `lookup_enabled_tool` |
| …and they survive the hooks feature being turned off entirely, with warnings and load errors cleared | SKILL Layer 0, mechanism Layer 0 | `hooks/src/engine/mod.rs` | `Disabled ordinary hooks must not emit warnings` |
| The built-in classification never reaches a client: not on the wire, filtered from the app-server hook catalog, no lifecycle notification | SKILL Layer 0, mechanism Layer 0 | `protocol/src/protocol.rs`; `app-server/src/request_processors/catalog_processor.rs`; `core/src/hook_runtime.rs` | `Internal classification used to suppress lifecycle notifications`; `fn should_emit_hook_notification` |
| Plugin enablement and `allow_managed_hooks_only` still bound the exception | mechanism Layer 0 | `plugin/src/load_outcome.rs`; `hooks/src/engine/discovery.rs` | `fn effective_plugin_hook_sources`; `allow_managed_hooks_only` |
| Executor plugin hooks are filtered down to the same allowlist and nothing else, and land in the engine already stamped builtin | SKILL Layer 0, mechanism Layer 0 | `core-plugins/src/executor_hooks.rs`; `hooks/src/engine/mod.rs` | `Remove this temporary filter once executor plugin hooks can be trusted`; `allowlisted executor handler must be an MCP tool` |
| Goal steering pushes back on scope shrinking | mechanism §1 | `prompts/templates/goals/continuation.md` | `Do not substitute a narrower` |
| Periodic timestamp reminder | mechanism §1 | `core/src/context/current_time_reminder.rs` | `<current_time_reminder>` |
| A saved prefix rule or network rule is echoed back as a context fragment | mechanism §1, §2 | `core/src/context/approved_command_prefix_saved.rs`; `core/src/context/mod.rs` | `approved_command_prefix_saved`; `NetworkRuleSaved` |
| Untrusted projects load no project AGENTS.md at all | SKILL inside playbook, mechanism §1 | `core/src/agents_md.rs` | `active_project.is_untrusted()` |
| Sandboxed AGENTS.md read failure returns an error rather than degrading, and the callers are now traced: fatal at session init through `thread/start`, and a pre-sampling `EventMsg::Error` on a later refresh. The model never sees it | SKILL outside playbook, mechanism §1 | `core/src/agents_md.rs`; `core/src/session/session.rs`; `core/src/session/mod.rs`; `app-server/src/request_processors/thread_processor.rs` | `failed to load AGENTS.md instructions` |
| `project_doc_max_bytes` is one pool across all environments | SKILL outside playbook, mechanism §1 | `core/src/agents_md.rs` | `let mut remaining = config.project_doc_max_bytes` |
| Skills budget warnings go to the client, not into context | SKILL default surface, mechanism §6 | `ext/skills/src/render.rs`; `ext/skills/src/extension.rs` | `fn warning_message` |
| Skill locators are aliased to `r0`/`r1` unannounced — and aliasing is selected by a three-key comparison (included, then truncated description chars, then cost), so it is *not* usable as a budget-pressure signal | SKILL default surface, mechanism §6 | `ext/skills/src/render.rs` | `aliased_render_is_better` |
| …and the combined path compares five candidates, so one source catalog can be aliased while another is not | mechanism §6 | `ext/skills/src/render.rs` | `combined_render_is_better` |
| Combined catalogs share one budget | mechanism §6 | `ext/skills/src/render.rs` | `render_combined_available_skills` |
| User-shell (`!command`, `thread/shellCommand`) output re-enters model context unsandboxed, with no exec-policy check and no approval, through a login shell with any managed proxy stripped, and lands mid-turn when a turn is already active | SKILL inside playbook, mechanism §1 | `core/src/tasks/user_shell.rs`; `core/src/context/user_shell_command.rs` | `<user_shell_command>`; `strip_managed_proxy_env`; `inject_no_new_turn` |
| IDE context is injected unmarked into the user's own text item, split by `## My request for Codex:` | SKILL inside playbook, mechanism §1 | `tui/src/ide_context/prompt.rs`; `tui/src/chatwidget/ide_context.rs` | `Context from my IDE setup` |
| `<git_attribution>` is a developer fragment driven by the backend `commit_attribution_enabled` setting; disabled emits nothing from an absent prior | mechanism §1 | `ext/git-attribution/src/world_state.rs`; `ext/git-attribution/src/policy.rs`; `ext/git-attribution/src/lib.rs` | `Ignore any earlier instructions disabling Codex attribution` |
| `<shell_version>` is feature-gated: `powershell_shell_version` is under development and default off, and the element appears only for a single local environment whose shell is PowerShell, probed under a 2-second timeout and yielding a version only when the shell exits cleanly with at most 64 bytes of stdout, with the result memoized per shell path — misses included | mechanism §1 | `features/src/lib.rs`; `core/src/context/world_state/environment.rs` | `powershell_shell_version`; `POWERSHELL_VERSIONS` |
| …and a version that disappears renders as an explicit unavailable element rather than being dropped | mechanism §1 | `core/src/context/world_state/environment.rs` | `shell_version_removed` |
| Any project not explicitly *trusted* loads no project-local config, hooks or exec policies | SKILL inside playbook | `config/src/loader/mod.rs`; `config/src/config_toml.rs` | `project-local config, hooks, and exec policies` |
| A disabled config layer contributes nothing to the merged config | SKILL inside playbook | `config/src/state.rs` | `fn layers_low_to_high` |
| Project skill roots survive a disabled project layer | SKILL inside playbook | `ext/skills/src/host_roots.rs` | `SkillScope::Repo` |
| Four auth requirement fields are stripped from backend-delivered layers, and a disallowed stored credential is filtered to `None` rather than reported | mechanism Layer 0 | `config/src/requirements_layers/layer.rs`; `login/src/auth/manager.rs` | `LOCAL_ONLY_AUTH_REQUIREMENTS` |
| Web search mode defaults to `Cached`, which grants the same external access as `Disabled` (`external_web_access: false`) while still registering the tool, where `Disabled` returns no tool spec at all | SKILL outside playbook | `core/src/config/mod.rs`; `core/src/tools/hosted_spec.rs`; `ext/web-search/src/extension.rs` | `external_web_access_for_mode` |
| …and an unsupported preference walks a fallback list rather than erroring | SKILL outside playbook | `core/src/config/mod.rs` | `resolve_web_search_mode_for_turn` |
| …the four modes are `Disabled`, `Cached`, `Indexed`, `Live`, `Cached` is the `#[default]`, and `restrict_to` intersects two modes by taking the more restrictive | SKILL outside playbook | `protocol/src/config_types.rs`; `core/tests/suite/web_search.rs` | `enum WebSearchMode`; `fn restrict_to`; `web_search_mode_cached_sets_external_web_access_false` |
| …and the registered tool name is `web__run` | SKILL outside playbook | observed in the 2026-09-20 code-mode tool inventory; see the empirical section | `web__run` |
| `web.run` auto-ships a two-user-message tail with a 1,000-token assistant budget | mechanism §2 | `ext/web-search/src/history.rs`; `core/src/tools/spec_plan.rs` | `ASSISTANT_CONTEXT_TOKEN_LIMIT` |

## Results on the way back

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| Exec results are framed with exit code and wall time | mechanism §4 | `core/src/tools/mod.rs` | `Wall time:` |
| A spawn failure reaches the model as `exec_command failed: {err:?}` with no command echoed | SKILL triage, mechanism §4 | `core/src/tools/handlers/unified_exec/exec_command.rs` | `exec_command failed: ` |
| …and the whole message is middle-truncated at 900 bytes — a byte cap, not a character cap | mechanism §4 | `core/src/tools/handlers/unified_exec/exec_command.rs`; `utils/string/src/truncate.rs` | `EXEC_COMMAND_REJECTION_MAX_BYTES`; `truncate_middle_chars` |
| Timeout prefix | mechanism §4 | `core/src/tools/mod.rs` | `command timed out after` |
| PostToolUse can swap visible text while retaining the original | mechanism §4 | `core/src/tools/registry.rs` | `PostToolUseFeedbackOutput` |
| Unknown tool name | SKILL triage, mechanism §2 | `core/src/tools/registry.rs` | `unsupported call:` |
| PreToolUse veto wording forks on whether the call carries a command string | SKILL triage, mechanism §2 | `core/src/hook_runtime.rs` | `blocked by PreToolUse hook` |
| Hooks that exit before reading stdin keep their output | mechanism §2 | `hooks/src/engine/command_runner.rs` | `ErrorKind::BrokenPipe` |
| Unsupported modalities replaced with placeholder text | mechanism §4 | `core/src/context_manager/normalize.rs` | `content omitted because you do not support` |
| A rejected image ends the turn, no silent retry | mechanism §4 | `core/src/session/turn.rs` | `Invalid image in your last message` |
| Orphan tool outputs dropped before each request | mechanism §4 | `core/src/context_manager/normalize.rs` | `remove_orphan_outputs` |
| The two direct app-server MCP requests return a server-originated protocol error verbatim — its numeric `code`, `message` and `data` — while every other failure flattens to `-32603` with no `data`, and the mapper is the app-server's only structural read of a server error | mechanism §4 | `app-server/src/request_processors/mcp_processor.rs`; `app-server/src/error_code.rs` | `fn mcp_operation_error` |
| The structured arm fires only on `ServiceError::McpError`, and the `.with_context()` the connection manager adds preserves the downcast that finds it | mechanism §4 | `rmcp-client/src/service_error.rs`; `rmcp-client/src/rmcp_client.rs`; `codex-mcp/src/connection_manager.rs`; `codex-mcp/src/connection_manager/resources.rs`; `rmcp-client/tests/resources.rs` | `ServiceError::McpError`; `enum ClientOperationError` |
| The two requests are the wire methods `mcpServer/tool/call` and `mcpServer/resource/read` | mechanism §4 | `app-server-protocol/src/protocol/common.rs` | `mcpServer/tool/call` |
| On `mcpServer/resource/read` two codex-side steps also yield `-32603` — the config load before the spawn and the response deserialization after the mapper — while `mcpServer/tool/call`'s pre-mapper failures are `-32600` | mechanism §4 | `app-server/src/request_processors/mcp_processor.rs`; `app-server/src/request_processors/thread_input.rs` | `failed to deserialize MCP resource read response: ` |
| An unknown server and a tool excluded by codex's own `[mcp_servers.<name>]` `enabled_tools`/`disabled_tools` filter are plain `anyhow` errors, so they take the `-32603` arm | mechanism §4 | `codex-mcp/src/connection_manager.rs`; `codex-mcp/src/connection_manager/resources.rs`; `codex-mcp/src/tools.rs`; `config/src/mcp_types.rs` | `is disabled for MCP server`; `unknown MCP server` |

## Tool registry and session plumbing

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| Reserved names and duplicate external tools are dropped with only a local log | mechanism §2 | `core/src/tools/registry.rs` | `skipping external tool with reserved name` |
| Collisions are fatal only under an opt-in flag | mechanism §2 | `core/src/tools/spec_plan.rs` | `error_on_tool_collisions` |
| `clock.curr_time` and `clock.sleep` are registered by two independent gates, and `model_driven` sleep selection branches on the reminder feature instead of combining with it | SKILL off-by-default table, mechanism §2 | `core/src/tools/spec_plan.rs` | `model_has_clock`; `SleepToolMode::AlwaysOn` |
| `sleep_tool` is Stable/default-on with a `model_driven`/`always_on` mode, while `current_time_reminder` is under development, default off, and its `sleep_tool` sub-flag defaults to false | SKILL off-by-default table, mechanism §2 | `features/src/lib.rs`; `features/src/feature_configs.rs`; `core/src/config/mod.rs` | `SleepToolMode`; `sleep_tool_mode` |
| Only the hidden `gpt-6-astra` catalog entry advertises `clock`, and hidden entries are skipped when the default preset is chosen, so a default-configured session has neither clock tool | SKILL off-by-default table, mechanism §2 | `models-manager/models.json`; `models-manager/src/manager.rs` | `"slug": "gpt-6-astra"`; `mark_default_by_picker_visibility` |
| Shell commands receive `CODEX_SESSION_ID` and `CODEX_VERSION` | mechanism §2 | `core/src/exec_env.rs` | `CODEX_SESSION_ID_ENV_VAR`; `CODEX_VERSION_ENV_VAR` |
| `CODEX_VERSION` is re-asserted four ways — injected after the policy env is built, dropped from the exec-server policy's `set` and force-added to its `exclude`, always re-sent in the exec-server overlay, and re-exported after a shell snapshot is sourced | mechanism §2 | `core/src/unified_exec/process_manager.rs`; `core/src/tools/runtimes/mod.rs`; `core/src/tasks/user_shell.rs` | `inject_session_env`; `exec_env_policy_from_shell_policy`; `maybe_wrap_shell_lc_with_snapshot` |
| The same version rides outbound MCP request metadata as `codex_version`, inserted after client-supplied metadata is merged, so a client-supplied value is silently overwritten | mechanism §2 | `core/src/turn_metadata.rs`; `core/src/responses_metadata.rs`; `core/src/mcp_tool_call.rs`; `core/src/hook_runtime.rs`; `core/src/tools/handlers/extension_tools.rs` | `CODEX_VERSION_KEY`; `current_meta_value_for_mcp_request`; `filter_extra_metadata` |
| A retryable `ConnectionFailed` on a **sampling** request retries without a count limit — non-internal, non-Bedrock only; the feature is Stable/default-on | SKILL outside playbook, mechanism §5 | `features/src/lib.rs`; `core/src/responses_retry.rs` | `unbounded_connection_retries`; `is_amazon_bedrock` |
| …with backoff doubling from 5 s to a 60 s cap, and a server-side warning alongside the frontend notice | SKILL outside playbook, mechanism §5 | `core/src/responses_retry.rs` | `MAX_CONNECTION_RETRY_DELAY` |
| …emitting only a reconnect notice | mechanism §5 | `core/src/responses_retry.rs` | `waiting for network` |
| Remote compaction V2 can retain client-authored developer messages across the boundary | mechanism §5 | `core/src/compact_remote_v2.rs`; `features/src/lib.rs` | `retain_client_developer_messages`; `is_client_authored_developer_message` |
| MCP tool names are sanitized, hash-suffixed on collision and truncated at 128 bytes before registration, so MCP-vs-MCP names never reach the duplicate drop | mechanism §2 | `codex-mcp/src/tools.rs`; `core/src/config/mod.rs`; `features/src/lib.rs` | `MAX_TOOL_NAME_LENGTH` |
| A misalignment-policy verdict is typed non-retryable and the reference client kills the thread | mechanism §5 | `codex-api/src/sse/responses.rs`; `protocol/src/error.rs`; `tui/src/chatwidget/misalignment_policy.rs` | `MisalignmentPolicyViolation` |
| `invalid_grant` on a 400 is a permanent, cached refresh failure; other 400s stay transient | mechanism §5 | `login/src/auth/manager.rs`; `core/src/client.rs` | `is_invalid_grant_bad_request` |
| Queued messages committed by another process start turns on already-loaded, idle threads | mechanism §5 | `ext/queue/src/service.rs`; `state/src/runtime/queued_items.rs`; `app-server/src/extensions.rs` | `watch_external_messages` |

## Cross-cutting loops

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| Compaction presents the agent's own past as a stranger's | mechanism §5 | `prompts/templates/compact/summary_prefix.md` | `Another language model started to solve this problem` |
| `<turn_aborted>` marker | SKILL inside playbook, mechanism §5 | `core/src/context/turn_aborted.rs` | `turn_aborted` |
| Aborted calls become synthetic outputs | mechanism §5 | `core/src/tools/parallel.rs` | `aborted by user after` |
| One-time WebSocket→HTTPS fallback notice | mechanism §5 | `core/src/responses_retry.rs` | `Falling back from WebSockets` |
| Stop hooks fail open on timeout | mechanism §5 | `hooks/src/events/stop.rs` | `should_block` |
| MCP hook timeout defaults to 600 s; only `SessionEnd` and `Interrupt` clamp to a 1 s default / 3 s cap, and a SessionEnd MCP hook never reaches that clamp | mechanism §5 | `hooks/src/events/session_end.rs`; `hooks/src/engine/discovery.rs` | `SESSION_END_MAX_TIMEOUT_SEC`; `fn normalize_command_hook` |
| `HookHandlerConfig` accepts an `mcp_tool` handler type with `server`, `tool`, `input`, `timeout` and `statusMessage`; `prompt` and `agent` parse but are rejected at load | SKILL outside playbook, mechanism §5 | `config/src/hook_config.rs`; `hooks/src/engine/discovery.rs` | `rename = "mcp_tool"`; `prompt hooks are not supported yet` |
| An MCP hook's `input` map must be TOML-representable, because the normalized handler is what the trust hash covers | mechanism §5 | `config/src/hook_config.rs`; `hooks/src/engine/discovery.rs` | `MCP hook input must be representable as TOML`; `struct NormalizedHookIdentity` |
| `${a.b}` templates expand recursively over dotted object keys; a whole placeholder preserves JSON type, an embedded one stringifies, an unresolvable path fails the hook | mechanism §5 | `hooks/src/engine/mcp_runner.rs` | `fn expand_mcp_argument_template`; `hook input placeholder` |
| `SessionEnd` MCP hooks and empty `server`/`tool` are dropped at load, before normalization | mechanism §5 | `hooks/src/engine/discovery.rs` | `MCP hooks are not supported`; `server and tool must not be empty` |
| Local-source MCP hooks run sync against a live executor and can apply control effects; executor-scoped ones are forced async and cannot | mechanism §5 | `hooks/src/engine/mod.rs`; `hooks/src/engine/dispatcher.rs`; `core/src/session/session.rs` | `ConfiguredHandlerKind::McpTool { .. } => HookExecutionMode::Sync`; `CoreHookMcpExecutor {` |
| An MCP hook reports exit 0 on success, no exit code on failure and never writes stderr, so exit-code-2 blocking is unreachable and a failure fails open | mechanism §5 | `hooks/src/engine/mcp_runner.rs`; `hooks/src/events/pre_tool_use.rs` | `stderr: String::new()`; `Some(2) if handler.can_apply_control_effects()` |
| A hook MCP call produces no MCP tool call in the transcript — only a hook run summary carrying the handler type and source path, not the server/tool pair | SKILL outside playbook, mechanism §5 | `hooks/src/engine/mcp_runner.rs`; `hooks/src/engine/dispatcher.rs`; `hooks/src/engine/mod.rs` | `fn running_summary`; `ConfiguredHandlerKind::McpTool { .. } => HookHandlerType::McpTool` |
| Hook MCP calls go straight to the server connection — no approval, Guardian or app approval-mode gate; the surviving gates are unknown server, the server's config-level tool filter, readiness, and a timeout min'd against the server's own | SKILL outside playbook, mechanism §5 | `core/src/hook_mcp_executor.rs`; `codex-mcp/src/runtime.rs`; `codex-mcp/src/connection_manager.rs`; `codex-mcp/src/tools.rs`; `core/src/mcp_tool_call.rs` | `wait_for_server`; `is disabled for MCP server`; `is not connected`; `MCP tool call blocked by app configuration` |
| An MCP result marked `isError` becomes a hook failure | mechanism §5 | `core/src/hook_mcp_executor.rs` | `MCP tool returned an error` |
| `Interrupt` hooks fire on turn abort from core, skip subagent sessions, have no veto, parse only a system message, and merge in the last known step's executor hook sources so a turn with no local `Interrupt` hook still runs the executor's | mechanism §5 | `core/src/hook_runtime.rs`; `hooks/src/registry.rs`; `core/src/tasks/mod.rs`; `hooks/src/events/interrupt.rs`; `hooks/src/engine/output_parser.rs` | `fn run_turn_interrupt_hooks`; `executor_hook_sources_for_step`; `struct InterruptOutcome`; `fn parse_interrupt` |
| SessionEnd is root-session only | mechanism §5 | `core/src/hook_runtime.rs` | `SessionSource::SubAgent` |
| SessionEnd `async: true` is a no-op | mechanism §5 | `hooks/src/engine/discovery.rs` | `running async SessionEnd hook synchronously` |
| Review mode forces `never` and disables web search | mechanism §5 | `core/src/tasks/review.rs` | `WebSearchMode::Disabled` |
| Headless `codex exec` defaults to `never`; AutoReview lifts it | SKILL outside playbook, mechanism §3 | `exec/src/lib.rs` | `ApprovalsReviewer::AutoReview` |
| `codex exec --worktree` starts the session in a fresh managed worktree; it is exec-only, and resume, review, every other subcommand and the bare TUI are rejected | SKILL outside playbook, mechanism §3 | `exec/src/lib.rs`; `cli/src/main.rs` | `currently supports only`; `cannot resume an existing session` |
| `--worktree` also refuses `--ignore-user-config`, `--ephemeral`, and a remote default environment | mechanism §3 | `exec/src/lib.rs` | `--worktree requires local execution` |
| The `worktrees` feature is **Stable and default-on** as of this range (it was Experimental/default-off at the previous pin), and its gate config still sets `ignore_project_config`, so project config can neither enable nor disable it | SKILL outside playbook, mechanism §3 | `features/src/lib.rs`; `exec/src/lib.rs` | `key: "worktrees"`; `--worktree requires the worktrees feature` |
| …and `--worktree` is no longer exec-only: a bare interactive session, `codex exec`, `codex exec fork` and `codex fork` with an explicit session id all accept it | SKILL outside playbook, mechanism §3 | `cli/src/main.rs` | `fn reject_unsupported_worktree_for_subcommand` |
| …and an explicitly untrusted source is refused twice, the second time after allocation, leaking the checkout | mechanism §3 | `exec/src/lib.rs` | `requires a source that is not explicitly untrusted` |
| CLI allocation is host-scoped, shares Desktop's `git-worktree-root` (default `<codex_home>/worktrees`) and forces cleanup off; the crate is `codex-exec`'s alone, exposes no public removal, and nothing in codex-rs reads `auto_cleanup_enabled` or `keep_count` | SKILL outside playbook, mechanism §3 | `worktree/src/settings.rs`; `exec/src/lib.rs`; `worktree/src/lib.rs`; `exec/Cargo.toml` | `fn for_cli`; `auto_cleanup_enabled: false`; `codex-worktree` |
| The checkout is a detached, branchless copy of the source repository's committed `HEAD`, allocated as `<root>/<4-hex bucket>/<repo>` | SKILL outside playbook, mechanism §3 | `worktree/src/lib.rs`; `worktree/src/paths.rs` | `"--detach"`; `fn allocate_worktree_root` |
| Relative `--add-dir` is rebased before the cwd swap, session bootstrap config loads after it, and the thread id is bound into the checkout | mechanism §3 | `exec/src/lib.rs` | `fn bind_thread` |
| Inside the checkout the project-config layer and the AGENTS.md chain anchor on the new cwd, but trust resolves back to the linked worktree's main checkout, so the source repository's trust decision still applies | mechanism §3 | `config/src/loader/mod.rs`; `core/src/agents_md.rs`; `git-utils/src/trust.rs`; `core/src/worktree_trust_tests.rs`; `protocol/src/permissions.rs` | `fn get_writable_roots_with_cwd`; `fn resolve_root_git_project_for_trust`; `find_project_root`; `find_nearest_ancestor_with_markers` |

## Subsystems

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| **Multi-agent V1 is stable and default-enabled**, and V2 (`multi_agent_v2`) is Stable but default **off** | SKILL default surface, mechanism §6 | `features/src/lib.rs` | `key: "multi_agent"`; `key: "multi_agent_v2"` |
| The resolved version comes from the V2 feature, else the model catalog, else the `multi_agent` feature | SKILL default surface, mechanism §6 | `core/src/config/mod.rs` | `fn multi_agent_version_for_model` |
| V2 replaces `send_input`/`resume_agent`/`close_agent` with `send_message`, `followup_task`, `interrupt_agent`, `list_agents`; `wait_agent` is conditional | SKILL default surface, mechanism §6 | `core/src/tools/spec_plan.rs` | `SendMessageHandlerV2`; `wait_agent_enabled` |
| V1's tools register `Deferred` on a search-capable model | mechanism §6 | `core/src/tools/spec_plan.rs` | `fn search_tool_enabled` |
| Spawn depth defaults to 1 | SKILL default surface, mechanism §6 | `core/src/config/mod.rs` | `agent_max_depth` |
| Under V2 the catalog's `multi_agent.mode` replaces the built-in effort-derived delegation posture; config `multi_agent_mode_hint_text` outranks it, and the resolver returns nothing for V1 | SKILL Layer 0, mechanism §6 | `core/src/session/multi_agents.rs`; `protocol/src/openai_models.rs` | `fn effective_multi_agent_mode` |
| …an empty mode string deletes the section rather than restoring the built-in text, and custom text is truncated to 400 tokens | SKILL Layer 0, mechanism §6 | `core/src/context/multi_agent_mode_instructions.rs`; `core/src/context/world_state/multi_agent_mode.rs` | `MULTI_AGENT_MODE_MAX_TOKENS` |
| …and the resolver yields no mode for internal sessions or any subagent source but `ThreadSpawn` | mechanism §6 | `core/src/session/multi_agents.rs` | `SessionSource::Internal(_) \| SessionSource::SubAgent(_) => None` |
| …but a `None` mode is not silence: the world-state section still emits a bare `ExplicitRequestOnly` when the previous snapshot was `Proactive` or is unreadable | mechanism §6 | `core/src/context/world_state/multi_agent_mode.rs`; `core/src/context/world_state/mod.rs` | `PreviousSectionState::Unknown) => MultiAgentMode::ExplicitRequestOnly` |
| Built-in roles are currently inert | mechanism §6 | `core/src/agent/role.rs` | `built_in` |
| A role's override set is closed, and its `features` field honors only a *disable* of **five** capability features, keyed by `shell_tool`, `apps`, `plugins`, `memories`, `request_permissions_tool`, plus explicitly selected skills | SKILL default surface, mechanism §6 | `core/src/agent/role.rs`; `features/src/legacy.rs` | `struct AgentRoleOverrides`; `Feature::RequestPermissionsTool`; `fn feature_for_key` |
| …`personality` dropped out of that set in this range: the feature is `Stage::Removed` and no longer resolves through the role's `features` map, though the role's own `personality` override field remains | SKILL default surface, mechanism §6 | `core/src/agent/role.rs`; `features/src/lib.rs` | `Feature::Personality`; `Stage::Removed` |
| Authority is not inherited but *reapplied*: spawning copies the live parent turn's approval policy, approvals reviewer, cwd and permission-profile snapshot | SKILL default surface, mechanism §6 | `core/src/tools/handlers/multi_agents_common.rs` | `fn apply_spawn_agent_runtime_overrides` |
| …while model, provider, reasoning settings and developer instructions are set from the spawn path and may differ | SKILL default surface, mechanism §6 | `core/src/tools/handlers/multi_agents_common.rs` | `fn build_agent_shared_config` |
| …and a regression test asserts it | mechanism §6 | `core/src/agent/role_tests.rs` | `apply_role_cannot_expand_parent_authority` |
| Code-mode feature flags are under development, default off | SKILL off-by-default table | `features/src/lib.rs` | `CodeModeOnly` |
| Code mode exposes `exec` and `wait` | SKILL Layer 0, mechanism §6 | `code-mode-protocol/src/lib.rs` | `PUBLIC_TOOL_NAME` |
| `console` is deleted from the JS global object | mechanism §6 | `code-mode-runtime/src/runtime/globals.rs` | `delete_global(scope, global, "console")` |
| Nested tool calls go through the same router | SKILL Layer 0, mechanism §6 | `core/src/tools/code_mode/mod.rs`; `core/src/tools/parallel.rs` | `handle_tool_call_with_source` |
| A failed nested call becomes a rejected promise | SKILL Layer 0, mechanism §6 | `code-mode-runtime/src/runtime/mod.rs`, definition `code-mode-runtime/src/runtime/module_loader.rs` | `resolve_tool_response` |
| `CodeModeOnly` hides other tools from the menu | mechanism §6 | `core/src/tools/spec_plan.rs` | `DirectModelOnly` |
| Skills are reachable as `list` / `read` tools | SKILL default surface, mechanism §6 | `ext/skills/src/tools/list.rs`; `ext/skills/src/tools/read.rs` | `TOOL_NAME` |
| Both skills tools page against the calling turn's response budget: `min(512 KiB, 1.2 × the model's truncation policy)` direct, flat 512 KiB in code mode | SKILL default surface, mechanism §6 | `tools/src/tool_call.rs`; `ext/skills/src/tools/mod.rs`; `core/src/tools/handlers/extension_tools.rs`; `core/src/tools/lifecycle.rs` | `fn response_byte_budget` |
| …which is 48,000 bytes per direct page on all nine shipped slugs, every one of which declares `tokens`/10,000 at four bytes per token; the `bytes`/10,000 exception left the catalog with `gpt-5.2` | SKILL default surface, mechanism §6 | `models-manager/models.json` (all nine `truncation_policy` blocks); `utils/string/src/truncate.rs`; `protocol/src/protocol.rs` | `fn approx_bytes_for_tokens`; `fn byte_budget` |
| `list` pages at 20, drops entries whose own response or whose handles will not fit, and reports it once per budget value in `warnings[]` | mechanism §6 | `ext/skills/src/tools/list.rs` | `OVERSIZED_ENTRY_WARNING`; `MAX_SKILLS_PER_PAGE` |
| …then sheds the page, then provider warnings, then every skill (warnings-only page, same-offset cursor), before failing outright | mechanism §6 | `ext/skills/src/tools/list.rs` | `leaves no room for discovery warnings`; `skill metadata is too large to list` |
| `read` binary-searches the largest page that fits on JSON and UTF-8 boundaries | mechanism §6 | `ext/skills/src/tools/read.rs` | `skills.read response budget leaves no room for contents` |
| Both cursors are fingerprinted over the paged value, so changed content fails rather than splicing | SKILL default surface, mechanism §6 | `ext/skills/src/tools/mod.rs` | `cursor is stale; restart from the first page` |
| An executor `read` continuation may serve a pre-edit snapshot: one slot per thread, written only for executor authority with pages remaining and contents ≤ 1 MiB, never expired or cleared | SKILL default surface, mechanism §6 | `ext/skills/src/tools/read.rs`; `ext/skills/src/state.rs`; `ext/skills/src/provider.rs` | `ExecutorReadSnapshot` |
| …and this reshape is one the model is told about, in the tool's own description | mechanism §6 | `ext/skills/src/tools/read.rs` | `continue the same snapshot while it is cached` |
| **No general secret redaction in the exec output path** | SKILL default surface, mechanism §4 | `secrets/src/sanitizer.rs` | `redact_secrets` |
| Plugins can declare MCP servers and hooks | mechanism §6 | `plugin/src/manifest.rs` | `PluginManifestHooks` |
| An Agent Plugins `mcp.json` stdio server has no `env_vars` field under `deny_unknown_fields`, and its `env` values expand only `${PLUGIN_ROOT}` and `${PLUGIN_DATA}` | mechanism §6 | `codex-mcp/src/agent_plugin_config.rs` | `enum AgentPluginMcpServer`; `fn expand_agent_plugin_placeholders` |
| …so a colocated `.codex-plugin/plugin.json` overlays `env_vars` onto matching servers, gated on the Agent Plugins manifest format alone, only for names already declared and only stdio-to-stdio | mechanism §6 | `core-plugins/src/loader.rs`; `core-plugins/src/agent_plugin_mcp_overlay.rs` | `fn apply_codex_env_overlay` |
| …with `source: "remote"` `env_vars` entries discarded, the rest appended, and a matching literal `${NAME}` `env` entry removed — case-insensitively on Windows, exactly elsewhere | mechanism §6 | `core-plugins/src/agent_plugin_mcp_overlay.rs`; `config/src/mcp_types.rs` | `fn environment_names_match`; `fn is_remote_source` |
| …which is load-bearing because the literal `env` map is applied after the `env_vars` process-environment lookup and overwrites it | mechanism §6 | `rmcp-client/src/utils.rs`; `rmcp-client/src/stdio_server_launcher.rs` | `fn create_env_for_mcp_server` |
| The overlay's servers come from its own `mcpServers` object or the file it names, defaulting to `<plugin root>/.mcp.json`, and load as legacy config — skipping the Agent Plugins regular-file and inside-the-root checks | mechanism §6 | `core-plugins/src/agent_plugin_mcp_overlay.rs`; `core-plugins/src/manifest.rs`; `core-plugins/src/loader.rs` | `PluginManifestFormat::Legacy`; `DEFAULT_MCP_CONFIG_FILE` |
| A root `plugin.json` with an agent-plugins.org `$schema` wins over `.codex-plugin/plugin.json`, which then supplies the resolved manifest's `apps` and `hooks` paths and replaces its `interface` — unless a root `com.openai` extension object displaces it | mechanism §6 | `utils/plugins/src/plugin_namespace.rs`; `core-plugins/src/manifest.rs`; `core-plugins/src/agent_plugin_manifest.rs` | `fn find_plugin_manifest_path`; `fn apply_codex_agent_plugin_extension` |
| …but an Agent Plugins-format plugin loads neither apps nor hooks at runtime, so those two paths are inert while the `interface` override still reaches the listing | mechanism §6 | `core-plugins/src/loader.rs`; `core-plugins/src/manager.rs`; `core-plugins/src/loader_tests.rs` | `agent_plugin_overlay_apps_are_not_runtime_active`; `fn plugin_interface_with_marketplace_category` |
| Plan mode hard-blocks `update_plan` only | mechanism §6 | `core/src/tools/handlers/plan.rs` | `not allowed in Plan mode` |
| `update_plan` is opt-in: an absent or unset `[tools.update_plan]` resolves to off | SKILL off-by-default table, mechanism §6 | `core/src/config/mod.rs`; `config/src/config_toml.rs` | `fn resolve_update_plan_enabled`; `UpdatePlanToolConfig` |
| Registration is conditional on that resolved flag | mechanism §6 | `core/src/tools/spec_plan.rs` | `registry.add(PlanHandler)` |
| Disabling it strips codex's own checklist guidance — three headings unconditionally, `## Planning` only when the section carries the checklist text, plus the `Progress visibility:` pair and plan-tool bullets with their continuations | SKILL off-by-default table, mechanism §6 | `core/src/context/update_plan_instructions.rs` (marker guard at `20-24`) | `fn without_update_plan_instructions`; `## Plan Mode vs update_plan tool`; `You have access to an ` |
| The strip runs on model instructions, rendered base instructions, built-in collaboration presets, inherited multi-agent role text and the goal continuation template | mechanism §6 | `core/src/session/world_state.rs`; `core/src/session/mod.rs`; `core/src/context/world_state/collaboration_mode.rs`; `core/src/session/multi_agents.rs`; `core/src/agent/control/spawn.rs`; `ext/goal/src/steering.rs` | `update_plan_enabled` |
| Compaction and Guardian feedback read the already-stripped base instructions | mechanism §6 | `core/src/compact.rs`; `core/src/compact_remote_request.rs`; `core/src/compact_remote_v2_attempt.rs`; `core/src/guardian/feedback.rs` | `get_prompt_base_instructions` |
| Caller-authored instructions are left unstripped, under a different guard per site | SKILL off-by-default table, mechanism §6 | `core/src/session/multi_agents.rs`; `core/src/session/world_state.rs`; `core/src/session/mod.rs`; `core/src/context/world_state/collaboration_mode.rs` | `BaseInstructionsProvenance::Model`; `builtin_collaboration_mode_presets` |
| No `<collaboration_mode>` fragment in a default session | mechanism §6 | `core/tests/suite/collaboration_instructions.rs` | `no_collaboration_instructions_by_default` |
| zsh-fork backends are under development, default off | SKILL off-by-default table | `features/src/lib.rs` | `shell_zsh_fork` |
| `write_stdin_approval` is under development, default off | SKILL off-by-default table, mechanism §2 | `features/src/lib.rs` | `key: "write_stdin_approval"` |
| Per-exec denial wording | SKILL triage, mechanism §2 | `shell-escalation/src/unix/escalate_client.rs` | `Execution denied:` |
| Memories are stable but default off | SKILL off-by-default table | `features/src/lib.rs` | `"memories"` |
| The full shell surface is `exec_command` + `write_stdin` | mechanism §2 | `core/src/tools/spec_plan.rs` | `fn add_shell_tools` |
| …conditional on three gates, and `unified_exec` off degrades to a one-shot `exec_command` rather than removing the surface | mechanism §2 | `core/src/tools/spec_plan.rs` | `registry.add(ExecCommandHandler::one_shot` |
| The one-shot spec drops `tty`, `yield_time_ms` and the returned `session_id`, and adds `timeout_ms` defaulting to 10,000 ms | mechanism §2 | `core/src/tools/handlers/unified_exec/exec_command.rs` | `fn one_shot_exec_command_spec` |
| `shell_tool` and `unified_exec` are Stable/default-enabled | mechanism §2 | `features/src/lib.rs` | `key: "unified_exec"` |
| `unified_exec_zsh_fork` is `Removed`/default-true **but still a live gate**: zsh-fork mode ANDs `ShellTool`, `UnifiedExec`, `ShellZshFork` and `UnifiedExecZshFork` | SKILL off-by-default table, mechanism §3 | `tools/src/tool_config.rs`; `features/src/lib.rs` | `fn for_session`; `UnifiedExecShellMode` |
| …and a regression test asserts that disabling either zsh-fork flag forces Direct mode | mechanism §3 | `tools/src/tool_config_tests.rs` | `unified_exec_shell_mode_respects_feature_and_policy_gates` |
| The `shell` argument is resolved to a shell *type*, not launched as a path | mechanism §2 | `core/src/tools/handlers/unified_exec.rs`; `shell-command/src/shell_detect.rs` | `get_shell_by_model_provided_path` |
| Local zsh-fork rejects an explicit `shell` | mechanism §2 | `core/src/tools/handlers/unified_exec.rs` | `is not supported for local zsh-fork exec` |
| `shell_command` survives as a legacy catalog alias for unified exec | mechanism §2 | `protocol/src/openai_models.rs` | `alias = "shell_command"` |
| App-server `thread/shellCommand` runs unsandboxed with full access | SKILL outside playbook, mechanism §2 | `app-server-protocol/src/protocol/common.rs`; `app-server-protocol/src/protocol/v2/thread.rs` | `runs unsandboxed with full` |
| Code mode runs in a separate host process | mechanism §6 | `code-mode/src/remote_session.rs` | `OwnedCodeModeHost` |
| Guardian review injects a bounded root-conversation authorization block | mechanism §6 | `guardian-context/src/authorization.rs` | `ROOT CONVERSATION START` |
| The block also carries host-verified `request_user_input` answers, the verified paths of user-owned skills invoked on the latest root user turn, and an incomplete-evidence marker | mechanism §6 | `core/src/agent/control/user_authorization.rs`; `core/src/context/guardian_review_evidence.rs` | `GuardianRootMessage::IncompleteVerifiedAnswers`; `trusted_skill_paths` |
| `wait_agent` clamps a too-short timeout | mechanism §6 | `core/src/tools/handlers/multi_agents_v2/wait.rs` | `was clamped to the minimum of` |
| apply_patch refuses duplicate resolved paths | SKILL triage, mechanism §2 | `apply-patch/src/invocation.rs` | `multiple operations target` |
| apply_patch disables symlink traversal when the sandbox is bypassed | mechanism §2 | `core/src/tools/runtimes/apply_patch.rs` | `follow_symlinks` |
| Catalog supplies collaboration-mode text, `model_specialty`, and a per-model tool allowlist | SKILL Layer 0, mechanism Layer 0 | `protocol/src/openai_models.rs`; `core/src/tools/spec_plan.rs` | `experimental_supported_tools` |
| Managed requirements inject `<managed_developer_instructions>` as a separate developer message, replacing or removing the prior block | SKILL Layer 0 | `core/src/context/world_state/managed_developer_instructions.rs`; `core/src/session/mod.rs` | `REPLACEMENT_NOTICE`; `managed_developer_instructions` |
| …and a rendered block over 10,000 estimated tokens is rejected at config load | SKILL Layer 0 | `core/src/context/world_state/managed_developer_instructions.rs` | `MAX_MANAGED_DEVELOPER_INSTRUCTIONS_TOKENS` |
| `history.*` / `notes.*` need `use_history_notes_extension`, an OpenAI provider, and Codex-backend auth | SKILL off-by-default table, mechanism §6 | `ext/history-notes/src/extension.rs` | `use_history_notes_extension` |
| …and the same extension contributes a thread hint fetched from `alpha/notes/v2/thread_hint`, scoped to the calling agent's name | SKILL off-by-default table, mechanism §6 | `ext/history-notes/src/extension.rs`; `ext/history-notes/src/backend.rs` | `alpha/notes/v2/thread_hint`; `current_agent_name` |
| …injected as a `PromptSlot::ContextWindow` fragment of kind `notes.thread_hint`, the only producer of that slot outside tests | mechanism §6 | `ext/history-notes/src/extension.rs`; `core/src/session/mod.rs` | `notes.thread_hint`; `PromptSlot::ContextWindow` |
| The 4,000-byte bound is advertised to the backend as a truncation-policy header, not applied client-side; an oversized hint is dropped whole rather than truncated | SKILL off-by-default table, mechanism §6 | `ext/history-notes/src/extension.rs`; `ext/history-notes/src/backend.rs` | `MAX_THREAD_HINT_BYTES`; `TOOL_OUTPUT_TRUNCATION_POLICY_HEADER` |
| Failure, a missing `text` field and an overrun record `Failed`; an empty hint records `Succeeded` and contributes nothing | mechanism §6 | `ext/history-notes/src/extension.rs` | `ThreadHintStatus::Failed` |
| Context-window hints are collected, joined and rendered as the trailing lines of the developer-role `<context_window>` message alongside the agent path and window ids | mechanism §6 | `core/src/session/mod.rs`; `core/src/context/token_budget_context.rs`; `protocol/src/protocol.rs` | `context_window_hints`; `token_budget.context_window`; `CONTEXT_WINDOW_OPEN_TAG` |
| …while `token_budget.guidance_message` renders as a separate `<context_window_guidance>` fragment, not inside that body | mechanism §6 | `core/src/context/token_budget_context.rs`; `protocol/src/protocol.rs` | `token_budget.context_window_guidance` |
| The hint is fetched only when full context is built — the first real user turn, a new context window, and every compaction rebuild — not per turn in steady state or per sampling step | mechanism §6 | `core/src/session/mod.rs`; `core/src/compact.rs` | `build_initial_context_with_world_state`; `contribute_thread_context` |
| The fetch is awaited inline with a 35-second backend timeout | mechanism §6 | `ext/history-notes/src/backend.rs` | `HISTORY_NOTES_BACKEND_TIMEOUT` |
| The `<context_window>` message and the hint inside it render only when the token-budget feature is on and the turn has a resolved model context window; the contributor fetch happens before that check, so a turn with no window pays for the fetch and discards the hint | mechanism §6 | `core/src/session/mod.rs`; `core/src/config/mod.rs`; `features/src/lib.rs` | `Feature::TokenBudget` |
| With the token-budget feature on but `use_history_notes_extension` off, a legacy MCP `notes`/`thread_hint` bridge fills the same slot with `wait_for_server` set; the two paths are mutually exclusive and a failed native request does not fall back to the bridge | mechanism §6 | `core/src/session/mod.rs` | `latest_call_tool`; `thread_hint` |
| The catalog can activate token budgeting and the history/notes extension at thread startup, but only when neither a `features.token_budget` table nor an already-resolved `config.token_budget` exists and managed requirements have not pinned the feature off | SKILL off-by-default table, mechanism §6 | `core/src/session/token_budget.rs`; `core/src/session/mod.rs`; `protocol/src/openai_models.rs` | `fn apply_model_defaults` |
| …and a ChatGPT Plus/Pro/Pro-lite Codex-backend session with `context_management` on sets `use_history_notes_extension` on its own first, enabling `token_budget` in the same guard, which then blocks the catalog path | SKILL off-by-default table, mechanism §6 | `core/src/session/token_budget.rs` | `fn apply_experimental_context` |
| `token_budget` and `context_management` are both under development and default off; `config.token_budget` is resolved only from a `features.token_budget` table and only while the feature is enabled | SKILL off-by-default table, mechanism §6 | `features/src/lib.rs`; `core/src/config/mod.rs` | `fn token_budget_toml_config` |
| Both activation flags are fixed at thread startup; only the prompt halves re-resolve per turn from the active model, and only while no local prompt key is set — invalid model-owned defaults are logged and discarded whole | mechanism §6 | `core/src/session/token_budget.rs`; `core/src/session/turn_context.rs` | `ignoring invalid model-owned token-budget defaults` |
| Token budgeting registers `new_context` (direct-model-only) and `get_context_remaining` | SKILL off-by-default table | `core/src/tools/spec_plan.rs`; `core/src/tools/handlers/new_context_window_spec.rs`; `core/src/tools/handlers/get_context_remaining_spec.rs` | `NEW_CONTEXT_WINDOW_TOOL_NAME` |
| …and register direct-model-only; history is read-only and eventually consistent, notes are virtual/writable/strongly consistent | SKILL off-by-default table, mechanism §6 | `ext/history-notes/src/tools.rs` | `NOTES_DESCRIPTION` |
| The client-side history/notes caps are gone: arguments and the turn's truncation policy are forwarded to the backend, which enforces its own limits | mechanism §6 | `ext/history-notes/src/tools.rs` | `call.truncation_policy` |
| An attached environment independently constrains MCP authority; pending/failed attachments expose no environment-owned servers | mechanism §6 | `core/src/mcp.rs` (call site); `codex-mcp/src/catalog.rs` (definition) | `build_with_environment_authority` |
| …and a restricted attachment disables non-matching servers and empty-policy plugin registrations | mechanism §6 | `codex-mcp/src/catalog.rs` | `McpEnvironmentAuthority::Restricted` |
| `request_user_input_async` is catalog-gated and root-session-only; the catalog still advertises it under the old `send_user_message_async` name and supplies its description under that key, an empty string included | SKILL default surface, SKILL Layer 0, mechanism Layer 0 | `core/src/tools/spec_plan.rs`; `core/src/tools/handlers/request_user_input_async.rs`; `protocol/src/openai_models.rs` | `send_user_message_async: Option<ToolMessage>` |
| `send_message_to_user_async` is a second, independently catalog-gated async tool for free-form text | SKILL default surface, mechanism Layer 0 | `core/src/tools/spec_plan.rs`; `core/src/tools/handlers/send_message_to_user_async.rs` | `tool == "send_message_to_user_async"` |
| …and both return immediately while the turn continues | SKILL default surface | `core/src/tools/handlers/request_user_input_async.rs`; `core/src/tools/handlers/send_message_to_user_async.rs` | `"accepted":true` |
| Quoting a glob preserves it as a literal argv token rather than expanding it | SKILL inside playbook | `shell-command/src/bash.rs` | `preserves_quoted_literals` |
| `uses_codex_backend()` is false only for `ApiKey`, `BedrockApiKey` and `BedrockAccessKeys` | SKILL default surface, mechanism Layer 0 | `protocol/src/auth.rs` | `fn uses_codex_backend` |
| …consumed by the apps gate, connectors, the plugin marketplace, app routing, the cloud bundle and history/notes | SKILL default surface, mechanism §6 | `core/src/session/turn_context.rs`; `core/src/connectors.rs`; `core-plugins/src/manager.rs`; `core-plugins/src/app_mcp_routing.rs`; `cloud-config/src/service.rs`; `ext/history-notes/src/extension.rs` | `apps_enabled_for_auth`; `uses_codex_backend` |
| …but memories is **not** a consumer: non-codex-backend auth skips the rate-limit check rather than disabling memories | — | `memories/write/src/guard.rs` | `rate_limits_check` |
| Auth mode selects the curated marketplace and clears plugin-declared apps while leaving `mcp_servers` | mechanism §6 | `core-plugins/src/manager.rs`; `core-plugins/src/app_mcp_routing.rs` | `target_curated_marketplace` |
| Managed `[marketplaces] restrict_to_allowed_sources` is absent by default and is what arms the marketplace source allowlist | mechanism §6 | `config/src/config_requirements.rs`; `core-plugins/src/marketplace_policy.rs` | `restrict_to_allowed_sources` |
| The bundled curated source is the literal `https://github.com/openai/plugins.git` and is validated like any third-party Git source | mechanism §6 | `core-plugins/src/startup_sync.rs`; `core-plugins/src/marketplace_policy.rs` | `OPENAI_PLUGINS_GIT_URL` |
| A disallowed curated source skips the local curated repo sync — a path already conditional on the remote catalog being inactive — and the listing only offers a curated root that is already on disk | mechanism §6 | `core-plugins/src/manager.rs` | `maybe_start_curated_repo_sync_for_config` |
| …fails `validate_install` for the two curated marketplace names, filtering the curated marketplace out of the listing and rejecting installs from it | mechanism §6 | `core-plugins/src/marketplace_policy.rs`; `core-plugins/src/lib.rs`; `core-plugins/src/manager.rs` | `is_openai_curated_marketplace_name`; `is not allowed by requirements from` |
| …and drops curated `[plugins]` entries from the policy-filtered effective config, with a catch-all that drops any non-configured, non-curated entry too | mechanism §6 | `core-plugins/src/marketplace_policy.rs` | `policy_filtered_plugin_config` |
| A managed bundled marketplace configured at its own root under its own name is exempt from the allowlist; either half mismatched is refused without consulting it | mechanism §6 | `core-plugins/src/marketplace_policy.rs` | `managed_marketplace_config_name` |
| A `GitUrl` allowlist entry matches on exact URL equality after the normalization both sides share, and one that pins a `ref` never admits the curated source because the curated check passes no ref | mechanism §6 | `core-plugins/src/marketplace_policy.rs`; `core-plugins/src/marketplace_add/source.rs` | `is_none_or`; `fn normalize_git_url` |
| The marketplace allowlist fails closed on a defective entry: the compile error is returned from every source validation | mechanism §6 | `core-plugins/src/marketplace_policy.rs` | `invalid marketplace allowed source` |
| The cloud-delivered requirements bundle needs codex-backend auth and a business/education/enterprise plan; header auth reports no plan | mechanism Layer 0 | `cloud-config/src/service.rs`; `protocol/src/account.rs`; `login/src/auth/manager.rs` | `cloud_config_eligible_auth` |
| Workload identity is env-selected, immutable and rejects logout; `codex mcp-server` refuses it | mechanism Layer 0 | `login/src/auth/workload_identity.rs`; `login/src/auth/manager.rs`; `mcp-server/src/lib.rs` | `workload identity auth is managed by the host and cannot be logged out` |
| Only `ApiKey`, `BedrockApiKey` and `BedrockAccessKeys` are exempt from workspace restrictions; header auth is checked via its account id | mechanism Layer 0 | `login/src/auth/manager.rs` | `Login is restricted to workspace(s)` |
| Bedrock is two auth modes: `bedrockApiKey` holds a bearer token, `bedrockAccessKeys` holds an AWS access-key ID, secret access key and optional session token, under that same name on the app-server wire; both are also false in `has_chatgpt_account()` | mechanism Layer 0 | `protocol/src/auth.rs`; `login/src/auth/bedrock_access_keys.rs`; `app-server-protocol/src/protocol/common.rs` | `bedrockAccessKeys` |
| Both Bedrock modes count as the `api` login method; `allowed_login_methods` only filters the credential to `None`, while the separate `forced_login_method` key is enforced again at `codex exec` and TUI startup, clearing every configured auth store and exiting 1 | mechanism Layer 0 | `login/src/auth/manager.rs`; `exec/src/lib.rs`; `tui/src/startup_orchestration.rs` | `ChatGPT login is required, but an API key is currently being used` |
| Managed `enforce_residency` overrides a provider's configured residency header at request time rather than erroring, leaving only a startup warning | — | `model-provider/src/provider.rs`; `core/src/config/requirements.rs` | `because managed residency is required` |
| Every registered MCP tool is `Deferred` on a search-capable model, and a server's `omit_tools_from` removes it from any of the three exposure surfaces | SKILL default surface, mechanism §6 | `core/src/tools/spec_plan.rs`; `config/src/mcp_types.rs`; `protocol/src/config_types.rs` | `omit_tools_from` |
| Optional MCP servers get a shared startup grace, spent once per server rather than per capture, then are omitted with only a trace log while their startup continues in its own task; cached catalogs publish with `read_only_hint` cleared | SKILL default surface, mechanism §6 | `codex-mcp/src/connection_manager/tool_catalog.rs`; `codex-mcp/src/connection_manager.rs` | `omitting pending optional MCP server` |
| …the grace is `mcp_optional_startup_grace_ms`, default 1000 ms; `0` disables the shared grace and awaits each server's own `startup_timeout_sec` (30 s when unset) instead, while a cached catalog short-circuits ahead of both | SKILL default surface, mechanism §6 | `config/src/config_toml.rs`; `core/src/config/mod.rs`; `codex-mcp/src/mcp/mod.rs`; `codex-mcp/src/connection_manager/tool_catalog.rs`; `codex-mcp/src/rmcp_client.rs` | `mcp_optional_startup_grace_ms` |
| …a live config refresh carries the new grace into the session and the per-server cache re-keys its stored deadline when the grace value changes | mechanism §6 | `core/src/session/mod.rs`; `codex-mcp/src/tool_catalog_cache.rs` | `mcp_optional_startup_grace` |
| A selected-plugin MCP server is forced through startup only when its plugin ID is in the turn's `required_plugins`, populated by an explicit `plugin://` mention or its `@` plaintext link and skipped entirely in a Guardian-reviewer or internal-Guardian session | SKILL default surface, mechanism §6 | `codex-mcp/src/connection_manager/tool_catalog.rs`; `core/src/session/turn.rs`; `core/src/plugins/mentions.rs`; `utils/plugins/src/mention_syntax.rs`; `core/src/guardian/review.rs` | `collect_explicit_plugin_ids` |
| …and a config-required server is forced through startup only while it is not dormant or has no cached non-empty catalog, so a dormant required server with a cached catalog is published from cache like an optional one | mechanism §6 | `codex-mcp/src/connection_manager/tool_catalog.rs` | `startup_is_dormant` |
| `is not available to the model` fires when `prepare_mcp_call` resolves to no binding for that server/tool; the cached-catalog case leaves the tool model-visible with no call, while a server with no resolved owner permission profile has its tools skipped before the model-visible push | SKILL triage, mechanism §4, §6 | `core/src/mcp_tool_call.rs`; `core/src/session/mcp_runtime.rs`; `codex-mcp/src/binding.rs`; `codex-mcp/src/connection_manager/tool_catalog.rs` | `fn prepare_mcp_call` |
| Under `never`, an MCP permission prompt auto-approves for a Disabled/External profile or a Managed one with full-disk write — evaluated against the calling server's own owner profile — so the `requires approval` string appears only under a restricted Managed owner profile | SKILL triage, mechanism §4, §6 | `codex-mcp/src/mcp/mod.rs`; `core/src/mcp_tool_call.rs` | `mcp_permission_prompt_is_auto_approved` |
| A published MCP runtime resolves one permission profile per enabled server, in precedence order: host-owned Apps takes the thread's effective profile; else the attached environment named by the server's `environment_id` (workspace roots materialized); else the thread's profile for a local-environment or selected-plugin server; else no entry | mechanism §6 | `codex-mcp/src/mcp/mod.rs`; `core/src/session/mcp_runtime.rs`; `core/src/session/turn_context.rs` | `fn set_server_permission_profiles`; `permission_profile_with_workspace_roots` |
| A prepared call carries the owner profile validated at preparation, and approval review is handed that profile rather than the thread-wide one; the approval policy stays runtime-wide | SKILL triage, mechanism §6 | `codex-mcp/src/binding.rs`; `core/src/mcp_tool_call.rs` | `prepared MCP calls retain their immutable permission authority` |
| Threadless discovery and resource reads rebuild the config with `PermissionProfile::default()` — restricted managed filesystem with no entries, restricted network, which never auto-approves under `never` — for every server | mechanism §6 | `codex-mcp/src/mcp/mod.rs`; `core/src/connectors.rs`; `app-server/src/request_processors/apps_processor/installed.rs`; `protocol/src/models.rs`; `protocol/src/permissions.rs` | `fn for_threadless_operations` |
| …and a test asserts both the unattached-server drop and the threadless narrowing of a `Disabled` thread profile | mechanism §6 | `codex-mcp/src/mcp/mod_tests.rs` | `mcp_server_permissions_handle_unattached_and_threadless_servers` |
| An elicitation from a server with no resolved owner profile is declined before both the strict-auto-review branch and the policy check; that server's own profile feeds the auto-accept check, while the strict-auto-review branch turns only on the approval policy and reviewer handle | mechanism §6 | `codex-mcp/src/elicitation.rs` | `permission_profile_for_server` |
| MCP catalog enumeration aborts whole (100 pages, 2,048 items / 8,192 for Apps, 64 KiB cursor, startup-timeout bound) rather than truncating | mechanism §6 | `codex-mcp/src/pagination.rs`; `codex-mcp/src/rmcp_client.rs` | `MAX_MCP_CATALOG_ITEMS` |
| An MCP connection is keyed by `McpServerConnectionIdentity`; the configured `auth` and — for stored-OAuth connections only — the credential store and effective `McpOAuthRefreshMode` are compared, so an auth or refresh-mode change drops the live client instead of reusing it | mechanism §6 | `codex-mcp/src/server.rs`; `codex-mcp/src/connection_manager.rs`; `core/src/config/mod.rs`; `features/src/lib.rs` | `has_same_connection_config` |
| …while tool policy and presentation metadata stay publication-only, the stored credential snapshot and its contention flag are skipped by that comparison and weighed in `reusable_client`, `startup_timeout` is checked only for a connection that has not finished starting, and a changed `catalog_item_limit` still drops reuse | mechanism §6 | `codex-mcp/src/server.rs`; `codex-mcp/src/connection_manager.rs` | `struct McpServerView`; `Startup-only budget` |
| An MCP server with `auth = "chatgpt"` and a non-default `environment_id` fails startup before any transport opens unless it carries an inline `Authorization` header; the guard rejects a `bearer_token_env_var` and any non-empty `env_http_headers`, config parsing rejects `http_headers_helper` for such a server, and a stdio transport can never satisfy it | SKILL outside playbook, mechanism §6 | `codex-mcp/src/rmcp_client.rs`; `codex-mcp/src/server.rs`; `config/src/mcp_types.rs`; `codex-mcp/src/connection_manager_tests.rs` | `cannot use hosted ChatGPT authentication`; `only supported for local MCP servers` |
| …and that message is not authentication-required, so it is wrapped by `mcp_init_error_display`'s generic branch and emitted to the client as a failed startup status, never into the model's context | SKILL outside playbook, mechanism §6 | `codex-mcp/src/connection_manager/startup.rs`; `codex-mcp/src/connection_manager.rs`; `codex-mcp/src/connection_manager_tests.rs` | `` `failed to start:` `` |
| MCP elicitations: an auto-deny switch and a `tool_suggestion` decline precede everything; `never` declines, but an empty-schema standard form is auto-accepted first; a strict-auto-review elicitation fails closed **only where Guardian is consulted** — the strict branch calls the core reviewer before auto-accept, and both core shortcuts (Full Access, user-CUA) return an accept carrying the `approvals_reviewer: auto_review` meta the strict branch validates | mechanism §6 | `codex-mcp/src/elicitation.rs`; `core/src/session/mcp.rs` | `elicitation_is_rejected_by_policy`; `can_auto_accept_elicitation`; `mcp_elicitation_auto_meta` |
| When the core reviewer returns no decision the router forwards the elicitation to the client, so an ordinary non-empty-schema form reaches a human without full-access surfacing | mechanism §6 | `codex-mcp/src/elicitation.rs` | `NEXT_ELICITATION_REQUEST_ID` |
| Only standard MCP forms and the `openai/elicitation/create` variant can be surfaced as a full-access form; legacy `openai/form` never is, and any elicitation carrying `codex_approval_kind` is refused | mechanism §6 | `codex-mcp/src/elicitation.rs`; `protocol/src/mcp_approval_meta.rs` | `should_surface_form_in_full_access` |
| Full-access form surfacing is armed once at thread start — client declared `openai/standard-form-input`, `ThreadSource::User`, root agent — not per call | mechanism §6 | `core/src/thread_manager.rs`; `codex-mcp/src/runtime.rs`; `codex-mcp/src/elicitation.rs` | `enable_full_access_form_input`; `OPENAI_STANDARD_FORM_INPUT_EXTENSION_ID` |
| `openai/elicitation/create` is enabled only when the client's `openai/elicitation` capability carries an object-valued `form` key; the initialize path retains only that key | mechanism §6 | `rmcp-client/src/elicitation_client_service.rs`; `codex-mcp/src/client_capabilities.rs`; `app-server/src/request_processors/initialize_processor.rs`; `protocol/src/mcp.rs` | `supports_openai_elicitation_form`; `OPENAI_ELICITATION_EXTENSION_ID` |
| The TUI declares none of the OpenAI form extensions and resolves an OpenAI form variant as `Decline` | mechanism §6 | `tui/src/app/thread_routing.rs` | `startup_request_may_open_protected_view` |
| Catalog `browser_use`/`computer_use` confirmation policies are forwarded verbatim under `openai/confirmation_policies` to `node_repl`/`cua_repl` only, following the issuing step's model snapshot, excluding Guardian-reviewer sessions | mechanism §6 | `core/src/mcp_tool_call.rs`; `protocol/src/mcp.rs`; `core/src/guardian/review.rs` | `build_confirmation_policies_request_meta`; `CONFIRMATION_POLICIES_META_KEY`; `is_basic_session_source` |
| …and an eligible confirmation-policy call with no configured policies still receives an empty object, clearing runtime startup defaults | mechanism §6 | `core/src/mcp_tool_call.rs` | `CONFIRMATION_POLICIES_META_KEY` |
| A selected remote executor contributes its own eligible HTTP MCP servers to the thread | mechanism §6 | `core/src/session/mcp_runtime.rs`; `exec-server/src/environment_config.rs` | `discover_http_mcp_servers` |
| App-tool policy blocks a `codex_apps` call with its own model-visible string; the same evaluator also filters the menu, and both annotation hints default to `true` | SKILL triage, mechanism §4, §6 | `core/src/mcp_tool_call.rs`; `core/src/mcp_tool_exposure.rs`; `connectors/src/app_tool_policy.rs` | `MCP tool call blocked by app configuration` |
| `apps.<app>.links.<link_id>` carries only `approvals_reviewer` and `default_tools_approval_mode` — no enable/disable keys | SKILL inside playbook, mechanism §6 | `config/src/types.rs` | `struct AppLinkConfig` |
| App-tool approval mode resolves managed → per-tool → link → app → `apps.default` (connector-gated) → `Auto` | mechanism §6 | `connectors/src/app_tool_policy.rs` | `default_tools_approval_mode` |
| MCP reviewer resolves managed-model requirement → link → app → `apps.default`, and the app-resolved reviewer applies only if the managed `approvals_reviewer` requirement permits that value | mechanism §6 | `core/src/connectors.rs` | `mcp_approvals_reviewer_from_layers` |
| The link selector comes from tool metadata `link_id` unless the `codex_apps` meta sets `requires_explicit_link_id`, when it must be a non-blank string call argument; only the `codex_apps` server resolves a link at all | SKILL triage, mechanism §6 | `core/src/mcp_tool_call/account.rs`; `core/src/mcp_tool_call.rs` | `requires_explicit_link_id`; `This app tool requires a non-empty string link_id argument` |
| …and a rejected selector ends the call before the app-enabled check, before approval and before the server is contacted | SKILL triage, mechanism §6 | `core/src/mcp_tool_call.rs` | `McpToolAccountError` |
| Approval elicitations carry `link_id` and `link_is_implicit`, the latter keyed off the reserved `implicit_link::` prefix | mechanism §6 | `core/src/mcp_tool_call.rs` | `MCP_TOOL_LINK_IS_IMPLICIT_META_KEY`; `implicit_link::` |
| Apps/connector surface is default-on, gated only by auth; both non-auth inputs default true | mechanism §6 | `core/src/session/turn_context.rs`; `features/src/lib.rs`; `core/src/config/mod.rs`; `core/src/session/turn.rs`; `core/src/session/world_state.rs` | `apps_enabled_for_auth` |
| A hosted `codex_apps` remote MCP server is contributed whenever `Feature::Apps` is on; the contributor checks no auth | mechanism §6 | `ext/mcp/src/lib.rs`; `app-server/src/extensions.rs` | `hosted_plugin_runtime_mcp_server_config` |
| `image_gen.imagegen` is filtered out of the menu unless five gates pass, and its save path bypasses the filesystem sandbox | mechanism §6 | `core/src/tools/spec_plan.rs`; `ext/image-generation/src/tool.rs`; `ext/image-generation/src/artifact.rs`; `model-provider-info/src/lib.rs` | `image_generation_available` |
| Goal tools register only in app-server sessions with persistent thread state, excluding review subagents; the budget ceiling exists only once configured | mechanism §1 | `ext/goal/src/spec.rs`; `app-server/src/extensions.rs`; `ext/goal/src/extension.rs`; `ext/goal/src/runtime.rs`; `ext/goal/src/tool.rs` | `tools_available_for_thread` |
| A child thread's token usage is charged to the root goal's accounting state, transitively through each parent's own root, and that delta feeds the budget comparison | mechanism §1 | `ext/goal/src/extension.rs`; `ext/goal/src/accounting.rs`; `ext/goal/src/runtime.rs`; `state/src/runtime/goals.rs` | `record_descendant_token_usage` |
| The auto-block streak counts only a failure of the code-mode `exec` tool whose handler executed, in a turn with no successful tool; `exec` is the code-mode public tool name, not `shell`/`exec_command` | mechanism §1 | `ext/goal/src/accounting.rs`; `code-mode-protocol/src/lib.rs`; `core/src/tools/code_mode/mod.rs`; `core/src/tools/spec_plan.rs`; `core/src/tools/handlers/shell_spec.rs` | `consecutive_execution_failure_turns`; `PUBLIC_TOOL_NAME` |
| Any successful tool, a goal replacement, or a Plan-mode turn resets the streak; every other outcome is neutral | mechanism §1 | `ext/goal/src/accounting.rs`; `ext/goal/src/extension.rs` | `clear_current_turn_goal` |
| At the third qualifying turn, turn stop sets the goal to `Blocked` under the event name `execution-unavailable`, and skips it if the goal was replaced | mechanism §1 | `ext/goal/src/extension.rs`; `ext/goal/src/runtime.rs`; `state/src/model/thread_goal.rs` | `ExecutionUnavailable` |
| A `code_mode_only` session keeps direct-model-only tools and hosted tools; only code-mode-nested tools are hidden | SKILL Layer 0, mechanism §6 | `core/src/tools/spec_plan.rs`; `tools/src/tool_executor.rs`; `code-mode-protocol/src/description.rs` | `is_hidden_by_code_mode_only`; `fn hosted_model_tool_specs` |
| Ordinary `CodeMode` falls back to `Direct` when the host is unavailable; `CodeModeOnly` never does, and the host failure reaches the model as tool output | SKILL Layer 0, mechanism §6 | `core/src/tools/mod.rs`; `core/src/tools/code_mode/mod.rs`; `core/src/tools/code_mode/execute_handler.rs` | `disable_in_process_fallback`; `Code mode will fail closed` |
| `hide_spawn_agent_metadata` suppresses the inherited-model guidance and drops the nickname from the result; `non_code_mode_only` selects `DirectModelOnly` exposure | mechanism §6 | `core/src/tools/handlers/multi_agents_spec.rs` | `hide_spawn_agent_metadata`; `spawn_agent_output_schema_v2` |
| …it does **not** touch `service_tier`, which is not a spawn argument at all — the older claim was wrong at the previous pin too | mechanism §6 | `core/src/tools/handlers/multi_agents_v2/spawn.rs`; asserted by `core/src/tools/spec_plan_tests.rs` | `struct SpawnAgentArgs`; `deny_unknown_fields` |
| V2 rejects `fork_context`, and `followup_task` cannot target the root agent | mechanism §6 | `core/src/tools/handlers/multi_agents_v2/spawn.rs`; `core/src/tools/handlers/multi_agents_v2/message_tool.rs` | `fork_context is not supported in MultiAgentV2` |
| `is_non_root_agent` covers internal sessions as well as subagents, so both are excluded from the async user tools | SKILL default surface | `protocol/src/protocol.rs`; `core/src/tools/spec_plan.rs` | `fn is_non_root_agent` |
| A child's service tier is overwritten with the root thread's after the role is applied, so a role's `service_tier` is never effective | SKILL default surface, mechanism §6 | `core/src/tools/handlers/multi_agents_common.rs`; `core/src/tools/handlers/multi_agents/spawn.rs`; `core/src/tools/handlers/multi_agents_v2/spawn.rs` | `fn apply_spawn_agent_service_tier` |
| …and a child model that does not support the root's tier receives none | mechanism §6 | `core/src/tools/handlers/multi_agents_common.rs` | `supports_service_tier` |
| V1's tools register under a `multi_agent_v1` namespace rather than as bare names | SKILL default surface | `core/src/tools/handlers/multi_agents_spec.rs`; `core/src/tools/registry.rs`; `core/src/tools/spec_plan.rs` | `MULTI_AGENT_V1_NAMESPACE` |
| A V1 full-history fork fixes the child's role: `fork_context` together with `agent_type` is refused | SKILL default surface | `core/src/agent/child_config.rs`; pinned by `core/src/tools/handlers/multi_agents_tests.rs` | `Full-history forked agents inherit the parent agent type` |

| A `$mention` injects at most 8,000 bytes of the skill body, cut on a UTF-8 character boundary | SKILL default surface, mechanism §1 and §6 | `ext/skills/src/render.rs`; pinned by `ext/skills/src/render_tests.rs` | `MAX_SKILL_PROMPT_BYTES` |
| …and the cap is not universal: every extension-read skill is capped, a host skill only when it belongs to an agent plugin | same | `ext/skills/src/extension.rs`; `ext/skills/src/host_prompt.rs` | `is_agent_plugin_skill` |
| …and the truncation warning reaches the client, never the model | same | `ext/skills/src/extension.rs` | `ExtensionWarning`; `exceeded the main prompt context limit` |
| `ReasoningEffort::Persistent` injects a `<persistent_mode>` developer fragment; every other effort produces none | mechanism §1 | `core/src/context/world_state/persistent_mode.rs` | `persistent_mode.instructions` |
| …whose template's `{{ approval_request_channel }}` is filled only when the async user-message tool is registered | mechanism §1 | `core/src/context/world_state/persistent_mode.rs` | `approval_request_channel` |
| …and which replaces rather than appends on change | mechanism §1 | `core/src/context/world_state/persistent_mode.rs` | `REPLACEMENT_NOTICE`; `REMOVAL_NOTICE` |
| Classification, `apply_patch` authority and approval path convention follow the **reported executor** platform, not the host | SKILL outside playbook | `core/src/environment_selection.rs`; `core/src/tools/spec_plan.rs`; `core/src/tools/approvals.rs`; `core/src/tools/network_approval.rs` | `executor_platform_os` |
| …and an unrecognized or absent report resolves to `Unknown`, which carries no path convention — it is **not** coerced to Windows | SKILL outside playbook | `utils/path-uri/src/platform.rs` | `fn from_platform_os` |
| `thread/shellCommand`'s `timeoutMs` defaults to one hour; `0` is an immediate timeout, not unlimited; a negative value is refused | SKILL outside playbook | `app-server-protocol/src/protocol/v2/thread.rs`; `app-server/src/request_processors/thread_processor.rs` | `ThreadShellCommandParams`; `timeoutMs must be non-negative` |
| An idle app-server thread unloads after `thread_unload_delay_secs`, default 60 | SKILL outside playbook | `core/src/config/mod.rs` | `thread_unload_delay` |

| `unified_exec_tty` (stable, default on) gates the `tty` parameter; with it off the schema drops `tty` and an explicit `tty: true` is refused before classification | SKILL triage table | `features/src/lib.rs`; `core/src/tools/handlers/unified_exec/exec_command.rs`; `core/src/tools/spec_plan.rs` | `key: "unified_exec_tty"`; `TTY execution is disabled by config` |
| A resolved `use_history_notes_extension` on a model whose `supports_experimental_context` is false aborts startup rather than being ignored | SKILL off-by-default table, mechanism §6 | `core/src/session/mod.rs` | `supports_experimental_context`; `is not supported by model` |
| The macOS symlinked-writable-root rejection has one opt-out, `allow_symlinked_codex_home`, scoped to roots at or beneath that home and readable from the user config layer only | SKILL triage table, mechanism §3 | `sandboxing/src/seatbelt.rs`; `config/src/codex_home_symlink.rs`; `config/src/config_toml.rs` | `allow_symlinked_codex_home` |
| On Linux a seccomp filter is installed whenever the filesystem profile lacks full-disk write, even with network fully enabled: `AF_VSOCK` is denied in `VmSocketRestricted`, and `io_uring_*` in every socket-restricted mode | SKILL inside playbook, mechanism §3 | `linux-sandbox/src/landlock.rs` | `NetworkSeccompMode::VmSocketRestricted`; `SYS_io_uring_setup` |
| A goal also self-blocks with reason `empty-response` after three consecutive empty automatic continuation turns, outside code mode | mechanism §1 | `ext/goal/src/accounting.rs`; `ext/goal/src/runtime.rs` | `fn empty_response_goal`; `consecutive_empty_turns` |
| `send_message_to_user_async` gained its own under-development, default-off feature flag as a second opt-in beside the catalog key; `request_user_input_async` stayed catalog-only | SKILL default surface | `features/src/lib.rs`; `core/src/tools/spec_plan.rs` | `Feature::SendMessageToUserAsync` |
| The extension approval walk is first-claim-wins in registration order, and `require_fresh_review` downgrades a fast `Allow` rather than skipping the extension | mechanism §2 | `ext/extension-api/src/registry.rs`; `ext/extension-api/src/contributors/approval_review.rs`; `ext/guardian-reviewer/src/routing.rs` | `Returns the first claimed decision in registration order`; `require_fresh_review` |
| Description-shortening lost its warning entirely in this range; only whole-skill omission still warns, and only to the client | SKILL default surface, mechanism §6 | `ext/skills/src/render.rs`; absent: `SKILL_DESCRIPTION_TRUNCATED_WARNING` | `SKILL_DESCRIPTIONS_REMOVED_WARNING_PREFIX` |
| `thread/shellCommand` runs with `SandboxType::None`, `PermissionProfile::Disabled` and no managed proxy, and its result is injected as a user-role `<user_shell_command>` fragment | SKILL outside playbook | `core/src/tasks/user_shell.rs`; `core/src/context/user_shell_command.rs` | `USER_SHELL_TIMEOUT_MS`; `<user_shell_command>` |

---

## Empirically verified

Two probe families, run at different times against different builds. Do not read a row as re-verified unless its own family was — the tables are split for exactly that reason. Raw dumps and probe transcripts are not checked in.

### Prompt composition — re-run 2026-09-25, `codex-cli 0.158.0-alpha.8`

Dumped with `codex debug prompt-input` against the installed `codex-cli 0.158.0-alpha.8`, from a scratch git repository whose only file is a one-line `AGENTS.md`, under a **stock `CODEX_HOME`** carrying nothing but `model = "gpt-6-sol"` and `model_reasoning_effort = "high"` — no rules file, no plugins, no apps, no MCP servers, no auth. "Stock" has one leak: the skill root `r0` is `~/.agents/skills`, which sits outside `CODEX_HOME` and was still read. Eleven configurations succeeded: bare; `read-only`, `workspace-write` and `danger-full-access` each under `approval_policy=never`; `read-only` and `workspace-write` under `on-request`; `workspace-write` under `on-failure`; `--enable powershell_shell_version`; `sandbox_workspace_write.network_access=true`; `network_proxy.allowed_domains` / `denied_domains`; and a second bare dump. One was refused by the CLI (see the `untrusted` row). Three more held `CODEX_HOME` fixed and changed only `-c model=` (`gpt-6-astra`, `gpt-5.6-luna`, `gpt-5.4`), and two were taken under the machine's real `CODEX_HOME` (default and `approval_policy=never`) purely to compare composition.

This binary is main `53446f90a5` plus a release commit, 17 commits before the source pin. Two of those 17 change model-facing text this dump cannot show — `1cd253c987` edits the Guardian review prompt and `29f056c26c` the `request_user_input_async` schema — because `prompt-input` renders only the input list, not tool schemas or the Guardian reviewer's prompt. Those two surfaces are unprobed at this pass; nothing below speaks to them.

| Claim | How it was confirmed | Result |
|---|---|---|
| `<permissions instructions>`, `<environment_context>`, `# AGENTS.md instructions` markers are real | every dump | All three present in every dump, in that order |
| **The message shape is set by the model, not by `CODEX_HOME`** | the same stock home dumped with four models, then the real home | Changed since the previous run, which read the difference as a property of `CODEX_HOME`. With the home held fixed, `gpt-6-sol` and `gpt-6-astra` compose four messages — developer `[host_skills.instructions, permissions.instructions, collaboration_mode.instructions]`, developer `[multi_agent.role_instructions]`, developer `[multi_agent.mode_instructions]`, user `[agents_md.instructions, environments.environment_context]` — while `gpt-5.6-luna` and `gpt-5.4` compose two: developer `[host_skills.instructions, permissions.instructions]` and the same user message. The real home (also `gpt-6-sol`) reproduces the four-message shape and adds only `plugins.recommendations` to the first developer message. So a message count says which model family resolved, and neither the count nor a missing marker says anything about authorship |
| Harness-authored developer fragments observed at startup: `<skills_instructions>`, `<collaboration_mode>`, `<multi_agent_role>`, `<multi_agent_mode>`, `<recommended_plugins>` | same | The first four under both `gpt-6-*` models; `<recommended_plugins>` under the real home only. `<apps_instructions>` and `<plugins_instructions>` appeared in **no** dump, the real home included, though both are still in the binary |
| No developer fragment arrives unmarked | same | Changed: the previous run recorded the multi-agent usage hint as a bare developer message. At this build the `gpt-6-*` models wrap it in `<multi_agent_role>` — `You are `/root`, the primary agent in a team of agents…`, naming `spawn_agent`, `followup_task`, `send_message` and `fork_turns`, the V2 vocabulary — and the `gpt-5.*` models send no hint at all. `<multi_agent_role>` is in the source at both pins, so this row records an observation, not a dated code change |
| A default session receives a `<collaboration_mode>` fragment | every `gpt-6-*` dump | New at this run: `# Collaboration Mode: Default` opens it, followed by `You are now in Default mode. Any previous instructions for other modes (e.g. Plan mode) are no longer active.` It is absent under `gpt-5.*` |
| `<recommended_plugins>` is a developer-role fragment | the real-home dumps | Changed from user role: it is appended to the first developer message with kind `plugins.recommendations`, opening `Here is a list of plugins that are available but not installed.` The stock home, with no auth, carries none |
| The AGENTS.md body is wrapped in `<INSTRUCTIONS>` | every dump | Unchanged: `# AGENTS.md instructions` is followed by `<INSTRUCTIONS>…</INSTRUCTIONS>` around the collected text |
| `<environment_context>` is structured XML with a filesystem permission profile | every dump | Unchanged element by element: `<cwd>`, `<shell>`, `<current_date>`, `<timezone>`, then `<filesystem><workspace_roots>…<permission_profile type="managed"><file_system type="restricted">` with `<entry access="read\|write">` rows. Under `workspace-write` the rows are `read :root`, `write` on the cwd, `write :slash_tmp`, `write :tmpdir`, and explicit `read` entries for the root's `.git`, `.agents` and `.codex`. Under `read-only` the profile collapses to the single `read :root` entry under both `never` and `on-request` |
| `danger-full-access` renders a third profile shape | `-c sandbox_mode=danger-full-access -c approval_policy=never` | Unchanged: `<permission_profile type="disabled"><file_system type="unrestricted" />`, no `<entry>` rows, `<workspace_roots>` itself intact. `<permission_profile type="external">` is still in the binary and still reached by no configuration tried |
| Network domain lists in `<environment_context>` | `-c sandbox_workspace_write.network_access=true`; `-c network_proxy.allowed_domains=["example.com"] -c network_proxy.denied_domains=["evil.test"]` | Unchanged: no `<network>` element in any dump. The proxy-domain dump is byte-identical to the bare one; `network_access=true` changes only the announcement's `Network access is enabled`. The `<network enabled=` string group is still in the binary, trigger still undetermined |
| The permissions announcement discloses sandbox mode, read/write scope, and network state up front | every dump | Unchanged, byte for byte, in all three opening sentences the previous run recorded, including `No filesystem sandboxing - all commands are permitted. Network access is enabled.` with an ASCII hyphen |
| An unconfigured session in an untrusted directory resolves to `read-only` with escalation instructions | the bare dump | New at this run: the bare stock-home dump is byte-identical to `-c sandbox_mode=read-only -c approval_policy=on-request`. The machine's real home resolves the same directory to `workspace-write` |
| The announcement lists already-approved prefix rules verbatim | the real-home dumps | `## Approved command prefixes` appears only where persisted rules exist — never under the stock home — and disappears under `never` in the real home too (4,790 characters shrink to 797) |
| The `never` announcement text | `-c approval_policy=never` under each of `read-only`, `workspace-write` and `danger-full-access` | Unchanged, on its own line in all three: ``Approval policy is currently never. Do not provide the `sandbox_permissions` for any reason, commands will be rejected.`` |
| The `on-request` announcement forbids destructive and heredoc prefix rules | `-c sandbox_mode=workspace-write -c approval_policy=on-request` | Unchanged, including the trailing space on the `### Banned prefix_rules ` heading and on `NEVER provide a prefix_rule if your command uses a heredoc or herestring. ` |
| Under `never`, the escalation instructions are removed entirely rather than reworded | `workspace-write` under `on-request`, then `never` | Unchanged in kind: 4,211 characters shrink to 670 under the stock home, and `read-only` under `never` is 341. These are local measurements — the writable-roots line embeds this machine's paths — and are not comparable with the previous run's 4,966, which also carried persisted rules |
| `on-failure` is an announcement-layer alias of `on-request` | `-c sandbox_mode=workspace-write` under each | Unchanged, and now checked over the whole dump rather than the announcement alone: the two dumps are identical |
| `approval_policy = "untrusted"` is refused at startup | `-c approval_policy=untrusted` | Unchanged: exit status 1, empty stdout, stderr `Error: approval_policy = "untrusted" is no longer supported; remove this setting` |
| Escalation is requested via `sandbox_permissions: "require_escalated"` | the `workspace-write` + `on-request` dump | Unchanged, with a required `justification` and an optional `prefix_rule` |
| `<shell_version>` is feature-gated, default off, and PowerShell-only | `--enable powershell_shell_version` with `<shell>zsh</shell>` | Unchanged and still only **consistent with** the claim: the dump is byte-identical to the bare one, and `<shell_version status="unavailable" />` is still in the binary; the positive case needs a Windows host |
| No fragment marker the docs name is absent from this build | the dumps, plus a string search of the installed binary | All twenty-two markers named across `SKILL.md` and `mechanism.md` are in the binary, and so is `<multi_agent_role>`. Startup composition now covers `<permissions instructions>`, `<environment_context>`, `<skills_instructions>`, `<collaboration_mode>`, `<multi_agent_role>`, `<multi_agent_mode>`, `<recommended_plugins>` and `# AGENTS.md instructions`; `<collaboration_mode>` has left the mid-turn group, and `<apps_instructions>` and `<plugins_instructions>` have joined it in the sense that no startup dump carried them. Absence from a startup dump is evidence about *composition*, never about the build |
| `<skills_instructions>` is filled by the bundled catalog, plus whatever the skill roots hold | both homes | 20,415 characters under the stock home against 20,525 under the real one. The stock figure is not bundled-only: `r0` resolved to `~/.agents/skills` on this machine |
| The `<multi_agent_mode>` text is the bare `ExplicitRequestOnly` message | every `gpt-6-*` dump | Unchanged: `Any earlier instruction enabling proactive multi-agent delegation no longer applies. Do not spawn sub-agents unless …` — under the stock home now as well as the real one |

### Exec behavior — re-run 2026-09-20, `codex-cli 0.155.0-alpha.2.3`, `gpt-5.6-luna` at `xhigh`

One session was driven through the interactive TUI in a tmux pane (`-s read-only -c approval_policy=never`, cwd `/private/tmp/codex_probe_ro`) under the machine's real `CODEX_HOME`, whose `rules/default.rules` carries a `prompt` rule on `rm` — which is what selects the bare-constant branch in the forced-rm row. **Every string in this table was read out of the session's rollout JSONL, from the raw `custom_tool_call_output` bytes, rather than out of the model's own report.** That is the fix for the methodology failure recorded at the end of this section, and it is how this table should be re-run from now on: drive the session, then parse `~/.codex/sessions/<date>/rollout-*.jsonl` for `custom_tool_call` and `custom_tool_call_output` pairs.

| Claim | How it was confirmed | Result |
|---|---|---|
| **A default session is in code mode** | tool inventory dumped from inside `functions.exec` | Confirmed on `gpt-5.6-luna` as well. Direct tools were exactly `functions.exec`, `functions.wait`, `functions.request_user_input`, `mcp__cua_repl.js`, `mcp__cua_repl.js_reset`; `exec_command` and `apply_patch` were reachable only as `tools.exec_command(...)` / `tools.apply_patch(...)` |
| The `collaboration.*` tools are **not** a code-mode constant | same inventory | The previous run saw six of them as direct tools; this session had none. Their presence follows configuration, so do not read a code-mode tool list as fixed |
| Nested tool inventory is discoverable from inside `functions.exec` | `ALL_TOOLS.map(x => x.name)` | Returned `apply_patch`, `exec_command`, `write_stdin`, `view_image`, `read_mcp_resource`, `list_mcp_resources`, `list_mcp_resource_templates`, `image_gen__imagegen`, `create_goal`, `get_goal`, `update_goal`, `web__run`, the five `multi_agent_v1__*` tools, and every `mcp__<server>__<tool>` entry. `update_plan` was absent, consistent with its opt-in gate |
| **OS-jail denials reach the model as unwrapped, verbatim command stderr** | `touch /tmp/codex_probe_a.txt` via nested `exec_command` | Raw: `{"chunk_id":"e3219a","wall_time_seconds":0,"exit_code":1,"original_token_count":14,"output":"touch: /tmp/codex_probe_a.txt: Operation not permitted\n"}`. The result object now also carries `wall_time_seconds`, which the previously recorded shape did not |
| **Policy rejections reach the model nested in a spawn-error wrapper** | `rm -rf /tmp/codex_probe_dir` via nested `exec_command` | Confirmed from raw bytes for the first time: `exec_command failed: CreateProcess { message: "Rejected(\"approval required by policy, but AskForApproval is set to Never\")" }` — the bare constant with no command in it, which is the branch a `prompt` rule on `rm` selects. The source trace behind this wrapper is unchanged and still the authority for its text |
| **A denied nested call is a rejected promise, and an uncaught one fails the whole script** | the same call, once uncaught and once in `try`/`catch` | Uncaught: `Script failed`, error under `Script error:`. Caught: `Script completed`, same text under `Output:` |
| **The caught value is a plain string, not an `Error`** | the `catch` branch printed `String(e)`, `JSON.stringify(e)`, `e.name`, `e.message` | `String(e)` is the wrapper text; `JSON.stringify(e)` is a JSON *string*; `e.name` and `e.message` are both `undefined`. Reading `e.message` in a `catch` yields nothing |
| A 20,000-character nested `exec_command` output is not truncated at that size | `echo` of 20,000 `x`, once with no `max_output_tokens` and once with `10000` | 20,001 characters both times, no marker, `original_token_count: 5001` — byte-identical to the previous pass. Re-verified at this build |
| **The token-truncation marker's exact form** | the same `echo` under `max_output_tokens: 1000` | The output field is prefixed with `Warning: truncated output (original token count: 5001)`, then `Total output lines: 1`, then a blank line, and the whole field came back at 4,101 characters with `exit_code: 0`. Truncation is budget-driven, not size-driven |
| **V1 subagent tools are namespaced** | the same inventory | They register as `multi_agent_v1__spawn_agent`, `multi_agent_v1__wait_agent`, `multi_agent_v1__close_agent`, `multi_agent_v1__resume_agent`, `multi_agent_v1__send_input` — `MULTI_AGENT_V1_NAMESPACE` (`core/src/tools/handlers/multi_agents_spec.rs:14`), not the bare names |
| **A V1 full-history fork cannot also set `agent_type`** | `multi_agent_v1__spawn_agent({agent_type:"reviewer", fork_context:true, …})` | Rejected outright: `Full-history forked agents inherit the parent agent type; omit agent_type, or spawn without a full-history fork.` (`core/src/agent/child_config.rs:162`, pinned by `core/src/tools/handlers/multi_agents_tests.rs:375`) |
| **The spawn tool advertises each model's available reasoning efforts** | the `multi_agent_v1__spawn_agent` description | It enumerates `gpt-6-astra` low(default)/medium/high/xhigh/max/ultra, `gpt-5.6-sol` low(default)/…/ultra, `gpt-5.6-terra` low/medium(default)/…/ultra, `gpt-5.6-luna` low/medium(default)/high/xhigh/max (**no `ultra`**), `gpt-5.5` low/medium(default)/high/xhigh, all with service tier `priority`. The effort ladder differs per slug and the agent is told so |

The `strings -a` cross-check the previous pass used as a substitute for a session is no longer needed for this table, since the session itself was re-run; it remains a cheap way to confirm a quoted literal exists in a shipped binary without driving one.

Reproduce the prompt dumps by running `codex debug prompt-input` from a stable cwd, once per configuration: bare for the machine's own config, then `-c sandbox_mode=<read-only\|workspace-write\|danger-full-access>` crossed with `-c approval_policy=<never\|on-request\|on-failure>`, then `--enable powershell_shell_version`, `-c sandbox_workspace_write.network_access=true`, and `-c network_proxy.allowed_domains=[…]`. Diff the announcement (`.[0].content[1].text`) and the environment context (`.[3].content[2].text`) across them rather than reading each dump whole. Run the dumps under a stock `CODEX_HOME` when the question is about the harness, and under the real one when the question is about composition, because the two differ. Reproduce the exec probe by driving an interactive `codex -s read-only -c approval_policy=never` in a tmux pane, or `codex exec --json -s read-only -c approval_policy=never --skip-git-repo-check --ephemeral`, with the prompt below — then read the results out of `~/.codex/sessions/<yyyy>/<mm>/<dd>/rollout-*.jsonl` rather than off the screen:

```
This is a harness introspection probe. Report the exact names of every tool available to you and whether exec_command/apply_patch are direct or nested. Then run: touch /tmp/codex_probe_a.txt ; then run: rm -rf /tmp/codex_probe_dir ; quote both errors verbatim, do not retry. Then echo a 20000-character string of x and report the returned length and any truncation marker.
```

**A probe model cannot be trusted to quote an error string.** Both probe sessions independently reported the policy rejection as `` exec_command failed for `/bin/zsh -c '…'`: CreateProcess { … } `` — the shape this document itself claimed at the previous pin. That string does not exist: the only site that builds this message is `format!("exec_command failed: {err:?}")`, the fragment `exec_command failed for` appears nowhere in the tree, and the probe binary's own strings contain the new form and not the old one. The model reproduced a documented-looking shape rather than the bytes it received, which means a probe can silently confirm whatever the docs already say. Take verbatim strings from source; use probes only for structure that source cannot show — which tools are exposed, whether a denial arrives wrapped or bare, whether an uncaught rejection ends the script, whether output was truncated at all. **The 2026-09-20 re-run closes this hole a second way**: its strings come from the rollout's `custom_tool_call_output` bytes rather than from anything the model wrote, and read that way the wrapper matched source exactly. Parse the rollout, never the transcript.

## Re-verification procedure

1. **Scope the diff.** From a codex checkout. This is the union of every directory cited in the tables above — the previous pass listed only eleven of them, and every stale claim it missed sat in one of the nineteen it did not walk:

   ```
   git log --oneline <pinned>..HEAD -- \
     codex-rs/app-server codex-rs/app-server-protocol codex-rs/apply-patch codex-rs/cli codex-rs/cloud-config \
     codex-rs/code-mode codex-rs/code-mode-protocol codex-rs/code-mode-runtime codex-rs/codex-api \
     codex-rs/codex-mcp codex-rs/config codex-rs/connectors codex-rs/context-fragments codex-rs/core \
     codex-rs/core-plugins codex-rs/exec codex-rs/exec-server codex-rs/execpolicy codex-rs/ext \
     codex-rs/features codex-rs/git-utils codex-rs/guardian-context codex-rs/history codex-rs/hooks \
     codex-rs/linux-sandbox codex-rs/login codex-rs/memories codex-rs/model-provider \
     codex-rs/model-provider-info codex-rs/models-manager codex-rs/network-proxy codex-rs/plugin \
     codex-rs/process-hardening codex-rs/prompts codex-rs/protocol codex-rs/rmcp-client codex-rs/sandboxing \
     codex-rs/secrets codex-rs/shell-command codex-rs/shell-escalation codex-rs/skills codex-rs/state \
     codex-rs/tools codex-rs/tui codex-rs/utils codex-rs/windows-sandbox-rs codex-rs/worktree
   ```

   Regenerate this list from the **Source** column whenever rows are added, rather than editing it by hand. `codex-rs/mcp-server` was dropped at the 2026-09-19 re-pin because the crate no longer exists, and `codex-rs/agent-roles` and `codex-rs/windows-sandbox-service` at the 2026-09-24 re-pin because no row cites them any more; a pathspec naming a directory gone from the working tree still matches its history, so a stale entry fails silently rather than loudly.

2. **Check the anchors.** For each row, `rg` the **match on** column. A hit means the claim's anchor survives; follow it to confirm the surrounding logic still says the same thing. A miss means the constant or string was renamed or removed — re-derive the claim before editing the prose, and do not assume the behavior changed just because the identifier did.

3. **Navigate by identifier, not by line.** The **Source** column names files only. Line numbers were removed at the 2026-09-24 re-pin because they drifted faster than any pass repointed them — 297 of 386 rows in one five-day window, and that pass left them drifted — so most of them were already wrong. The **match on** column is the key. When a fragment occurs more than once in its file, sharpen it to its definition-site form (`fn name`, `struct Name`, `const NAME`, `key: "name"`) or to a longer literal until it hits once, and cite a line only when nothing sharper exists.

4. **Update the pin** at the top of this file and the verification line at the top of `SKILL.md`.

### Known gaps in the current pass

**The 2026-09-19 pass re-greped every row rather than sampling.** Seventeen agents split the 386 source rows into eight slices and re-derived each one at HEAD: **70 intact, 297 line-drift, 19 changed**. Every `changed` verdict was then handed to a separate adversarial verifier instructed to refute it and to default to "the auditor is wrong"; nine survived and were applied, and the ones that did not survive are not in these files. Two scouts walked the 685 in-range commits in the cited crates for behavior the ledger never had. The history before this pass is in git; what follows is what this pass did *not* close.

**The largest gap is mechanical, and it is deliberate: the `file:line` column was not bulk-repointed.** 297 of 386 rows drifted in line number while their behavior held, and each auditor reported the new location, but those were not written back — the ledger's own procedure says to treat `file:line` as a starting point and the **match on** column as the real key, and a bulk rewrite of 297 cells is a large diff with a large chance of silently mispairing a row. Expect a cited line to be off by a few and navigate by the identifier. Four citations are worse than drifted and are **dead**, because the code moved crates or the file is gone: `mcp-server/src/lib.rs` (the crate was deleted; `cli/src/main.rs:1264-1268` is the replacement), `rejected due to unacceptable risk` (now in the `codex-prompts` crate), `ext/history-notes/src/extension.rs:72-79` (agent-name scoping now flows through `HistoryNotesAgentIdentity`), and `core/src/tools/lifecycle.rs:135-138` (no truncation-policy plumbing left; see `core/src/tools/context.rs`). The `features/src/lib.rs` line citations on the feature rows were already wrong at the previous pin, so those rows cannot be verified as drift-from-a-correct-start at all.

**Three prose statements are known-imprecise and were left alone as out of scope for a correction pass**: mechanism §6 says `skills.list` drops an entry whose "authority, package, name or main_resource" exceeds 2,048 bytes, where the check at HEAD covers fewer fields; mechanism §6's "exact feature key" framing for a role's `features` disable is narrower than the code, since `feature_for_key` falls through to a legacy alias table; and the claim that the code-mode feature flags are under development and default off is over-broad, because `code_mode_host` is `Stage::Stable` with `default_enabled: true`. Three further mechanism lines still treat `codex mcp-server` as a live install context after its deletion.

**Two mechanisms were found beside audited rows and are not ledger claims, so they carry no verdict**: `MAX_THREAD_INSTRUCTIONS_TOKENS = 10_000` with `validate_thread_instruction_size`, and `mcp_permission_prompt_is_auto_approved` being bypassed entirely when turn-scoped `strict_auto_review` is on.

The largest unapplied clusters, so the next pass does not rediscover them:

- **Approvals and Guardian**: closed at the 2026-09-19 re-pin, and the closing corrected the prose it was filed against. mechanism §2 had said escalation and retries "bypass the extension shortcut entirely"; at HEAD they set `require_fresh_review`, which downgrades a fast `Allow` into a full synchronous pass without removing the extension from the path, so an extension returning `Reviewed(decision)` still decides an escalated call.
- **Tool surface**: closed at the 2026-09-19 re-pin. `<persistent_mode>` is documented in mechanism §1, together with the `{{ approval_request_channel }}` substitution and the replace-not-append notices, which the original note did not carry.
- **Skills**: closed at the 2026-09-19 re-pin, and the original note was imprecise twice over. The cap is real, but it does not apply to every mention — extension-read skills always, host skills only when they belong to an agent plugin — and it is not silent at the harness level, only silent to the model, because the warning leaves as an `ExtensionWarning` to the client. Both mechanism §1 and §6 now say so, and §6's "injects the full body" is gone.
- **Injected context**: closed. §1's fragment list now names the developer-role markers (`<skills_instructions>`, `<apps_instructions>`, `<plugins_instructions>`, `<multi_agent_mode>`) and the user-role `<recommended_plugins>`, records the `<INSTRUCTIONS>` wrapper around the AGENTS.md body, and scopes the marker rule to the user role — so the unmarked multi-agent usage hint no longer contradicts it. `<environment_context>` is documented as structured XML carrying a `<permission_profile>` in §1 and in the prompt-composition table above.
- **Executor platform**: closed at the 2026-09-19 re-pin. The backlog note was right but under-specified, and a first attempt at this entry got it backwards by reading only one of the three call sites. There is no single fallback rule: command classification sends every unrecognized string to Windows deliberately (`unified_exec/process_manager.rs:1474-1480`), an approval's path convention sends it to Posix (`tools/approvals.rs:596-604`), and `Platform::from_platform_os` sends it to `Unknown` (`utils/path-uri/src/platform.rs:17-24`). Quote the site, never the generalization.
- Smaller, closed at the 2026-09-19 re-pin: `thread/shellCommand`'s `timeoutMs` defaults to one hour, `0` means an immediate timeout rather than unlimited, and a negative value is refused; an idle app-server thread unloads after `thread_unload_delay_secs`, 60 by default rather than a constant.

**A pre-existing error the recount found**, corrected at the 2026-09-19 re-pin: the Granular rejection row quoted `approval required by policy, but AskForApproval::Granular.rules is false`, where `exec_policy.rs:53` says `approval required by policy rule, but …`. Only that one row of the three carries the extra `rule`, so a grep written from its neighbours misses it, and mechanism §4's table now says so.

What the sweeps themselves did not cover: the survey's per-slice reports each carry an honest `not_opened` list, and the coverage is uneven. Roughly 155 of 187 `tui` commits, about 90 of 160 `app-server` commits, and about 80 of 131 `protocol`/`features`/`config` commits were triaged from subject line plus file list without opening a diff. `ext/memories` was read at HEAD but its change history was not walked. Guardian V2's context composition, transcript retention and decision internals were read only at grepped sites.

**The empirical rows have a different status than the source rows, and the two empirical tables differ from each other.** The prompt-composition table was re-run on 2026-09-05 against `codex-cli 0.154.0-alpha.3`; the exec table still carries its 2026-09-04 `alpha.2` observations. Its two weakest rows were closed from source rather than by a new session — the rejection wrapper's exact text is now traced end to end through `orchestrator.rs`, `process_manager.rs`, `errors.rs` and `exec_command.rs`, and the forced-rm row's stated cause was wrong and has been corrected — and the shipped `alpha.3` binary was checked for every literal string those rows quote. What stays unverified at `alpha.3` is the *structure* the probe observed: that a rejection arrives wrapped while an OS denial does not, that a denied nested call is a rejected promise that can fail the whole script, and the exact tool inventory. Re-running the exec probe would settle only those. The probe methodology failed here in a way worth reading before trusting any future probe: see the note at the end of the empirical section.

### Traps

- **Do not trust "the file did not change in range" as evidence a claim is true.** It is evidence the claim did not *change*. The Windows degrade-vs-refuse error survived a previous review precisely this way.
- **Do not `rg` a full quoted template.** See the note at the top of this file.
