# Provenance ledger

Evidence for every load-bearing claim in `SKILL.md` and `references/mechanism.md`, so the next update is a diff against this table rather than a re-reading of the prose.

**Pinned:** codex-rs `main` @ `8e6a44b428`, 2026-09-04.

Paths are relative to `codex-rs/` in the [openai/codex](https://github.com/openai/codex) repository.

The **match on** column is what to `rg` for. It is deliberately *not* the full string as quoted in the prose: most quoted strings are `format!` templates whose placeholder names differ from the source (the docs say `unsupported call: {tool}`; the source says `format!("unsupported call: {tool_name}")`). Searching the whole template returns zero hits for claims that are perfectly correct. **Always match on the literal fragment between placeholders, or on the constant/type name.**

---

## Command classification (exec policy)

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| Banned allow-prefix list has 88 entries | mechanism §2 | `core/src/exec_policy.rs:56-145` | `BANNED_PREFIX_SUGGESTIONS` |
| Forbidden prefix never spawns | SKILL triage, mechanism §2 | `core/src/exec_policy.rs:1039` | `rejected: policy forbids commands starting with` |
| Dangerous-rm heuristic forbids outright | SKILL triage, mechanism §2 | `core/src/exec_policy.rs:799-806,1058-1096,1110-1117` | `rm -f style commands are not permitted` |
| …but the rm string only replaces the fallback reason when a forced-rm match is still identified at rejection time, and it is never consulted at all when a policy `prompt` prefix rule is what produced the decision | SKILL triage, mechanism §2, §4 | `core/src/exec_policy.rs:408-418,1058-1108`; `core/src/exec_policy_tests.rs:1414,2234,2256` | `derive_rejected_prompt_reason`; `prompt_is_rule` |
| Prompt suppressed under `never` | SKILL triage, mechanism §2 | `core/src/exec_policy.rs:47-48,219` | `approval required by policy, but AskForApproval` |
| Two Granular-specific rejections exist | mechanism §4 table | `core/src/exec_policy.rs:49-52` | `Granular.sandbox_approval is false` |
| Windows-without-backend prompts (or forbids under `never`) for every unmatched command under the downgraded managed read-only profile | SKILL Windows para, mechanism §3 | `core/src/exec_policy.rs:749,759,817` | `windows_managed_fs_restrictions_without_sandbox_backend` |
| Escalation request in a forbidding mode is scripted-rejected | SKILL triage, mechanism §2 | `core/src/tools/handlers/unified_exec/exec_command.rs:292` | `you cannot ask for escalated permissions` |
| One-shot migration strips banned saved rules | mechanism §2 | `execpolicy/src/sandbox_migration.rs:85` | `strip_banned_allow_rules` |
| The known-safe safelist module is gone; only the dangerous-command classifier remains | SKILL Windows para, mechanism §2, §3 | `shell-command/src/command_safety/mod.rs:8` | `pub mod is_dangerous_command` |
| Dangerous-command classification fails closed past wrapper depth 8 | mechanism §2 | `shell-command/src/command_safety/is_dangerous_command.rs:16,27` | `MAX_DANGEROUS_COMMAND_WRAPPER_DEPTH` |
| `approval_policy = "untrusted"` is a hard config error | SKILL mode-matrix note, SKILL outside playbook, mechanism §2 | `core/src/config/mod.rs:199-200,3606` | `UnsupportedUntrustedApprovalPolicyError` |
| App-server config writes reject it separately | mechanism §2 | `app-server/src/config_manager_service.rs:742` | `is no longer supported; remove this setting` |
| The CLI approval enum no longer spells `untrusted` | SKILL mode-matrix note | `utils/cli/src/approval_mode_cli_arg.rs:9-16` | `enum ApprovalModeCliArg` |
| An untrusted project resolves to `UnlessTrusted` by default | SKILL mode-matrix note, mechanism §2 | `core/src/config/mod.rs:3615-3622` | `active_project.is_untrusted()` |
| An app-server `thread/start` approval policy becomes a config override and bypasses the check | SKILL mode-matrix note, mechanism §2 | `app-server/src/request_processors/thread_processor.rs:1549,1556,1570-1571`; `core/src/config/mod.rs:3207` | `fn build_thread_config_overrides`; `approval_policy_override` |
| The v2 wire enum still exposes `"untrusted"` | mechanism §2 | `app-server-protocol/src/protocol/v2/shared.rs:174-177` | `enum AskForApproval` |
| Managed requirements pin the first allowed policy as the required default | mechanism §2 | `config/src/config_requirements.rs:917,1659-1665` | `allowed_approval_policies` |
| Under `unless-trusted` every unmatched command prompts | SKILL mode-matrix note, mechanism §2 | `core/src/exec_policy.rs:774-779` | `Projects marked untrusted require approval` |
| Unmatched-command fallback = policy × sandbox kind × sandbox-override request | mechanism §2 | `core/src/exec_policy.rs:769-812` | `requests_sandbox_override()` |
| The Windows no-backend guard needs a codex-managed, restricted, non-full-write profile | SKILL Windows para, mechanism §3 | `core/src/exec_policy.rs:749-751,817-825` | `profile_has_managed_filesystem_restrictions` |
| An explicit exec-policy allow still bypasses the sandbox | SKILL Windows para, mechanism §3 | `core/src/exec_policy.rs:419-422` | `bypass_sandbox` |
| Unquoted shell metacharacters abort literal parsing | SKILL inside playbook, mechanism §2 | `shell-command/src/bash.rs:243,260-274` | `is_literal_word_or_number` |
| …and the whole wrapper then becomes the policy subject | SKILL inside playbook, mechanism §2 | `core/src/exec_policy.rs:831-841` | `fn commands_for_exec_policy` |
| Cyber models / managed `ignore_rules` strip every Allow prefix rule | SKILL Layer 0, mechanism §2 | `core/src/session/turn_context.rs:246-261`; `core/src/exec_policy/model_policy.rs:11,27-52` | `IgnoreForCyberModel` |
| Two shipped catalog slugs carry `model_specialty: "cyber"` | SKILL Layer 0, mechanism §2 | `models-manager/models.json:408,526` | `"model_specialty": "cyber"` |
| …and no reusable prefix-rule amendment is proposed in that mode | SKILL Layer 0, mechanism §2 | `core/src/exec_policy.rs:340,362,434` | `auto_amendment_allowed` |
| Environments carry a restrictive-only exec-policy overlay | mechanism §2 | `core/src/session/environment.rs:78`; `core/src/exec_policy/model_policy.rs:15-25` | `environment command policy cannot contain allow rules` |

## Approval gate

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| Approval cache key is 7 fields (adds executable, tty) | SKILL inside playbook, mechanism §2 | `core/src/tools/runtimes/unified_exec.rs:89-97` | `UnifiedExecApprovalKey` |
| …and the lookup pairs that key with the selected environment's exec-policy fingerprint | SKILL inside playbook, mechanism §2 | `core/src/tools/approvals.rs:640-650` | `RequirementsExecPolicy::fingerprint` |
| Only `ApprovedForSession` decisions are cached; denials are not | SKILL triage, mechanism §2 | `core/src/tools/sandboxing.rs:88-97` | `ReviewDecision::ApprovedForSession` |
| Command is canonicalized before keying, while the executable field keeps the raw first token | SKILL inside playbook, mechanism §2 | `core/src/command_canonicalization.rs:5,14-36`; `core/src/tools/approvals.rs:207-215` | `__codex_shell_script__`; `executable: command.first().cloned()` |
| Denial reasons are propagated; the hook default is "PermissionRequest hook denied approval" and "rejected by configuration" is network-amendment-deny only | SKILL triage, mechanism §4 | `core/src/tools/approvals.rs:420`; `hooks/src/engine/output_parser.rs:413` | `PermissionRequest hook denied approval`; `rejected by configuration` |
| Guardian denial wording | SKILL triage, mechanism §4 | `core/src/guardian/review.rs:733` | `rejected due to unacceptable risk` |
| Human denial normalized — and *only* the exact string `rejected by user` is | SKILL triage, mechanism §4 | `core/src/tools/events.rs:441-448` | `exec command rejected by user` |
| Deserialization/client/transport failures keep the reason `approval request failed` | SKILL triage, mechanism §2 | `app-server/src/bespoke_event_handling.rs:1954,2042` | `approval request failed` |
| Abort tears down the whole turn — for the exec and patch approval ops only | mechanism §2 | `core/src/session/handlers.rs:196-208` | `ReviewDecision::Abort` |
| …but an MCP approval abort is converted back into a tool result (`user cancelled MCP tool call`), because MCP approvals travel through `request_user_input`, whose handler does not interrupt | SKILL triage, mechanism §2 | `core/src/mcp_tool_call.rs:296,1391-1396,1505-1512`; `core/src/session/handlers.rs:214-220` | `user cancelled MCP tool call` |
| MCP denial never contacts the server | SKILL triage, mechanism §4 | `core/src/mcp_tool_call.rs:1894` | `user rejected MCP tool call` |
| Escalation, retries **and** an elicitation marked `codex_sensitive_action` all set `require_synchronous_review`, which forces the synchronous path and skips extension fast approval wherever a Guardian review is already running | mechanism §2, §3, §6 | `core/src/guardian/review.rs:344-362` | `require_synchronous_review` |
| Guardian denial circuit breaker: 3 consecutive / 10-in-50, and 1/1 for a cyber-specialty model **only** — an `auto_review.ignore_rules` slug does not select it | SKILL triage, mechanism §2 | `core/src/guardian/mod.rs:55-59,163-183`; `core/src/guardian/review.rs:252-257` | `MAX_CONSECUTIVE_CYBER_GUARDIAN_DENIALS_PER_TURN`; `MODEL_SPECIALTY_CYBER` |
| `guardian_approval` is stable and default-enabled; guardian routing additionally requires `approvals_reviewer` to be `auto_review` (default `user`) and the policy to be `on-request` or granular | SKILL off-by-default table | `features/src/lib.rs:1386-1391`; `protocol/src/config_types.rs:183-189`; `core/src/guardian/review.rs:201-209` | `key: "guardian_approval"`; `routes_approval_policy_to_guardian` |
| A stored grant preapproves only an exactly-matching request | mechanism §2 | `core/src/tools/handlers/mod.rs:309` | `fn preapproved_permission_profile` |
| Strict auto-review can be enabled mid-turn | mechanism §2 | `core/src/state/turn.rs:255`; `core/src/session/mod.rs:2850` | `enable_strict_auto_review` |
| Managed requirements can force AutoReview by model slug | mechanism §2 | `config/src/requirements_layers/models.rs:22,49` | `required_on_models` |
| A network request disconnecting during approval cancels the *owning execution*, and only when one is attributed | SKILL triage, mechanism §3, §4 | `core/src/tools/network_approval.rs:354,660-676` | `before approval could complete`; `execution_id: owner_call` |
| …but the disconnect never cancels the approval reviewer or changes its decision | SKILL triage, mechanism §3 | `network-proxy/src/request_disconnect.rs:8-11` | `struct NetworkRequestDisconnect` |
| Codex delegates are refused unless approval policy is `never` | mechanism §5 | `core/src/codex_delegate.rs:65` | `Codex delegates require approval policy` |
| A required-auto-review model coerces full access to workspace-write and forces the Guardian reviewer at startup | SKILL Layer 0, mechanism §2 | `core/src/session/mod.rs:602-635` | `auto_review_required_for_model` |
| …and the validation that runs after it passes unless `guardian_approval` is off or the profile still has full-disk write, so at startup it bites only in those cases; a *later* settings change undoing either coercion is rejected the same way, with `To use model X, you need to use auto review.` | SKILL Layer 0, mechanism §2 | `core/src/session/session.rs:315-353`; `config/src/constraint.rs:19-20` | `AutoReviewRequired` |
| …and MCP *reviewer selection* returns Guardian ahead of any per-account or per-app reviewer setting, but the three skip-review shortcuts are evaluated separately and still fire, so a per-tool `approval_mode = "approve"` runs unprompted anyway | SKILL Layer 0, SKILL inside playbook, mechanism §2 | `core/src/connectors.rs:523-527`; `core/src/mcp_tool_call.rs:1315-1350,2220-2232` | `mcp_approvals_reviewer_from_layers`; `AppToolApproval::Approve` |
| `--approve-for-me` expands to exactly three config overrides | SKILL off-by-default table | `utils/cli/src/shared_options.rs:43-50,75-90` | `take_auto_review_config_overrides` |
| An `ApprovedMcpPolicyAmendment` decision writes `approval_mode = "approve"` into config.toml (server, plugin or apps variant) and degrades to a session-only memory on any write failure | SKILL inside playbook, mechanism §2 | `core/src/mcp_tool_call.rs:2006,2126-2140,2159-2172,2208-2248` | `ApprovedMcpPolicyAmendment` |
| …and whether an MCP tool prompts at all is decided per tool by its approval mode, defaulting to `auto`, where the server's own `read_only_hint` decides | SKILL inside playbook, mechanism §6 | `core/src/mcp_tool_call.rs:2201-2232`; `config/src/mcp_types.rs:23-31` | `requires_mcp_tool_approval_for_mode` |
| Turn-scoped `strict_auto_review` routes every approval to Guardian and cancels three MCP shortcuts; the only writer is a client `request_permissions` response | mechanism §2 | `core/src/mcp_tool_call.rs:1311-1351`; `core/src/tools/approvals.rs:508`; `core/src/session/mod.rs:2848-2851` | `strict_auto_review_enabled` |
| Persisting an execpolicy amendment is a second approval that outlives the call, so MCP persistence is not unique | mechanism §2 | `core/src/session/handlers.rs:181-187` | `ApprovedExecpolicyAmendment` |
| A remembered MCP approval is keyed by server + `connector_id` + `link_id` + tool name, and the persistent key is that same key | SKILL inside playbook, mechanism §2 | `core/src/mcp_tool_call.rs:1374-1379,1628-1656` | `struct McpToolApprovalKey`; `persistent_mcp_tool_approval_key` |
| …built only when the resolved mode is `Auto`, and never on `codex_apps` without a `connector_id`; with no key the prompt does not even offer session-remember or persist, so the call is re-prompted every time | mechanism §2 | `core/src/mcp_tool_call.rs:1484-1485,1633-1640`; `config/src/mcp_types.rs:26-32` | `session_mcp_tool_approval_key`; `allow_session_remember` |
| …but the apps persist path writes `apps.<connector_id>.tools.<tool>.approval_mode`, with no `link_id`, so a persisted approval widens from one account to the whole app; a failed write degrades to the still-account-scoped in-memory record | SKILL inside playbook, mechanism §2 | `core/src/mcp_tool_call.rs:2113-2140,2142-2192` | `persist_codex_app_tool_approval` |
| …and a persisted apps approval reloads the session user config layer, but app-tool policy reads the `McpConfig` captured when the call was prepared | mechanism §2 | `core/src/mcp_tool_call.rs:205-217,2172`; `codex-mcp/src/binding.rs:172-224` | `reload_user_config_layer`; `Returns the configuration and approval authority captured with this client` |
| `codex_sensitive_action` requires synchronous review when routed to automatic review, and by its own definition does not opt the action into review | mechanism §6 | `protocol/src/mcp_approval_meta.rs:10-12`; `core/src/session/mcp.rs:787-792,882,975` | `SENSITIVE_ACTION_KEY` |
| The user-CUA shortcut auto-approves an empty-schema form only for a `user` reviewer on a `node_repl`/`cua_repl` `js` call under connector `node_repl` with an `mcp_tool_call` approval kind, and is disqualified by `codex_sensitive_action` or `codex_requires_user_input` — so on that one shape the marker converts a silent auto-approval into a client-forwarded form | mechanism §6 | `core/src/session/mcp.rs:17,730-741,743-763`; `codex-mcp/src/elicitation.rs:357-360,370-430` | `user_cua_execution`; `codex_requires_user_input` |
| Full Access is approval policy `never` plus a disabled permission profile on the thread and every selected environment; a pending or failed environment is never Full Access | SKILL outside playbook, mechanism §6 | `protocol/src/environment.rs:28-52`; `core/src/environment_selection.rs:790-807` | `fn has_full_access` |
| …and it approves any empty-schema elicitation before `require_synchronous_review` is computed, then short-circuits Guardian review a second time inside `run_guardian_review` | SKILL outside playbook, mechanism §6 | `core/src/session/mcp.rs:743-763,787-792`; `core/src/guardian/review.rs:329-343` | `has_full_access` |
| Under the default `user` reviewer a sensitive elicitation that is not also marked `codex_strict_auto_review` never reaches Guardian: routing returns before review | SKILL outside playbook, mechanism §6 | `core/src/session/mcp.rs:934-947`; `core/src/guardian/review.rs:201-217` | `routes_approval_policy_to_guardian` |
| An empty poll, a `\u{3}` to a non-TTY process, and the `write_stdin_approval` feature being off skip terminal-input review entirely | SKILL off-by-default table, mechanism §2 | `core/src/unified_exec/stdin_approval.rs:185-193` | `Feature::WriteStdinApproval` |
| A terminal whose original environment is no longer selected is refused, on both the direct and Guardian paths (different wording on each) | mechanism §2 | `core/src/unified_exec/stdin_approval.rs:194-203`; `core/src/tools/approvals.rs:610-619` | `cannot access the terminal's` |
| Denied-read and environment-owned network restrictions the running terminal cannot enforce are refused rather than prompted, and network settings are normalized out of the denied-read comparison | SKILL triage, SKILL inside playbook, mechanism §2, §4 | `core/src/unified_exec/stdin_approval.rs:69-77,111-129` | `restrictions; start a new terminal` |
| Matching permissions skip approval unless `strict_auto_review` is set; retained grants, a launch bypass and any other drift require it | mechanism §2 | `core/src/unified_exec/stdin_approval.rs:130-139,214-219`; `core/src/unified_exec/process_manager.rs:833-837,884` | `fn review_requirement` |
| A NUL byte in terminal input is refused before the reviewer, because manual approval shell-quotes the input | mechanism §2, §4 | `core/src/unified_exec/stdin_approval.rs:220-225` | `NUL byte and cannot be reviewed safely` |
| The formatted action plus its reason is capped at 8,000 bytes, and a formatter truncation refuses on the same string | mechanism §2, §4 | `core/src/unified_exec/process_manager.rs:848-878` | `too large to review safely` |
| The approval reason names the launch authority and serializes retained grants as JSON; it is reviewer-facing and never reaches the model | mechanism §2 | `core/src/unified_exec/stdin_approval.rs:142-175` | `fn approval_reason` |
| Every terminal-input refusal reaches the model wrapped as `write_stdin rejected: {reason}`, and an approval that ends in a harness error as `write_stdin approval failed: {err}`; neither is rewritten into the exec/patch normalized form | SKILL triage, mechanism §2, §4 | `core/src/tools/handlers/unified_exec/write_stdin.rs:102-113`; `core/src/tools/events.rs:441-447` | `write_stdin rejected: ` |
| The human prompt for a terminal write offers only `Approved` and `Abort` — no per-call decline, no `ApprovedForSession` — so every reviewed write re-prompts and a human refusal aborts the turn through the exec-approval op | SKILL triage, SKILL inside playbook, mechanism §2 | `core/src/tools/approvals.rs:749,761,776`; `core/src/session/mod.rs:2638,2673-2691`; `core/src/session/handlers.rs:196-208` | `ExecApprovalKind::WriteStdin` |
| A terminal write needing any sandbox override is scripted-rejected under a forbidding policy before any reviewer runs, unless `strict_auto_review` is set under `never` | SKILL triage, mechanism §2 | `core/src/tools/approvals.rs:500-510`; `core/src/exec_policy.rs:48-52,216-238`; `protocol/src/models.rs:76-80` | `requests_sandbox_override` |
| By default no approval sits on the `write_stdin` path, and a running process keeps its launch-time sandbox regardless of later policy changes | SKILL inside playbook, mechanism §2 | `core/src/unified_exec/stdin_approval.rs:1-4,119-120`; `core/src/unified_exec/process_manager.rs:845,891` | `neither an earlier approval nor a policy` |

## OS sandboxing

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| **Windows downgrades workspace-write to read-only** | SKILL mode matrix, mechanism §3 | `core/src/config/permissions.rs:48-59` | `default_builtin_permission_profile_name` |
| …and a test asserts exactly that | — | `cli/src/debug_sandbox.rs:938` | `downgrades to read-only when the Windows sandbox is disabled` |
| Windows sandbox backend ships disabled | SKILL mode matrix, mechanism §3 | `features/src/lib.rs:1076-1080` | `experimental_windows_sandbox` |
| `CODEX_SANDBOX=seatbelt` is macOS-only | SKILL inside playbook, mechanism §3 | `core/src/sandboxing/mod.rs:178-180` | `CODEX_SANDBOX_ENV_VAR` |
| `CODEX_SANDBOX_NETWORK_DISABLED` on every platform | SKILL inside playbook, mechanism §3 | `core/src/spawn.rs:21,86-88` | `CODEX_SANDBOX_NETWORK_DISABLED` |
| Protected workspace metadata is deleted and forces exit 1 | mechanism §3 | `linux-sandbox/src/linux_run_main.rs:1236` | `sandbox blocked creation of protected workspace metadata path` |
| Seatbelt profile is deny-by-default | mechanism §3 | `sandboxing/src/seatbelt_base_policy.sbpl:8` | `(deny default)` |
| Linux network syscalls return EPERM via seccomp | mechanism §3 | `linux-sandbox/src/landlock.rs:250-253` | `SeccompAction::Errno` |
| Denial detection is 7 keywords + exit codes + SIGSYS | SKILL triage, mechanism §3 | `sandboxing/src/denial.rs:52-59` | `landlock` |
| The single retry-unsandboxed gate | mechanism §3 | `core/src/tools/orchestrator.rs:552` | `retry without sandbox` |
| `UnlessTrusted` is the only policy the default `wants_no_sandbox_approval` accepts for every tool; `Granular` needs its `sandbox_approval` flag, and the apply_patch runtime overrides the default to accept `OnRequest` too | SKILL mode matrix, mechanism §2 | `core/src/tools/sandboxing.rs:329-337`; `core/src/tools/runtimes/apply_patch.rs:132-139`; `core/src/tools/orchestrator.rs:369,486` | `fn wants_no_sandbox_approval` |
| …and a denied-read filesystem policy blocks the unsandboxed attempt entirely | SKILL mode matrix, mechanism §2 | `core/src/tools/sandboxing.rs:275-279`; `core/src/tools/orchestrator.rs:232,391` | `fn unsandboxed_execution_allowed` |
| Blocked host wording | mechanism §3 | `core/src/tools/orchestrator.rs:404` | `is blocked by policy` |
| `LD_*` / `DYLD_*` stripped | mechanism §3 | `process-hardening/src/lib.rs:60,79,99` | `remove_env_vars_with_prefix` |
| Explicit loopback allowlist entries now connect | mechanism §3 | `network-proxy/src/connect_policy.rs:87,102,124` | `allows_non_public_target` |
| Missing protected paths are materialized on Linux as synthetic empty read-only mounts; macOS emits deny regexes covering paths that do not exist | mechanism §3 | `linux-sandbox/src/bwrap.rs:1044,1077`; `sandboxing/src/seatbelt.rs:644` | `append_missing_read_only_subpath_args` |
| Windows kills process trees via job objects | mechanism §3, §5 | `utils/pty/src/win/job.rs:34` | `JobObject` |
| `.git`/`.agents`/`.codex` carve-out is one shared list rendered by three backends | mechanism §3 | `protocol/src/permissions.rs:29,37,1238` | `PROTECTED_METADATA_PATH_NAMES` |
| …and Windows renders it as deny ACEs, dropping any path that does not yet exist | mechanism §3 | `windows-sandbox-rs/src/allow.rs:27-31,36-37` | `add_deny_path` |
| macOS denies unlink of writable dir roots and protected ancestors | mechanism §3 | `sandboxing/src/seatbelt.rs:508,644` | `deny file-write-unlink` |
| macOS refuses *nested* symlinked writable-root components, normalizing top-level aliases such as `/tmp -> /private/tmp` instead | SKILL triage, mechanism §3 | `sandboxing/src/seatbelt.rs:396-405,441-450` | `symlinked writable roots are not supported`; `fn nested_symlink_component` |
| macOS preferences policy is appended only under full-disk read | mechanism §3 | `sandboxing/src/seatbelt.rs:23,992` | `MACOS_SEATBELT_PREFERENCES_POLICY` |
| Linux drops all capabilities | mechanism §3 | `linux-sandbox/src/bwrap.rs:294,352` | `--cap-drop` |
| Linux sandbox runs as PID 1 and reaps orphans | mechanism §3 | `linux-sandbox/src/launcher.rs:39,197` | `--as-pid-1` |
| Linux materializes synthetic empty protected dirs | mechanism §3 | `linux-sandbox/src/bwrap.rs:1044,1077` | `append_missing_read_only_subpath_args` |
| Project-root discovery requires a `HEAD` file inside `.git` | mechanism §3 | `config/src/loader/mod.rs:1344-1348` | `join("HEAD")` |
| A named list strips five auth variables from model-reachable children, explicit command overrides included | mechanism §3 | `protocol/src/shell_environment.rs:13-50` | `NON_INHERITABLE_ENV_VARS` |

## Limits and truncation

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| Exec timeout 10 s, exit forced to 124 | mechanism §3 | `core/src/exec.rs:61,68,766` | `DEFAULT_EXEC_COMMAND_TIMEOUT_MS` |
| stdout, stderr, and aggregate share one 1 MiB constant | mechanism §3 | `utils/pty/src/lib.rs:12`, `core/src/exec.rs:79,264-268,732` | `DEFAULT_OUTPUT_BYTES_CAP` |
| Unified-exec wait constants | mechanism §3 | `core/src/unified_exec/mod.rs:73,76,77` | `MIN_EMPTY_YIELD_TIME_MS` |
| Empty polls clamp to `[5 s, configured max]`; non-empty capped at 30 s | mechanism §3 | `core/src/unified_exec/process_manager.rs:943-951` | `clamp(MIN_EMPTY_YIELD_TIME_MS` |
| Tool-schema compaction is 4 progressive passes over a 5,000-byte threshold | SKILL default surface, mechanism §1 | `tools/src/json_schema/compaction.rs:16,23-39,135` | `LARGE_SCHEMA_COMPACTION_PASSES` |
| Model-visible tool schemas are a fixed whitelist; bounds, `pattern`, `format`, `default` are dropped at deserialization | SKILL default surface, mechanism §1 | `tools/src/json_schema/types.rs:36-72`; `tools/src/json_schema.rs:45-54` | `pub struct JsonSchema`; `min_items` |
| Nothing forwards the raw schema JSON: every wire form is `JsonSchema`-typed | SKILL default surface, mechanism §1 | `tools/src/tool_definition.rs:10`; `tools/src/responses_api.rs:41`; `tools/src/tool_spec.rs:31,144-149` | `input_schema: JsonSchema`; `create_tools_raw_json_for_responses_api` |
| Numeric keywords infer `number` and `format` infers `string` on an untyped node, then are discarded | mechanism §1 | `tools/src/json_schema.rs:129-137`; `tools/src/json_schema_tests.rs:199-213` | `multipleOf`; `infers_number_from_numeric_keywords` |
| An untyped node whose only keywords are discarded ones is cleared to `{}` | mechanism §1 | `tools/src/json_schema.rs:138-140` | `map.clear()` |
| `const` is rewritten into a one-element `enum` | mechanism §1 | `tools/src/json_schema.rs:110-112` | `map.remove("const")` |
| The bound drop applies on the no-compaction parse path too | mechanism §1 | `tools/src/json_schema.rs:32-36`; `ext/web-search/src/tool.rs:59` | `parse_tool_input_schema_without_compaction` |
| The 5,000-byte budget is measured on the normalized, already-lossy schema | mechanism §1 | `tools/src/json_schema/compaction.rs:45-54` | `compact_normalized_schema_len` |
| Compaction pass 1 strips per-parameter `description` keys inside the schema, not the tool's own description | SKILL default surface, mechanism §1 | `tools/src/json_schema/compaction.rs:56-63`; `tools/src/responses_api.rs:34` | `strip_schema_descriptions`; `pub description: String` |
| Per-tool `output_token_limit` is a positive token budget merged by `min`, and the merge never touches approval mode | SKILL outside playbook, mechanism §4 | `config/src/mcp_types.rs:75-92`; `core-plugins/src/loader.rs:970-987,1432-1486` | `restrict_output_token_limit` |
| The effective MCP truncation policy is captured from the executing call's config snapshot, falling back to the model's own policy | mechanism §4 | `core/src/tools/handlers/mcp.rs:220-247`; `codex-mcp/src/binding.rs:271-281` | `fn output_token_limit` |
| Global `tool_output_token_limit` rewrites the model's truncation policy, which is the fallback when no per-tool limit is set | SKILL outside playbook, mechanism §4 | `config/src/config_toml.rs:309-310`; `models-manager/src/model_info.rs:38-50` | `tool_output_token_limit` |
| Model-facing MCP output is cut at policy × 1.2 (the 20% serialization allowance) and the history copy is cut at the same budget via `fallback_token_limit_override` when recorded, including on resume | SKILL inside playbook, mechanism §4 | `core/src/tools/context.rs:126-127,171-177`; `core/src/context_manager/history.rs:287-311`; `history/src/lib.rs:53-57`; `protocol/src/protocol.rs:3298-3306,3318-3331` | `fallback_token_limit_override` |
| `PostToolUse` receives the whole serialized `CallToolResult` including `_meta`, while the Code Mode copy of the same result has `_meta` deliberately stripped | SKILL inside playbook, mechanism §4 | `core/src/tools/context.rs:137-147`; `tools/src/tool_output.rs:182-208`; `core/src/tools/handlers/mcp.rs:445-461` | `must not reach Code Mode` |
| A Code Mode script's yielded output is re-cut at Code Mode's own token budget, not at the per-tool MCP limit | SKILL outside playbook, mechanism §4 | `core/src/tools/code_mode/mod.rs:250-292,326-343` | `truncate_code_mode_result` |
| The MCP completion event is byte-capped at the 1 MiB `DEFAULT_OUTPUT_BYTES_CAP` and collapses an oversized result to a text preview with `structured_content` and `_meta` dropped | SKILL inside playbook, mechanism §4 | `core/src/mcp_tool_call.rs:116,590,935-976,2386`; `utils/pty/src/lib.rs:14` | `truncate_mcp_tool_result_for_event` |
| A tool-lifecycle extension mutates the `CallToolResult` (content, structured content, `_meta`) after `tools/call` and before modality sanitization, the completion event and model output | mechanism §4 | `core/src/tools/lifecycle.rs:52-74`; `core/src/mcp_tool_call.rs:538-550`; `ext/extension-api/src/contributors/tool_lifecycle.rs:144-163` | `on_mcp_tool_result` |
| Hook output spills at 2,500 tokens | mechanism §4 | `hooks/src/output_spill.rs:12,127` | `Full hook output saved to` |

## Injected context

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| `project_doc_max_bytes` default is 32 KiB, total | SKILL outside playbook, mechanism §1 | `config/src/config_toml.rs:73` | `DEFAULT_PROJECT_DOC_MAX_BYTES` |
| AGENTS.md cut mid-file; only trace is a server-side log | SKILL outside playbook, mechanism §1 | `core/src/agents_md.rs:44,166-179` | `LOCAL_AGENTS_MD_FILENAME` |
| Skills catalog gets 2% of the context window | mechanism §1, §6 | `ext/skills/src/render.rs:20,127-153` | `SKILL_METADATA_CONTEXT_WINDOW_PERCENT` |
| Descriptions are shortened under that budget | mechanism §1, §6 | `ext/skills/src/render.rs:25` | `Skill descriptions were shortened` |
| Catalog budget = configured `max_context_tokens` capped at 10,000 tokens, else 2% of window, else 8,000 characters | mechanism §6 | `ext/skills/src/render.rs:17-19,127-153` | `MAX_CONFIGURED_SKILL_METADATA_TOKEN_BUDGET` |
| Ambiguous `$mention` is silently skipped | mechanism §1 | `skills/src/selection.rs:188` | `skill_count != 1` |
| Untrusted host values truncate at ~1,000 tokens | mechanism §1 | `context-fragments/src/additional_context.rs:6` | `MAX_ADDITIONAL_CONTEXT_VALUE_TOKENS` |
| Internal-context marker | mechanism §1 | `core/src/context/internal_model_context.rs:8` | `CONTEXT_START_MARKER` |
| The `never` policy announcement forbids `sandbox_permissions` | mechanism §1 | `prompts/templates/permissions/approval_policy/never.md:1` | `commands will be rejected` |
| The `on-request` announcement forbids destructive prefix rules | mechanism §1, §2 | `prompts/templates/permissions/approval_policy/on_request.md:50` | `NEVER provide a prefix_rule argument` |
| **Managed "force exact value" is an open set** | SKILL Layer 0, mechanism Layer 0 | `core/src/config/requirements.rs:29-49` | `allow_login_shell` |
| **Model catalog `tool_mode` is read first, with no local override** | SKILL Layer 0, mechanism Layer 0 | `core/src/tools/mod.rs:67-77,79` | `effective_tool_mode` |
| …and the shipped catalog uses it for seven slugs | SKILL Layer 0, mechanism Layer 0 | `models-manager/models.json:20,189,320,446,572,690,1255` | `"tool_mode": "code_mode_only"` |
| …including the highest-priority visible preset, so an unconfigured session is in code mode | mechanism Layer 0 | `models-manager/models.json:173,189` | `"slug": "gpt-5.6-sol"` |
| Allowlisted bundled cleanup hooks are trusted with no recorded hash and stay enabled through per-hook disablement | SKILL Layer 0, mechanism Layer 0 | `plugin/src/bundled_hooks.rs:26-108,113,130`; `hooks/src/engine/discovery.rs:667-680,794-815` | `ALLOWLISTED_BUNDLED_HOOKS`; `fn hook_trust_status` |
| …and the match requires an `mcp_tool` handler with no matcher, with the Apps target additionally requiring an empty input and the registered connector — which local discovery cannot supply, so that entry resolves only on the executor path | mechanism Layer 0 | `plugin/src/bundled_hooks.rs:100-107,113-152`; `hooks/src/engine/discovery.rs:673`; `core-plugins/src/executor_hooks.rs:87-95` | `connector_openai_browser`; `/*app_connector_id*/ None`; `lookup_enabled_tool` |
| …and they survive the hooks feature being turned off entirely, with warnings and load errors cleared | SKILL Layer 0, mechanism Layer 0 | `hooks/src/engine/mod.rs:238,255-259` | `Disabled ordinary hooks must not emit warnings` |
| The built-in classification never reaches a client: not on the wire, filtered from the app-server hook catalog, no lifecycle notification | SKILL Layer 0, mechanism Layer 0 | `protocol/src/protocol.rs:1670-1674,4473-4479`; `app-server/src/request_processors/catalog_processor.rs:66-69`; `core/src/hook_runtime.rs:847-849` | `Internal classification used to suppress lifecycle notifications`; `fn should_emit_hook_notification` |
| Plugin enablement and `allow_managed_hooks_only` still bound the exception | mechanism Layer 0 | `plugin/src/load_outcome.rs:39-41,171-177`; `hooks/src/engine/discovery.rs:82-92,106-114` | `fn effective_plugin_hook_sources`; `allow_managed_hooks_only` |
| Executor plugin hooks are filtered down to the same allowlist and nothing else, and land in the engine already stamped builtin | SKILL Layer 0, mechanism Layer 0 | `core-plugins/src/executor_hooks.rs:80-108`; `hooks/src/engine/mod.rs:305-324` | `Remove this temporary filter once executor plugin hooks can be trusted`; `allowlisted executor handler must be an MCP tool` |
| Goal steering pushes back on scope shrinking | mechanism §1 | `prompts/templates/goals/continuation.md:27` | `Do not substitute a narrower` |
| Periodic timestamp reminder | mechanism §1 | `core/src/context/current_time_reminder.rs:28-30,35-37` | `<current_time_reminder>` |
| A saved prefix rule or network rule is echoed back as a context fragment | mechanism §1, §2 | `core/src/context/approved_command_prefix_saved.rs:22`; `core/src/context/mod.rs:3,25,43-44,75` | `approved_command_prefix_saved`; `NetworkRuleSaved` |
| Untrusted projects load no project AGENTS.md at all | SKILL inside playbook, mechanism §1 | `core/src/agents_md.rs:64-66` | `active_project.is_untrusted()` |
| Sandboxed AGENTS.md read failure returns an error rather than degrading, and the callers are now traced: fatal at session init through `thread/start`, and a pre-sampling `EventMsg::Error` on a later refresh. The model never sees it | SKILL outside playbook, mechanism §1 | `core/src/agents_md.rs:103-124`; `core/src/session/session.rs:1221-1231`; `core/src/session/mod.rs:735-774,3191-3200`; `app-server/src/request_processors/thread_processor.rs:1417-1427` | `failed to load AGENTS.md instructions` |
| `project_doc_max_bytes` is one pool across all environments | SKILL outside playbook, mechanism §1 | `core/src/agents_md.rs:68` | `let mut remaining = config.project_doc_max_bytes` |
| Skills budget warnings go to the client, not into context | SKILL default surface, mechanism §6 | `ext/skills/src/render.rs:93`; `ext/skills/src/extension.rs:617-623` | `fn warning_message` |
| Skill locators are aliased to `r0`/`r1` unannounced — and aliasing is selected by a three-key comparison (included, then truncated description chars, then cost), so it is *not* usable as a budget-pressure signal | SKILL default surface, mechanism §6 | `ext/skills/src/render.rs:518-529,1014,1111-1126` | `aliased_render_is_better` |
| …and the combined path compares five candidates, so one source catalog can be aliased while another is not | mechanism §6 | `ext/skills/src/render.rs:605-652,839-869` | `combined_render_is_better` |
| Combined catalogs share one budget | mechanism §6 | `ext/skills/src/render.rs:540` | `render_combined_available_skills` |
| User-shell (`!command`, `thread/shellCommand`) output re-enters model context unsandboxed, with no exec-policy check and no approval, through a login shell with any managed proxy stripped, and lands mid-turn when a turn is already active | SKILL inside playbook, mechanism §1 | `core/src/tasks/user_shell.rs:147,165-167,204-219,447-471`; `core/src/context/user_shell_command.rs:35-52` | `<user_shell_command>`; `strip_managed_proxy_env`; `inject_no_new_turn` |
| IDE context is injected unmarked into the user's own text item, split by `## My request for Codex:` | SKILL inside playbook, mechanism §1 | `tui/src/ide_context/prompt.rs:9-16,26,47,65-74,181-183`; `tui/src/chatwidget/ide_context.rs:7-11` | `Context from my IDE setup` |
| `<git_attribution>` is a developer fragment driven by the backend `commit_attribution_enabled` setting; disabled emits nothing from an absent prior | mechanism §1 | `ext/git-attribution/src/world_state.rs:17-25,44-53`; `ext/git-attribution/src/policy.rs:47,50,79,90`; `ext/git-attribution/src/lib.rs:40-63` | `Ignore any earlier instructions disabling Codex attribution` |
| `<shell_version>` is feature-gated: `powershell_shell_version` is under development and default off, and the element appears only for a single local environment whose shell is PowerShell, probed under a 2-second timeout and yielding a version only when the shell exits cleanly with at most 64 bytes of stdout, with the result memoized per shell path — misses included | mechanism §1 | `features/src/lib.rs:953-958`; `core/src/context/world_state/environment.rs:24-25,45-56,394-431` | `powershell_shell_version`; `POWERSHELL_VERSIONS` |
| …and a version that disappears renders as an explicit unavailable element rather than being dropped | mechanism §1 | `core/src/context/world_state/environment.rs:181-182,284-285` | `shell_version_removed` |
| Any project not explicitly *trusted* loads no project-local config, hooks or exec policies | SKILL inside playbook | `config/src/loader/mod.rs:1063-1080`; `config/src/config_toml.rs:544-552` | `project-local config, hooks, and exec policies` |
| A disabled config layer contributes nothing to the merged config | SKILL inside playbook | `config/src/state.rs:481-484` | `fn layers_low_to_high` |
| Project skill roots survive a disabled project layer | SKILL inside playbook | `ext/skills/src/host_roots.rs:80-92` | `SkillScope::Repo` |
| Four auth requirement fields are stripped from backend-delivered layers, and a disallowed stored credential is filtered to `None` rather than reported | mechanism Layer 0 | `config/src/requirements_layers/layer.rs:11-17,91-96`; `login/src/auth/manager.rs:1141-1152,1181` | `LOCAL_ONLY_AUTH_REQUIREMENTS` |
| Web search mode defaults to `Cached`, which grants the same external access as `Disabled` (`external_web_access: false`) while still registering the tool, where `Disabled` returns no tool spec at all | SKILL outside playbook | `core/src/config/mod.rs:2595-2606,3650`; `core/src/tools/hosted_spec.rs:16`; `ext/web-search/src/extension.rs:86-92` | `external_web_access_for_mode` |
| …and an unsupported preference walks a fallback list rather than erroring | SKILL outside playbook | `core/src/config/mod.rs:2976-3020` | `resolve_web_search_mode_for_turn` |
| `web.run` auto-ships a two-user-message tail with a 1,000-token assistant budget | mechanism §2 | `ext/web-search/src/history.rs:10,18-26`; `core/src/tools/spec_plan.rs:936-945` | `ASSISTANT_CONTEXT_TOKEN_LIMIT` |

## Results on the way back

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| Exec results are framed with exit code and wall time | mechanism §4 | `core/src/tools/mod.rs:109` | `Wall time:` |
| A spawn failure reaches the model as `exec_command failed: {err:?}` with no command echoed | SKILL triage, mechanism §4 | `core/src/tools/handlers/unified_exec/exec_command.rs:460` | `exec_command failed: ` |
| …and the whole message is middle-truncated at 900 bytes — a byte cap, not a character cap | mechanism §4 | `core/src/tools/handlers/unified_exec/exec_command.rs:57,460-464`; `utils/string/src/truncate.rs:7,52` | `EXEC_COMMAND_REJECTION_MAX_BYTES`; `truncate_middle_chars` |
| Timeout prefix | mechanism §4 | `core/src/tools/mod.rs:134` | `command timed out after` |
| PostToolUse can swap visible text while retaining the original | mechanism §4 | `core/src/tools/registry.rs:212-217,730` | `PostToolUseFeedbackOutput` |
| Unknown tool name | SKILL triage, mechanism §2 | `core/src/tools/registry.rs:818` | `unsupported call:` |
| PreToolUse veto wording forks on whether the call carries a command string | SKILL triage, mechanism §2 | `core/src/hook_runtime.rs:223,227` | `blocked by PreToolUse hook` |
| Hooks that exit before reading stdin keep their output | mechanism §2 | `hooks/src/engine/command_runner.rs:269` | `ErrorKind::BrokenPipe` |
| Unsupported modalities replaced with placeholder text | mechanism §4 | `core/src/context_manager/normalize.rs:15,17` | `content omitted because you do not support` |
| A rejected image ends the turn, no silent retry | mechanism §4 | `core/src/session/turn.rs:567` | `Invalid image in your last message` |
| Orphan tool outputs dropped before each request | mechanism §4 | `core/src/context_manager/normalize.rs:155` | `remove_orphan_outputs` |
| The two direct app-server MCP requests return a server-originated protocol error verbatim — its numeric `code`, `message` and `data` — while every other failure flattens to `-32603` with no `data`, and the mapper is the app-server's only structural read of a server error | mechanism §4 | `app-server/src/request_processors/mcp_processor.rs:514,546,553-562`; `app-server/src/error_code.rs:6,22-31` | `fn mcp_operation_error` |
| The structured arm fires only on `ServiceError::McpError`, and the `.with_context()` the connection manager adds preserves the downcast that finds it | mechanism §4 | `rmcp-client/src/service_error.rs:10-15`; `rmcp-client/src/rmcp_client.rs:278-283,1350`; `codex-mcp/src/connection_manager.rs:961`; `codex-mcp/src/connection_manager/resources.rs:156`; `rmcp-client/tests/resources.rs:123-144` | `ServiceError::McpError`; `enum ClientOperationError` |
| The two requests are the wire methods `mcpServer/tool/call` and `mcpServer/resource/read` | mechanism §4 | `app-server-protocol/src/protocol/common.rs:1174,1194` | `mcpServer/tool/call` |
| On `mcpServer/resource/read` two codex-side steps also yield `-32603` — the config load before the spawn and the response deserialization after the mapper — while `mcpServer/tool/call`'s pre-mapper failures are `-32600` | mechanism §4 | `app-server/src/request_processors/mcp_processor.rs:100-103,110-117,473,513-521,536-537`; `app-server/src/request_processors/thread_input.rs:23-37` | `failed to deserialize MCP resource read response: ` |
| An unknown server and a tool excluded by codex's own `[mcp_servers.<name>]` `enabled_tools`/`disabled_tools` filter are plain `anyhow` errors, so they take the `-32603` arm | mechanism §4 | `codex-mcp/src/connection_manager.rs:315,916,925-929`; `codex-mcp/src/connection_manager/resources.rs:166`; `codex-mcp/src/tools.rs:73-85`; `config/src/mcp_types.rs:247,251` | `is disabled for MCP server`; `unknown MCP server` |

## Tool registry and session plumbing

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| Reserved names and duplicate external tools are dropped with only a local log | mechanism §2 | `core/src/tools/registry.rs:350` | `skipping external tool with reserved name` |
| Collisions are fatal only under an opt-in flag | mechanism §2 | `core/src/tools/spec_plan.rs:379` | `error_on_tool_collisions` |
| `clock.curr_time` and `clock.sleep` are registered by two independent gates, and `model_driven` sleep selection branches on the reminder feature instead of combining with it | SKILL off-by-default table, mechanism §2 | `core/src/tools/spec_plan.rs:1206-1231` | `model_has_clock`; `SleepToolMode::AlwaysOn` |
| `sleep_tool` is Stable/default-on with a `model_driven`/`always_on` mode, while `current_time_reminder` is under development, default off, and its `sleep_tool` sub-flag defaults to false | SKILL off-by-default table, mechanism §2 | `features/src/lib.rs:917-922,1597-1602`; `features/src/feature_configs.rs:402-404,413-422,426-431`; `core/src/config/mod.rs:1253-1265,2900-2902,3703-3711` | `SleepToolMode`; `sleep_tool_mode` |
| Only the hidden `gpt-6-astra` catalog entry advertises `clock`, and hidden entries are skipped when the default preset is chosen, so a default-configured session has neither clock tool | SKILL off-by-default table, mechanism §2 | `models-manager/models.json:4,67,72,128-131`; `models-manager/src/manager.rs:126-138` | `"slug": "gpt-6-astra"`; `mark_default_by_picker_visibility` |
| Shell commands receive `CODEX_SESSION_ID` and `CODEX_VERSION` | mechanism §2 | `core/src/exec_env.rs:13,16,40-50` | `CODEX_SESSION_ID_ENV_VAR`; `CODEX_VERSION_ENV_VAR` |
| `CODEX_VERSION` is re-asserted four ways — injected after the policy env is built, dropped from the exec-server policy's `set` and force-added to its `exclude`, always re-sent in the exec-server overlay, and re-exported after a shell snapshot is sourced | mechanism §2 | `core/src/unified_exec/process_manager.rs:132-158,171-186,1364-1369`; `core/src/tools/runtimes/mod.rs:226,264-276,300-301`; `core/src/tasks/user_shell.rs:168-169` | `inject_session_env`; `exec_env_policy_from_shell_policy`; `maybe_wrap_shell_lc_with_snapshot` |
| The same version rides outbound MCP request metadata as `codex_version`, inserted after client-supplied metadata is merged, so a client-supplied value is silently overwritten | mechanism §2 | `core/src/turn_metadata.rs:48,231-253,361-369`; `core/src/responses_metadata.rs:60-93,486-492`; `core/src/mcp_tool_call.rs:1250-1263`; `core/src/hook_runtime.rs:361`; `core/src/tools/handlers/extension_tools.rs:172` | `CODEX_VERSION_KEY`; `current_meta_value_for_mcp_request`; `filter_extra_metadata` |
| A retryable `ConnectionFailed` on a **sampling** request retries without a count limit — non-internal, non-Bedrock only; the feature is Stable/default-on | SKILL outside playbook, mechanism §5 | `features/src/lib.rs:1100-1105`; `core/src/responses_retry.rs:58-82` | `unbounded_connection_retries`; `is_amazon_bedrock` |
| …with backoff doubling from 5 s to a 60 s cap, and a server-side warning alongside the frontend notice | SKILL outside playbook, mechanism §5 | `core/src/responses_retry.rs:17-18,67-80` | `MAX_CONNECTION_RETRY_DELAY` |
| …emitting only a reconnect notice | mechanism §5 | `core/src/responses_retry.rs:74` | `waiting for network` |
| Remote compaction V2 can retain client-authored developer messages across the boundary | mechanism §5 | `core/src/compact_remote_v2.rs:459,475-477`; `features/src/lib.rs:1548-1553` | `retain_client_developer_messages`; `is_client_authored_developer_message` |
| MCP tool names are sanitized, hash-suffixed on collision and truncated at 128 bytes before registration, so MCP-vs-MCP names never reach the duplicate drop | mechanism §2 | `codex-mcp/src/tools.rs:58,113,153-197,226,241-244,288-311`; `core/src/config/mod.rs:1759-1762`; `features/src/lib.rs:1194-1198` | `MAX_TOOL_NAME_LENGTH` |
| A misalignment-policy verdict is typed non-retryable and the reference client kills the thread | mechanism §5 | `codex-api/src/sse/responses.rs:423-431`; `protocol/src/error.rs:135-136,388-389`; `tui/src/chatwidget/misalignment_policy.rs:5-43` | `MisalignmentPolicyViolation` |
| `invalid_grant` on a 400 is a permanent, cached refresh failure; other 400s stay transient | mechanism §5 | `login/src/auth/manager.rs:1586-1602,2297-2305`; `core/src/client.rs:2320-2341` | `is_invalid_grant_bad_request` |
| Queued messages committed by another process start turns on already-loaded, idle threads | mechanism §5 | `ext/queue/src/service.rs:91-95,136,148,190-240`; `state/src/runtime/queued_items.rs:34-47`; `app-server/src/extensions.rs:76-78` | `watch_external_messages` |

## Cross-cutting loops

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| Compaction presents the agent's own past as a stranger's | mechanism §5 | `prompts/templates/compact/summary_prefix.md:1` | `Another language model started to solve this problem` |
| `<turn_aborted>` marker | SKILL inside playbook, mechanism §5 | `core/src/context/turn_aborted.rs:34` | `turn_aborted` |
| Aborted calls become synthetic outputs | mechanism §5 | `core/src/tools/parallel.rs:258` | `aborted by user after` |
| One-time WebSocket→HTTPS fallback notice | mechanism §5 | `core/src/responses_retry.rs:94` | `Falling back from WebSockets` |
| Stop hooks fail open on timeout | mechanism §5 | `hooks/src/events/stop.rs` | `should_block` |
| MCP hook timeout defaults to 600 s; only `SessionEnd` and `Interrupt` clamp to a 1 s default / 3 s cap, and a SessionEnd MCP hook never reaches that clamp | mechanism §5 | `hooks/src/events/session_end.rs:20,23`; `hooks/src/engine/discovery.rs:742-764` | `SESSION_END_MAX_TIMEOUT_SEC`; `fn normalize_command_hook` |
| `HookHandlerConfig` accepts an `mcp_tool` handler type with `server`, `tool`, `input`, `timeout` and `statusMessage`; `prompt` and `agent` parse but are rejected at load | SKILL outside playbook, mechanism §5 | `config/src/hook_config.rs:186-200`; `hooks/src/engine/discovery.rs:635-654` | `rename = "mcp_tool"`; `prompt hooks are not supported yet` |
| An MCP hook's `input` map must be TOML-representable, because the normalized handler is what the trust hash covers | mechanism §5 | `config/src/hook_config.rs:203-217`; `hooks/src/engine/discovery.rs:769-791` | `MCP hook input must be representable as TOML`; `struct NormalizedHookIdentity` |
| `${a.b}` templates expand recursively over dotted object keys; a whole placeholder preserves JSON type, an embedded one stringifies, an unresolvable path fails the hook | mechanism §5 | `hooks/src/engine/mcp_runner.rs:98-162` | `fn expand_mcp_argument_template`; `hook input placeholder` |
| `SessionEnd` MCP hooks and empty `server`/`tool` are dropped at load, before normalization | mechanism §5 | `hooks/src/engine/discovery.rs:582-610` | `MCP hooks are not supported`; `server and tool must not be empty` |
| Local-source MCP hooks run sync against a live executor and can apply control effects; executor-scoped ones are forced async and cannot | mechanism §5 | `hooks/src/engine/mod.rs:141-156,225`; `hooks/src/engine/dispatcher.rs:215`; `core/src/session/session.rs:1339` | `ConfiguredHandlerKind::McpTool { .. } => HookExecutionMode::Sync`; `CoreHookMcpExecutor {` |
| An MCP hook reports exit 0 on success, no exit code on failure and never writes stderr, so exit-code-2 blocking is unreachable and a failure fails open | mechanism §5 | `hooks/src/engine/mcp_runner.rs:77-89`; `hooks/src/events/pre_tool_use.rs:204-212,260-262,283-288` | `stderr: String::new()`; `Some(2) if handler.can_apply_control_effects()` |
| A hook MCP call produces no MCP tool call in the transcript — only a hook run summary carrying the handler type and source path, not the server/tool pair | SKILL outside playbook, mechanism §5 | `hooks/src/engine/mcp_runner.rs:27-89`; `hooks/src/engine/dispatcher.rs:78-98`; `hooks/src/engine/mod.rs:184-189` | `fn running_summary`; `ConfiguredHandlerKind::McpTool { .. } => HookHandlerType::McpTool` |
| Hook MCP calls go straight to the server connection — no approval, Guardian or app approval-mode gate; the surviving gates are unknown server, the server's config-level tool filter, readiness, and a timeout min'd against the server's own | SKILL outside playbook, mechanism §5 | `core/src/hook_mcp_executor.rs:27-38`; `codex-mcp/src/runtime.rs:536-556`; `codex-mcp/src/connection_manager.rs:902-963`; `codex-mcp/src/tools.rs:73-95`; `core/src/mcp_tool_call.rs:235,256` | `wait_for_server`; `is disabled for MCP server`; `is not connected`; `MCP tool call blocked by app configuration` |
| An MCP result marked `isError` becomes a hook failure | mechanism §5 | `core/src/hook_mcp_executor.rs:49-51` | `MCP tool returned an error` |
| `Interrupt` hooks fire on turn abort from core, skip subagent sessions, have no veto, parse only a system message, and merge in the last known step's executor hook sources so a turn with no local `Interrupt` hook still runs the executor's | mechanism §5 | `core/src/hook_runtime.rs:318,486-525`; `hooks/src/registry.rs:101`; `core/src/tasks/mod.rs:809,967`; `hooks/src/events/interrupt.rs:34-37`; `hooks/src/engine/output_parser.rs:70-72,259-264` | `fn run_turn_interrupt_hooks`; `executor_hook_sources_for_step`; `struct InterruptOutcome`; `fn parse_interrupt` |
| SessionEnd is root-session only | mechanism §5 | `core/src/hook_runtime.rs:409-411` | `SessionSource::SubAgent` |
| SessionEnd `async: true` is a no-op | mechanism §5 | `hooks/src/engine/discovery.rs:532` | `running async SessionEnd hook synchronously` |
| Review mode forces `never` and disables web search | mechanism §5 | `core/src/tasks/review.rs:109,119` | `WebSearchMode::Disabled` |
| Headless `codex exec` defaults to `never`; AutoReview lifts it | SKILL outside playbook, mechanism §3 | `exec/src/lib.rs:513,686-719` | `ApprovalsReviewer::AutoReview` |
| `codex exec --worktree` starts the session in a fresh managed worktree; it is exec-only, and resume, review, every other subcommand and the bare TUI are rejected | SKILL outside playbook, mechanism §3 | `exec/src/lib.rs:294-307`; `cli/src/main.rs:1063,2399-2432` | `currently supports only`; `cannot resume an existing session` |
| `--worktree` also refuses `--ignore-user-config`, `--ephemeral`, and a remote default environment | mechanism §3 | `exec/src/lib.rs:294-300,369-374` | `--worktree requires local execution` |
| The `worktrees` feature is Experimental and default-off, and its gate config sets `ignore_project_config`, so project config cannot enable it — only user config or a CLI override | SKILL outside playbook, mechanism §3 | `features/src/lib.rs:1227-1236`; `exec/src/lib.rs:377-408`; `cli/src/main.rs:1066` | `key: "worktrees"`; `--worktree requires the worktrees feature` |
| CLI allocation is host-scoped, shares Desktop's `git-worktree-root` (default `<codex_home>/worktrees`) and forces cleanup off; the crate is `codex-exec`'s alone, exposes no public removal, and nothing in codex-rs reads `auto_cleanup_enabled` or `keep_count` | SKILL outside playbook, mechanism §3 | `worktree/src/settings.rs:12-14,28-36,44`; `exec/src/lib.rs:414-436`; `worktree/src/lib.rs:52-61,167,273,278,301`; `exec/Cargo.toml:46` | `fn for_cli`; `auto_cleanup_enabled: false`; `codex-worktree` |
| The checkout is a detached, branchless copy of the source repository's committed `HEAD`, allocated as `<root>/<4-hex bucket>/<repo>` | SKILL outside playbook, mechanism §3 | `worktree/src/lib.rs:61-165`; `worktree/src/paths.rs:13-40` | `"--detach"`; `fn allocate_worktree_root` |
| Relative `--add-dir` is rebased before the cwd swap, session bootstrap config loads after it, and the thread id is bound into the checkout | mechanism §3 | `exec/src/lib.rs:409-413,431-432,440-442,1028-1031` | `fn bind_thread` |
| Inside the checkout the project-config layer and the AGENTS.md chain anchor on the new cwd, but trust resolves back to the linked worktree's main checkout, so the source repository's trust decision still applies | mechanism §3 | `config/src/loader/mod.rs:1331,1340`; `core/src/agents_md.rs:210-218`; `git-utils/src/trust.rs:13,132-135`; `core/src/worktree_trust_tests.rs:15-24`; `protocol/src/permissions.rs:1437,1482-1522` | `fn get_writable_roots_with_cwd`; `fn resolve_root_git_project_for_trust`; `find_project_root`; `find_nearest_ancestor_with_markers` |

## Subsystems

| Claim | Where it ships | Source | Match on |
|---|---|---|---|
| **Multi-agent V1 is stable and default-enabled**, and V2 (`multi_agent_v2`) is Stable but default **off** | SKILL default surface, mechanism §6 | `features/src/lib.rs:1122-1133` | `key: "multi_agent"`; `key: "multi_agent_v2"` |
| The resolved version comes from the V2 feature, else the model catalog, else the `multi_agent` feature | SKILL default surface, mechanism §6 | `core/src/config/mod.rs:1507-1535` | `fn multi_agent_version_for_model` |
| V2 replaces `send_input`/`resume_agent`/`close_agent` with `send_message`, `followup_task`, `interrupt_agent`, `list_agents`; `wait_agent` is conditional | SKILL default surface, mechanism §6 | `core/src/tools/spec_plan.rs:1118-1201` | `SendMessageHandlerV2`; `wait_agent_enabled` |
| V1's tools register `Deferred` on a search-capable model | mechanism §6 | `core/src/tools/spec_plan.rs:578-580,1178-1182` | `fn search_tool_enabled` |
| Spawn depth defaults to 1 | SKILL default surface, mechanism §6 | `core/src/config/mod.rs:235,3761-3765` | `agent_max_depth` |
| Under V2 the catalog's `multi_agent.mode` replaces the built-in effort-derived delegation posture; config `multi_agent_mode_hint_text` outranks it, and the resolver returns nothing for V1 | SKILL Layer 0, mechanism §6 | `core/src/session/multi_agents.rs:155-187`; `protocol/src/openai_models.rs:639-658` | `fn effective_multi_agent_mode` |
| …an empty mode string deletes the section rather than restoring the built-in text, and custom text is truncated to 400 tokens | SKILL Layer 0, mechanism §6 | `core/src/context/multi_agent_mode_instructions.rs:16-25`; `core/src/context/world_state/multi_agent_mode.rs:13,24-35` | `MULTI_AGENT_MODE_MAX_TOKENS` |
| …and the resolver yields no mode for internal sessions or any subagent source but `ThreadSpawn` | mechanism §6 | `core/src/session/multi_agents.rs:189-198` | `SessionSource::Internal(_) \| SessionSource::SubAgent(_) => None` |
| …but a `None` mode is not silence: the world-state section still emits a bare `ExplicitRequestOnly` when the previous snapshot was `Proactive` or is unreadable | mechanism §6 | `core/src/context/world_state/multi_agent_mode.rs:63-86`; `core/src/context/world_state/mod.rs:117-135` | `PreviousSectionState::Unknown) => MultiAgentMode::ExplicitRequestOnly` |
| Built-in roles are currently inert | mechanism §6 | `core/src/agent/role.rs:351-377` | `built_in` |
| A role's override set is closed, and its `features` field honors only a *disable* of six capability features, keyed by `shell_tool`, `apps`, `personality`, `plugins`, `memories`, `request_permissions_tool`, plus explicitly selected skills | SKILL default surface, mechanism §6 | `core/src/agent/role.rs:37-48,91-110`; `features/src/lib.rs:837,993,1053,1147,1219,1465`; `features/src/legacy.rs:25-26` | `struct AgentRoleOverrides`; `Feature::RequestPermissionsTool`; `fn feature_for_key` |
| Authority is not inherited but *reapplied*: spawning copies the live parent turn's approval policy, approvals reviewer, cwd and permission-profile snapshot | SKILL default surface, mechanism §6 | `core/src/tools/handlers/multi_agents_common.rs:229-259` | `fn apply_spawn_agent_runtime_overrides` |
| …while model, provider, reasoning settings and developer instructions are set from the spawn path and may differ | SKILL default surface, mechanism §6 | `core/src/tools/handlers/multi_agents_common.rs:194-215` | `fn build_agent_shared_config` |
| …and a regression test asserts it | mechanism §6 | `core/src/agent/role_tests.rs:394` | `apply_role_cannot_expand_parent_authority` |
| Code-mode feature flags are under development, default off | SKILL off-by-default table | `features/src/lib.rs:908-937` | `CodeModeOnly` |
| Code mode exposes `exec` and `wait` | SKILL Layer 0, mechanism §6 | `code-mode-protocol/src/lib.rs:50-51` | `PUBLIC_TOOL_NAME` |
| `console` is deleted from the JS global object | mechanism §6 | `code-mode-runtime/src/runtime/globals.rs:17` | `delete_global(scope, global, "console")` |
| Nested tool calls go through the same router | SKILL Layer 0, mechanism §6 | `core/src/tools/code_mode/mod.rs:345-396`; `core/src/tools/parallel.rs:95-100` | `handle_tool_call_with_source` |
| A failed nested call becomes a rejected promise | SKILL Layer 0, mechanism §6 | `code-mode-runtime/src/runtime/mod.rs:236,244`, definition `code-mode-runtime/src/runtime/module_loader.rs:66` | `resolve_tool_response` |
| `CodeModeOnly` hides other tools from the menu | mechanism §6 | `core/src/tools/spec_plan.rs:231-243,464-479` | `DirectModelOnly` |
| Skills are reachable as `list` / `read` tools | SKILL default surface, mechanism §6 | `ext/skills/src/tools/list.rs:31`; `ext/skills/src/tools/read.rs:34` | `TOOL_NAME` |
| Both skills tools page against the calling turn's response budget: `min(512 KiB, 1.2 × the model's truncation policy)` direct, flat 512 KiB in code mode | SKILL default surface, mechanism §6 | `tools/src/tool_call.rs:148-157`; `ext/skills/src/tools/mod.rs:56`; `core/src/tools/handlers/extension_tools.rs:209-210`; `core/src/tools/lifecycle.rs:135-138` | `fn response_byte_budget` |
| …which is 48,000 bytes per direct page on ten of the eleven shipped slugs (`tokens`/10,000 at four bytes per token) and 12,000 on `gpt-5.2`, which declares `bytes`/10,000 | SKILL default surface, mechanism §6 | `models-manager/models.json:15-18,1142-1145`; `utils/string/src/truncate.rs:4,76-78`; `protocol/src/protocol.rs:3288-3295,3308-3331` | `fn approx_bytes_for_tokens`; `fn byte_budget` |
| `list` pages at 20, drops entries whose own response or whose handles will not fit, and reports it once per budget value in `warnings[]` | mechanism §6 | `ext/skills/src/tools/list.rs:32,33-34,95,132,139-146,202-213` | `OVERSIZED_ENTRY_WARNING`; `MAX_SKILLS_PER_PAGE` |
| …then sheds the page, then provider warnings, then every skill (warnings-only page, same-offset cursor), before failing outright | mechanism §6 | `ext/skills/src/tools/list.rs:165-196` | `leaves no room for discovery warnings`; `skill metadata is too large to list` |
| `read` binary-searches the largest page that fits on JSON and UTF-8 boundaries | mechanism §6 | `ext/skills/src/tools/read.rs:294-330` | `skills.read response budget leaves no room for contents` |
| Both cursors are fingerprinted over the paged value, so changed content fails rather than splicing | SKILL default surface, mechanism §6 | `ext/skills/src/tools/mod.rs:342-362` | `cursor is stale; restart from the first page` |
| An executor `read` continuation may serve a pre-edit snapshot: one slot per thread, written only for executor authority with pages remaining and contents ≤ 1 MiB, never expired or cleared | SKILL default surface, mechanism §6 | `ext/skills/src/tools/read.rs:155-180,257-275`; `ext/skills/src/state.rs:46,314`; `ext/skills/src/provider.rs:30` | `ExecutorReadSnapshot` |
| …and this reshape is one the model is told about, in the tool's own description | mechanism §6 | `ext/skills/src/tools/read.rs:66` | `continue the same snapshot while it is cached` |
| **No general secret redaction in the exec output path** | SKILL default surface, mechanism §4 | `secrets/src/sanitizer.rs:1-22` | `redact_secrets` |
| Plugins can declare MCP servers and hooks | mechanism §6 | `plugin/src/manifest.rs:19-42` | `PluginManifestHooks` |
| An Agent Plugins `mcp.json` stdio server has no `env_vars` field under `deny_unknown_fields`, and its `env` values expand only `${PLUGIN_ROOT}` and `${PLUGIN_DATA}` | mechanism §6 | `codex-mcp/src/agent_plugin_config.rs:46-57,213-215,374-386` | `enum AgentPluginMcpServer`; `fn expand_agent_plugin_placeholders` |
| …so a colocated `.codex-plugin/plugin.json` overlays `env_vars` onto matching servers, gated on the Agent Plugins manifest format alone, only for names already declared and only stdio-to-stdio | mechanism §6 | `core-plugins/src/loader.rs:1579-1581`; `core-plugins/src/agent_plugin_mcp_overlay.rs:13-24,58-71` | `fn apply_codex_env_overlay` |
| …with `source: "remote"` `env_vars` entries discarded, the rest appended, and a matching literal `${NAME}` `env` entry removed — case-insensitively on Windows, exactly elsewhere | mechanism §6 | `core-plugins/src/agent_plugin_mcp_overlay.rs:73-90,93-99`; `config/src/mcp_types.rs:121-123` | `fn environment_names_match`; `fn is_remote_source` |
| …which is load-bearing because the literal `env` map is applied after the `env_vars` process-environment lookup and overwrites it | mechanism §6 | `rmcp-client/src/utils.rs:16-26,39-53`; `rmcp-client/src/stdio_server_launcher.rs:260-269` | `fn create_env_for_mcp_server` |
| The overlay's servers come from its own `mcpServers` object or the file it names, defaulting to `<plugin root>/.mcp.json`, and load as legacy config — skipping the Agent Plugins regular-file and inside-the-root checks | mechanism §6 | `core-plugins/src/agent_plugin_mcp_overlay.rs:36-56`; `core-plugins/src/manifest.rs:221-226,272-275`; `core-plugins/src/loader.rs:70,1110-1130,1592-1642` | `PluginManifestFormat::Legacy`; `DEFAULT_MCP_CONFIG_FILE` |
| A root `plugin.json` with an agent-plugins.org `$schema` wins over `.codex-plugin/plugin.json`, which then supplies the resolved manifest's `apps` and `hooks` paths and replaces its `interface` — unless a root `com.openai` extension object displaces it | mechanism §6 | `utils/plugins/src/plugin_namespace.rs:11,17,42-56`; `core-plugins/src/manifest.rs:165-176`; `core-plugins/src/agent_plugin_manifest.rs:16,181-195,204-217` | `fn find_plugin_manifest_path`; `fn apply_codex_agent_plugin_extension` |
| …but an Agent Plugins-format plugin loads neither apps nor hooks at runtime, so those two paths are inert while the `interface` override still reaches the listing | mechanism §6 | `core-plugins/src/loader.rs:948-950,954-964,1133-1138`; `core-plugins/src/manager.rs:2659-2662,2680-2698`; `core-plugins/src/loader_tests.rs:31` | `agent_plugin_overlay_apps_are_not_runtime_active`; `fn plugin_interface_with_marketplace_category` |
| Plan mode hard-blocks `update_plan` only | mechanism §6 | `core/src/tools/handlers/plan.rs:87-90` | `not allowed in Plan mode` |
| `update_plan` is opt-in: an absent or unset `[tools.update_plan]` resolves to off | SKILL off-by-default table, mechanism §6 | `core/src/config/mod.rs:2651-2657`; `config/src/config_toml.rs:624,634-639` | `fn resolve_update_plan_enabled`; `UpdatePlanToolConfig` |
| Registration is conditional on that resolved flag | mechanism §6 | `core/src/tools/spec_plan.rs:1137-1139` | `registry.add(PlanHandler)` |
| Disabling it strips codex's own checklist guidance — three headings unconditionally, `## Planning` only when the section carries the checklist text, plus the `Progress visibility:` pair and plan-tool bullets with their continuations | SKILL off-by-default table, mechanism §6 | `core/src/context/update_plan_instructions.rs:4-59` (marker guard at `20-24`) | `fn without_update_plan_instructions`; `## Plan Mode vs update_plan tool`; `You have access to an ` |
| The strip runs on model instructions, rendered base instructions, built-in collaboration presets, inherited multi-agent role text and the goal continuation template | mechanism §6 | `core/src/session/world_state.rs:47-57,195-205`; `core/src/session/mod.rs:1340-1354`; `core/src/context/world_state/collaboration_mode.rs:27,43-61`; `core/src/session/multi_agents.rs:84,110,114-118`; `core/src/agent/control/spawn.rs:892,1062`; `ext/goal/src/steering.rs:17-22,79-83` | `update_plan_enabled` |
| Compaction and Guardian feedback read the already-stripped base instructions | mechanism §6 | `core/src/compact.rs:285`; `core/src/compact_remote_request.rs:33`; `core/src/compact_remote_v2_attempt.rs:41`; `core/src/guardian/feedback.rs:66` | `get_prompt_base_instructions` |
| Caller-authored instructions are left unstripped, under a different guard per site | SKILL off-by-default table, mechanism §6 | `core/src/session/multi_agents.rs:105-108`; `core/src/session/world_state.rs:47-53`; `core/src/session/mod.rs:1343-1348`; `core/src/context/world_state/collaboration_mode.rs:49-58` | `BaseInstructionsProvenance::Model`; `builtin_collaboration_mode_presets` |
| No `<collaboration_mode>` fragment in a default session | mechanism §6 | `core/tests/suite/collaboration_instructions.rs:117-151` | `no_collaboration_instructions_by_default` |
| zsh-fork backends are under development, default off | SKILL off-by-default table | `features/src/lib.rs:860-865` | `shell_zsh_fork` |
| `write_stdin_approval` is under development, default off | SKILL off-by-default table, mechanism §2 | `features/src/lib.rs:1145-1149` | `key: "write_stdin_approval"` |
| Per-exec denial wording | SKILL triage, mechanism §2 | `shell-escalation/src/unix/escalate_client.rs:118` | `Execution denied:` |
| Memories are stable but default off | SKILL off-by-default table | `features/src/lib.rs:992-997` | `"memories"` |
| The full shell surface is `exec_command` + `write_stdin` | mechanism §2 | `core/src/tools/spec_plan.rs:1075,1099-1101` | `fn add_shell_tools` |
| …conditional on three gates, and `unified_exec` off degrades to a one-shot `exec_command` rather than removing the surface | mechanism §2 | `core/src/tools/spec_plan.rs:1080-1085,1102-1106` | `registry.add(ExecCommandHandler::one_shot` |
| The one-shot spec drops `tty`, `yield_time_ms` and the returned `session_id`, and adds `timeout_ms` defaulting to 10,000 ms | mechanism §2 | `core/src/tools/handlers/unified_exec/exec_command.rs:102,470-497` | `fn one_shot_exec_command_spec` |
| `shell_tool` and `unified_exec` are Stable/default-enabled | mechanism §2 | `features/src/lib.rs:836-841,854-859` | `key: "unified_exec"` |
| `unified_exec_zsh_fork` is `Removed`/default-true **but still a live gate**: zsh-fork mode ANDs `ShellTool`, `UnifiedExec`, `ShellZshFork` and `UnifiedExecZshFork` | SKILL off-by-default table, mechanism §3 | `tools/src/tool_config.rs:41-75`; `features/src/lib.rs:866-871` | `fn for_session`; `UnifiedExecShellMode` |
| …and a regression test asserts that disabling either zsh-fork flag forces Direct mode | mechanism §3 | `tools/src/tool_config_tests.rs:25,43-64` | `unified_exec_shell_mode_respects_feature_and_policy_gates` |
| The `shell` argument is resolved to a shell *type*, not launched as a path | mechanism §2 | `core/src/tools/handlers/unified_exec.rs:113-124`; `shell-command/src/shell_detect.rs:250-264` | `get_shell_by_model_provided_path` |
| Local zsh-fork rejects an explicit `shell` | mechanism §2 | `core/src/tools/handlers/unified_exec.rs:125-129` | `is not supported for local zsh-fork exec` |
| `shell_command` survives as a legacy catalog alias for unified exec | mechanism §2 | `protocol/src/openai_models.rs:300` | `alias = "shell_command"` |
| App-server `thread/shellCommand` runs unsandboxed with full access | SKILL outside playbook, mechanism §2 | `app-server-protocol/src/protocol/common.rs:653`; `app-server-protocol/src/protocol/v2/thread.rs:1122-1128` | `runs unsandboxed with full` |
| Code mode runs in a separate host process | mechanism §6 | `code-mode/src/remote_session.rs:39,48,183` | `OwnedCodeModeHost` |
| Guardian review injects a bounded root-conversation authorization block | mechanism §6 | `guardian-context/src/authorization.rs:55-66` | `ROOT CONVERSATION START` |
| The block also carries host-verified `request_user_input` answers, the verified paths of user-owned skills invoked on the latest root user turn, and an incomplete-evidence marker | mechanism §6 | `core/src/agent/control/user_authorization.rs:71-88`; `core/src/context/guardian_review_evidence.rs:183,237` | `GuardianRootMessage::IncompleteVerifiedAnswers`; `trusted_skill_paths` |
| `wait_agent` clamps a too-short timeout | mechanism §6 | `core/src/tools/handlers/multi_agents_v2/wait.rs:148` | `was clamped to the minimum of` |
| apply_patch refuses duplicate resolved paths | SKILL triage, mechanism §2 | `apply-patch/src/invocation.rs:237` | `multiple operations target` |
| apply_patch disables symlink traversal when the sandbox is bypassed | mechanism §2 | `core/src/tools/runtimes/apply_patch.rs:183` | `follow_symlinks` |
| Catalog supplies collaboration-mode text, `model_specialty`, and a per-model tool allowlist | SKILL Layer 0, mechanism Layer 0 | `protocol/src/openai_models.rs:446,466,535`; `core/src/tools/spec_plan.rs:1043` | `experimental_supported_tools` |
| Managed requirements inject `<managed_developer_instructions>` as a separate developer message, replacing or removing the prior block | SKILL Layer 0 | `core/src/context/world_state/managed_developer_instructions.rs:14-16,32-45`; `core/src/session/mod.rs:3737-3743` | `REPLACEMENT_NOTICE`; `managed_developer_instructions` |
| …and a rendered block over 10,000 estimated tokens is rejected at config load | SKILL Layer 0 | `core/src/context/world_state/managed_developer_instructions.rs:13,67-76` | `MAX_MANAGED_DEVELOPER_INSTRUCTIONS_TOKENS` |
| `history.*` / `notes.*` need `use_history_notes_extension`, an OpenAI provider, and Codex-backend auth | SKILL off-by-default table, mechanism §6 | `ext/history-notes/src/extension.rs:46-52` | `use_history_notes_extension` |
| …and the same extension contributes a thread hint fetched from `alpha/notes/v2/thread_hint`, scoped to the calling agent's name | SKILL off-by-default table, mechanism §6 | `ext/history-notes/src/extension.rs:72-79,98,118-127`; `ext/history-notes/src/backend.rs:33,40-46` | `alpha/notes/v2/thread_hint`; `current_agent_name` |
| …injected as a `PromptSlot::ContextWindow` fragment of kind `notes.thread_hint`, the only producer of that slot outside tests | mechanism §6 | `ext/history-notes/src/extension.rs:145-149`; `core/src/session/mod.rs:3979-3983` | `notes.thread_hint`; `PromptSlot::ContextWindow` |
| The 4,000-byte bound is advertised to the backend as a truncation-policy header, not applied client-side; an oversized hint is dropped whole rather than truncated | SKILL off-by-default table, mechanism §6 | `ext/history-notes/src/extension.rs:31,126,137-140`; `ext/history-notes/src/backend.rs:16,55-67` | `MAX_THREAD_HINT_BYTES`; `TOOL_OUTPUT_TRUNCATION_POLICY_HEADER` |
| Failure, a missing `text` field and an overrun record `Failed`; an empty hint records `Succeeded` and contributes nothing | mechanism §6 | `ext/history-notes/src/extension.rs:130,134,138,141-143` | `ThreadHintStatus::Failed` |
| Context-window hints are collected, joined and rendered as the trailing lines of the developer-role `<context_window>` message alongside the agent path and window ids | mechanism §6 | `core/src/session/mod.rs:3921,3979-3983,4046-4056`; `core/src/context/token_budget_context.rs:18,41,44-46,60-75`; `protocol/src/protocol.rs:137` | `context_window_hints`; `token_budget.context_window`; `CONTEXT_WINDOW_OPEN_TAG` |
| …while `token_budget.guidance_message` renders as a separate `<context_window_guidance>` fragment, not inside that body | mechanism §6 | `core/src/context/token_budget_context.rs:95-130`; `protocol/src/protocol.rs:139-140` | `token_budget.context_window_guidance` |
| The hint is fetched only when full context is built — the first real user turn, a new context window, and every compaction rebuild — not per turn in steady state or per sampling step | mechanism §6 | `core/src/session/mod.rs:3913,3974,4198,4227,4270,4281-4286`; `core/src/compact.rs:103` | `build_initial_context_with_world_state`; `contribute_thread_context` |
| The fetch is awaited inline with a 35-second backend timeout | mechanism §6 | `ext/history-notes/src/backend.rs:14,81` | `HISTORY_NOTES_BACKEND_TIMEOUT` |
| The `<context_window>` message and the hint inside it render only when the token-budget feature is on and the turn has a resolved model context window; the contributor fetch happens before that check, so a turn with no window pays for the fetch and discards the hint | mechanism §6 | `core/src/session/mod.rs:3974,4005-4008,4046-4056`; `core/src/config/mod.rs:2769-2775`; `features/src/lib.rs:1579-1584` | `Feature::TokenBudget` |
| With the token-budget feature on but `use_history_notes_extension` off, a legacy MCP `notes`/`thread_hint` bridge fills the same slot with `wait_for_server` set; the two paths are mutually exclusive and a failed native request does not fall back to the bridge | mechanism §6 | `core/src/session/mod.rs:4009-4045` | `latest_call_tool`; `thread_hint` |
| The catalog can activate token budgeting and the history/notes extension at thread startup, but only when neither a `features.token_budget` table nor an already-resolved `config.token_budget` exists and managed requirements have not pinned the feature off | SKILL off-by-default table, mechanism §6 | `core/src/session/token_budget.rs:120-157`; `core/src/session/mod.rs:687-691`; `protocol/src/openai_models.rs:594-606` | `fn apply_model_defaults` |
| …and a ChatGPT Plus/Pro/Pro-lite Codex-backend session with `context_management` on sets `use_history_notes_extension` on its own first, enabling `token_budget` in the same guard, which then blocks the catalog path | SKILL off-by-default table, mechanism §6 | `core/src/session/token_budget.rs:13-56` | `fn apply_experimental_context` |
| `token_budget` and `context_management` are both under development and default off; `config.token_budget` is resolved only from a `features.token_budget` table and only while the feature is enabled | SKILL off-by-default table, mechanism §6 | `features/src/lib.rs:1579-1590`; `core/src/config/mod.rs:1051,2769-2775,2934-2939` | `fn token_budget_toml_config` |
| Both activation flags are fixed at thread startup; only the prompt halves re-resolve per turn from the active model, and only while no local prompt key is set — invalid model-owned defaults are logged and discarded whole | mechanism §6 | `core/src/session/token_budget.rs:59-76,79-118`; `core/src/session/turn_context.rs:741-750` | `ignoring invalid model-owned token-budget defaults` |
| Token budgeting registers `new_context` (direct-model-only) and `get_context_remaining` | SKILL off-by-default table | `core/src/tools/spec_plan.rs:1201-1203`; `core/src/tools/handlers/new_context_window_spec.rs:6`; `core/src/tools/handlers/get_context_remaining_spec.rs:8` | `NEW_CONTEXT_WINDOW_TOOL_NAME` |
| …and register direct-model-only; history is read-only and eventually consistent, notes are virtual/writable/strongly consistent | SKILL off-by-default table, mechanism §6 | `ext/history-notes/src/tools.rs:25-38` | `NOTES_DESCRIPTION` |
| The client-side history/notes caps are gone: arguments and the turn's truncation policy are forwarded to the backend, which enforces its own limits | mechanism §6 | `ext/history-notes/src/tools.rs:142-235,261-284` | `call.truncation_policy` |
| An attached environment independently constrains MCP authority; pending/failed attachments expose no environment-owned servers | mechanism §6 | `core/src/mcp.rs:272-300` (call site); `codex-mcp/src/catalog.rs:337` (definition) | `build_with_environment_authority` |
| …and a restricted attachment disables non-matching servers and empty-policy plugin registrations | mechanism §6 | `codex-mcp/src/catalog.rs:340-378` | `McpEnvironmentAuthority::Restricted` |
| `request_user_input_async` is catalog-gated and root-session-only; the catalog still advertises it under the old `send_user_message_async` name and supplies its description under that key, an empty string included | SKILL default surface, SKILL Layer 0, mechanism Layer 0 | `core/src/tools/spec_plan.rs:1162-1184`; `core/src/tools/handlers/request_user_input_async.rs:25,65`; `protocol/src/openai_models.rs:578-592` | `send_user_message_async: Option<ToolMessage>` |
| `send_message_to_user_async` is a second, independently catalog-gated async tool for free-form text | SKILL default surface, mechanism Layer 0 | `core/src/tools/spec_plan.rs:1187-1195`; `core/src/tools/handlers/send_message_to_user_async.rs:21,45` | `tool == "send_message_to_user_async"` |
| …and both return immediately while the turn continues | SKILL default surface | `core/src/tools/handlers/request_user_input_async.rs:136`; `core/src/tools/handlers/send_message_to_user_async.rs:98` | `"accepted":true` |
| Quoting a glob preserves it as a literal argv token rather than expanding it | SKILL inside playbook | `shell-command/src/bash.rs:451-470` | `preserves_quoted_literals` |
| `uses_codex_backend()` is false only for `ApiKey`, `BedrockApiKey` and `BedrockAccessKeys` | SKILL default surface, mechanism Layer 0 | `protocol/src/auth.rs:54-63` | `fn uses_codex_backend` |
| …consumed by the apps gate, connectors, the plugin marketplace, app routing, the cloud bundle and history/notes | SKILL default surface, mechanism §6 | `core/src/session/turn_context.rs:448-454`; `core/src/connectors.rs:128,218`; `core-plugins/src/manager.rs:625,706`; `core-plugins/src/app_mcp_routing.rs:6-8`; `cloud-config/src/service.rs:48-56`; `ext/history-notes/src/extension.rs:52` | `apps_enabled_for_auth`; `uses_codex_backend` |
| …but memories is **not** a consumer: non-codex-backend auth skips the rate-limit check rather than disabling memories | — | `memories/write/src/guard.rs:9-18` | `rate_limits_check` |
| Auth mode selects the curated marketplace and clears plugin-declared apps while leaving `mcp_servers` | mechanism §6 | `core-plugins/src/manager.rs:624-630`; `core-plugins/src/app_mcp_routing.rs:6-19` | `target_curated_marketplace` |
| Managed `[marketplaces] restrict_to_allowed_sources` is absent by default and is what arms the marketplace source allowlist | mechanism §6 | `config/src/config_requirements.rs:310`; `core-plugins/src/marketplace_policy.rs:53-60,79-81` | `restrict_to_allowed_sources` |
| The bundled curated source is the literal `https://github.com/openai/plugins.git` and is validated like any third-party Git source | mechanism §6 | `core-plugins/src/startup_sync.rs:30`; `core-plugins/src/marketplace_policy.rs:157-171` | `OPENAI_PLUGINS_GIT_URL` |
| A disallowed curated source skips the local curated repo sync — a path already conditional on the remote catalog being inactive — and the listing only offers a curated root that is already on disk | mechanism §6 | `core-plugins/src/manager.rs:705-707,715-719,3540-3541` | `maybe_start_curated_repo_sync_for_config` |
| …fails `validate_install` for the two curated marketplace names, filtering the curated marketplace out of the listing and rejecting installs from it | mechanism §6 | `core-plugins/src/marketplace_policy.rs:99-101,113-117`; `core-plugins/src/lib.rs:47-50`; `core-plugins/src/manager.rs:2050-2062,3571-3581` | `is_openai_curated_marketplace_name`; `is not allowed by requirements from` |
| …and drops curated `[plugins]` entries from the policy-filtered effective config, with a catch-all that drops any non-configured, non-curated entry too | mechanism §6 | `core-plugins/src/marketplace_policy.rs:210-255` | `policy_filtered_plugin_config` |
| A managed bundled marketplace configured at its own root under its own name is exempt from the allowlist; either half mismatched is refused without consulting it | mechanism §6 | `core-plugins/src/marketplace_policy.rs:280-282,485-497` | `managed_marketplace_config_name` |
| A `GitUrl` allowlist entry matches on exact URL equality after the normalization both sides share, and one that pins a `ref` never admits the curated source because the curated check passes no ref | mechanism §6 | `core-plugins/src/marketplace_policy.rs:186-197,372-377`; `core-plugins/src/marketplace_add/source.rs:119-126` | `is_none_or`; `fn normalize_git_url` |
| The marketplace allowlist fails closed on a defective entry: the compile error is returned from every source validation | mechanism §6 | `core-plugins/src/marketplace_policy.rs:83-91,346-353` | `invalid marketplace allowed source` |
| The cloud-delivered requirements bundle needs codex-backend auth and a business/education/enterprise plan; header auth reports no plan | mechanism Layer 0 | `cloud-config/src/service.rs:48-56`; `protocol/src/account.rs:59-86`; `login/src/auth/manager.rs:618-621` | `cloud_config_eligible_auth` |
| Workload identity is env-selected, immutable and rejects logout; `codex mcp-server` refuses it | mechanism Layer 0 | `login/src/auth/workload_identity.rs:123-167`; `login/src/auth/manager.rs:2557-2564,2859-2866`; `mcp-server/src/lib.rs:211-219` | `workload identity auth is managed by the host and cannot be logged out` |
| Only `ApiKey`, `BedrockApiKey` and `BedrockAccessKeys` are exempt from workspace restrictions; header auth is checked via its account id | mechanism Layer 0 | `login/src/auth/manager.rs:1245-1256,1343-1352` | `Login is restricted to workspace(s)` |
| Bedrock is two auth modes: `bedrockApiKey` holds a bearer token, `bedrockAccessKeys` holds an AWS access-key ID, secret access key and optional session token, under that same name on the app-server wire; both are also false in `has_chatgpt_account()` | mechanism Layer 0 | `protocol/src/auth.rs:30-37,41-51`; `login/src/auth/bedrock_access_keys.rs:12-19`; `app-server-protocol/src/protocol/common.rs:52-61` | `bedrockAccessKeys` |
| Both Bedrock modes count as the `api` login method; `allowed_login_methods` only filters the credential to `None`, while the separate `forced_login_method` key is enforced again at `codex exec` and TUI startup, clearing every configured auth store and exiting 1 | mechanism Layer 0 | `login/src/auth/manager.rs:1177-1182,1215-1225,1287,1307-1331,1399-1417`; `exec/src/lib.rs:573-578`; `tui/src/startup_orchestration.rs:452-461` | `ChatGPT login is required, but an API key is currently being used` |
| Managed `enforce_residency` overrides a provider's configured residency header at request time rather than erroring, leaving only a startup warning | — | `model-provider/src/provider.rs:35-42`; `core/src/config/requirements.rs:52-68` | `because managed residency is required` |
| Every registered MCP tool is `Deferred` on a search-capable model, and a server's `omit_tools_from` removes it from any of the three exposure surfaces | SKILL default surface, mechanism §6 | `core/src/tools/spec_plan.rs:156,174,208-227,578`; `config/src/mcp_types.rs:207`; `protocol/src/config_types.rs:399` | `omit_tools_from` |
| Optional MCP servers get a shared startup grace, spent once per server rather than per capture, then are omitted with only a trace log while their startup continues in its own task; cached catalogs publish with `read_only_hint` cleared | SKILL default surface, mechanism §6 | `codex-mcp/src/connection_manager/tool_catalog.rs:187-249,289-297`; `codex-mcp/src/connection_manager.rs:192,619,700-705,724-725` | `omitting pending optional MCP server` |
| …the grace is `mcp_optional_startup_grace_ms`, default 1000 ms; `0` disables the shared grace and awaits each server's own `startup_timeout_sec` (30 s when unset) instead, while a cached catalog short-circuits ahead of both | SKILL default surface, mechanism §6 | `config/src/config_toml.rs:290-294`; `core/src/config/mod.rs:4207-4210`; `codex-mcp/src/mcp/mod.rs:186-187`; `codex-mcp/src/connection_manager/tool_catalog.rs:211-222`; `codex-mcp/src/rmcp_client.rs:98,326-329` | `mcp_optional_startup_grace_ms` |
| …a live config refresh carries the new grace into the session and the per-server cache re-keys its stored deadline when the grace value changes | mechanism §6 | `core/src/session/mod.rs:1887,1943`; `codex-mcp/src/tool_catalog_cache.rs:122-150` | `mcp_optional_startup_grace` |
| A selected-plugin MCP server is forced through startup only when its plugin ID is in the turn's `required_plugins`, populated by an explicit `plugin://` mention or its `@` plaintext link and skipped entirely in a Guardian-reviewer or internal-Guardian session | SKILL default surface, mechanism §6 | `codex-mcp/src/connection_manager/tool_catalog.rs:201-209`; `core/src/session/turn.rs:203-210,379-383`; `core/src/plugins/mentions.rs:83-106`; `utils/plugins/src/mention_syntax.rs:4,7`; `core/src/guardian/review.rs:219-225` | `collect_explicit_plugin_ids` |
| …and a config-required server is forced through startup only while it is not dormant or has no cached non-empty catalog, so a dormant required server with a cached catalog is published from cache like an optional one | mechanism §6 | `codex-mcp/src/connection_manager/tool_catalog.rs:195-206` | `startup_is_dormant` |
| `is not available to the model` fires when `prepare_mcp_call` resolves to no binding for that server/tool; the cached-catalog case leaves the tool model-visible with no call, while a server with no resolved owner permission profile has its tools skipped before the model-visible push | SKILL triage, mechanism §4, §6 | `core/src/mcp_tool_call.rs:159-168`; `core/src/session/mcp_runtime.rs:61-72`; `codex-mcp/src/binding.rs:93-98,190-202`; `codex-mcp/src/connection_manager/tool_catalog.rs:317-343,355-375` | `fn prepare_mcp_call` |
| Under `never`, an MCP permission prompt auto-approves for a Disabled/External profile or a Managed one with full-disk write — evaluated against the calling server's own owner profile — so the `requires approval` string appears only under a restricted Managed owner profile | SKILL triage, mechanism §4, §6 | `codex-mcp/src/mcp/mod.rs:88-109`; `core/src/mcp_tool_call.rs:282,1403,1428-1438,1535` | `mcp_permission_prompt_is_auto_approved` |
| A published MCP runtime resolves one permission profile per enabled server, in precedence order: host-owned Apps takes the thread's effective profile; else the attached environment named by the server's `environment_id` (workspace roots materialized); else the thread's profile for a local-environment or selected-plugin server; else no entry | mechanism §6 | `codex-mcp/src/mcp/mod.rs:150,158,191-226`; `core/src/session/mcp_runtime.rs:340,357-365`; `core/src/session/turn_context.rs:156-160` | `fn set_server_permission_profiles`; `permission_profile_with_workspace_roots` |
| A prepared call carries the owner profile validated at preparation, and approval review is handed that profile rather than the thread-wide one; the approval policy stays runtime-wide | SKILL triage, mechanism §6 | `codex-mcp/src/binding.rs:190-202,226-233`; `core/src/mcp_tool_call.rs:272-282,1403,1428-1438` | `prepared MCP calls retain their immutable permission authority` |
| Threadless discovery and resource reads rebuild the config with `PermissionProfile::default()` — restricted managed filesystem with no entries, restricted network, which never auto-approves under `never` — for every server | mechanism §6 | `codex-mcp/src/mcp/mod.rs:229-239,426,503`; `core/src/connectors.rs:239`; `app-server/src/request_processors/apps_processor/installed.rs:62`; `protocol/src/models.rs:468-478`; `protocol/src/permissions.rs:901-911` | `fn for_threadless_operations` |
| …and a test asserts both the unattached-server drop and the threadless narrowing of a `Disabled` thread profile | mechanism §6 | `codex-mcp/src/mcp/mod_tests.rs:73-112` | `mcp_server_permissions_handle_unattached_and_threadless_servers` |
| An elicitation from a server with no resolved owner profile is declined before both the strict-auto-review branch and the policy check; that server's own profile feeds the auto-accept check, while the strict-auto-review branch turns only on the approval policy and reviewer handle | mechanism §6 | `codex-mcp/src/elicitation.rs:249-256,258-306,308-312` | `permission_profile_for_server` |
| MCP catalog enumeration aborts whole (100 pages, 2,048 items / 8,192 for Apps, 64 KiB cursor, startup-timeout bound) rather than truncating | mechanism §6 | `codex-mcp/src/pagination.rs:9-13,44-74`; `codex-mcp/src/rmcp_client.rs:97,629,926-932` | `MAX_MCP_CATALOG_ITEMS` |
| An MCP connection is keyed by `McpServerConnectionIdentity`; the configured `auth` and — for stored-OAuth connections only — the credential store and effective `McpOAuthRefreshMode` are compared, so an auth or refresh-mode change drops the live client instead of reusing it | mechanism §6 | `codex-mcp/src/server.rs:92-115,141-171,215-234,238-267`; `codex-mcp/src/connection_manager.rs:94-129,376-394`; `core/src/config/mod.rs:1746-1750`; `features/src/lib.rs:1291-1296` | `has_same_connection_config` |
| …while tool policy and presentation metadata stay publication-only, the stored credential snapshot and its contention flag are skipped by that comparison and weighed in `reusable_client`, `startup_timeout` is checked only for a connection that has not finished starting, and a changed `catalog_item_limit` still drops reuse | mechanism §6 | `codex-mcp/src/server.rs:92-95,105-107`; `codex-mcp/src/connection_manager.rs:85-91,109-128,162-168,422,425,450` | `struct McpServerView`; `Startup-only budget` |
| An MCP server with `auth = "chatgpt"` and a non-default `environment_id` fails startup before any transport opens unless it carries an inline `Authorization` header; the guard rejects a `bearer_token_env_var` and any non-empty `env_http_headers`, config parsing rejects `http_headers_helper` for such a server, and a stdio transport can never satisfy it | SKILL outside playbook, mechanism §6 | `codex-mcp/src/rmcp_client.rs:1092-1099`; `codex-mcp/src/server.rs:62-90`; `config/src/mcp_types.rs:22,177-188,271-273,471-479`; `codex-mcp/src/connection_manager_tests.rs:4290-4304` | `cannot use hosted ChatGPT authentication`; `only supported for local MCP servers` |
| …and that message is not authentication-required, so it is wrapped by `mcp_init_error_display`'s generic branch and emitted to the client as a failed startup status, never into the model's context | SKILL outside playbook, mechanism §6 | `codex-mcp/src/connection_manager/startup.rs:75,130-132`; `codex-mcp/src/connection_manager.rs:662-688`; `codex-mcp/src/connection_manager_tests.rs:5242-5253` | `` `failed to start:` `` |
| MCP elicitations: an auto-deny switch and a `tool_suggestion` decline precede everything; `never` declines, but an empty-schema standard form is auto-accepted first; a strict-auto-review elicitation fails closed **only where Guardian is consulted** — the strict branch calls the core reviewer before auto-accept, and both core shortcuts (Full Access, user-CUA) return an accept carrying the `approvals_reviewer: auto_review` meta the strict branch validates | mechanism §6 | `codex-mcp/src/elicitation.rs:44,212-232,258-304,306-319,321-344,462-470,472-481,483-495`; `core/src/session/mcp.rs:743-763,1135-1159,1180-1184` | `elicitation_is_rejected_by_policy`; `can_auto_accept_elicitation`; `mcp_elicitation_auto_meta` |
| When the core reviewer returns no decision the router forwards the elicitation to the client, so an ordinary non-empty-schema form reaches a human without full-access surfacing | mechanism §6 | `codex-mcp/src/elicitation.rs:353-368,370-430` | `NEXT_ELICITATION_REQUEST_ID` |
| Only standard MCP forms and the `openai/elicitation/create` variant can be surfaced as a full-access form; legacy `openai/form` never is, and any elicitation carrying `codex_approval_kind` is refused | mechanism §6 | `codex-mcp/src/elicitation.rs:321-344`; `protocol/src/mcp_approval_meta.rs:1-4` | `should_surface_form_in_full_access` |
| Full-access form surfacing is armed once at thread start — client declared `openai/standard-form-input`, `ThreadSource::User`, root agent — not per call | mechanism §6 | `core/src/thread_manager.rs:2043-2051`; `codex-mcp/src/runtime.rs:504-506`; `codex-mcp/src/elicitation.rs:126-133` | `enable_full_access_form_input`; `OPENAI_STANDARD_FORM_INPUT_EXTENSION_ID` |
| `openai/elicitation/create` is enabled only when the client's `openai/elicitation` capability carries an object-valued `form` key; the initialize path retains only that key | mechanism §6 | `rmcp-client/src/elicitation_client_service.rs:57,73-79,165-171`; `codex-mcp/src/client_capabilities.rs:16-45`; `app-server/src/request_processors/initialize_processor.rs:74`; `protocol/src/mcp.rs:26-31` | `supports_openai_elicitation_form`; `OPENAI_ELICITATION_EXTENSION_ID` |
| The TUI declares none of the OpenAI form extensions and resolves an OpenAI form variant as `Decline` | mechanism §6 | `tui/src/app/thread_routing.rs:301-315,1705-1717` | `startup_request_may_open_protected_view` |
| Catalog `browser_use`/`computer_use` confirmation policies are forwarded verbatim under `openai/confirmation_policies` to `node_repl`/`cua_repl` only, following the issuing step's model snapshot, excluding Guardian-reviewer sessions | mechanism §6 | `core/src/mcp_tool_call.rs:1285-1287,1292-1326`; `protocol/src/mcp.rs:35,38-40`; `core/src/guardian/review.rs:219-225` | `build_confirmation_policies_request_meta`; `CONFIRMATION_POLICIES_META_KEY`; `is_basic_session_source` |
| …and an eligible confirmation-policy call with no configured policies still receives an empty object, clearing runtime startup defaults | mechanism §6 | `core/src/mcp_tool_call.rs:1296-1297,1308-1325` | `CONFIRMATION_POLICIES_META_KEY` |
| A selected remote executor contributes its own eligible HTTP MCP servers to the thread | mechanism §6 | `core/src/session/mcp_runtime.rs:145-217`; `exec-server/src/environment_config.rs:67-91` | `discover_http_mcp_servers` |
| App-tool policy blocks a `codex_apps` call with its own model-visible string; the same evaluator also filters the menu, and both annotation hints default to `true` | SKILL triage, mechanism §4, §6 | `core/src/mcp_tool_call.rs:191-199,2264`; `core/src/mcp_tool_exposure.rs:157-183`; `connectors/src/app_tool_policy.rs:214-231` | `MCP tool call blocked by app configuration` |
| `apps.<app>.links.<link_id>` carries only `approvals_reviewer` and `default_tools_approval_mode` — no enable/disable keys | SKILL inside playbook, mechanism §6 | `config/src/types.rs:458-469,471-478,513-514` | `struct AppLinkConfig` |
| App-tool approval mode resolves managed → per-tool → link → app → `apps.default` (connector-gated) → `Auto` | mechanism §6 | `connectors/src/app_tool_policy.rs:166-203` | `default_tools_approval_mode` |
| MCP reviewer resolves managed-model requirement → link → app → `apps.default`, and the app-resolved reviewer applies only if the managed `approvals_reviewer` requirement permits that value | mechanism §6 | `core/src/connectors.rs:514-551` | `mcp_approvals_reviewer_from_layers` |
| The link selector comes from tool metadata `link_id` unless the `codex_apps` meta sets `requires_explicit_link_id`, when it must be a non-blank string call argument; only the `codex_apps` server resolves a link at all | SKILL triage, mechanism §6 | `core/src/mcp_tool_call/account.rs:9-38`; `core/src/mcp_tool_call.rs:1704-1708` | `requires_explicit_link_id`; `This app tool requires a non-empty string link_id argument` |
| …and a rejected selector ends the call before the app-enabled check, before approval and before the server is contacted | SKILL triage, mechanism §6 | `core/src/mcp_tool_call.rs:178-203,228-255` | `McpToolAccountError` |
| Approval elicitations carry `link_id` and `link_is_implicit`, the latter keyed off the reserved `implicit_link::` prefix | mechanism §6 | `core/src/mcp_tool_call.rs:1180-1181,1859,1925-1937` | `MCP_TOOL_LINK_IS_IMPLICIT_META_KEY`; `implicit_link::` |
| Apps/connector surface is default-on, gated only by auth; both non-auth inputs default true | mechanism §6 | `core/src/session/turn_context.rs:349-358`; `features/src/lib.rs:445-447,1147-1151`; `core/src/config/mod.rs:2633-2637`; `core/src/session/turn.rs:779-799`; `core/src/session/world_state.rs:218-231` | `apps_enabled_for_auth` |
| A hosted `codex_apps` remote MCP server is contributed whenever `Feature::Apps` is on; the contributor checks no auth | mechanism §6 | `ext/mcp/src/lib.rs:19-38`; `app-server/src/extensions.rs:108` | `hosted_plugin_runtime_mcp_server_config` |
| `image_gen.imagegen` is filtered out of the menu unless five gates pass, and its save path bypasses the filesystem sandbox | mechanism §6 | `core/src/tools/spec_plan.rs:611-648,1273-1276`; `ext/image-generation/src/tool.rs:277-296`; `ext/image-generation/src/artifact.rs:5-6,31-34`; `model-provider-info/src/lib.rs:471-479` | `image_generation_available` |
| Goal tools register only in app-server sessions with persistent thread state, excluding review subagents; the budget ceiling exists only once configured | mechanism §1 | `ext/goal/src/spec.rs:9-11`; `app-server/src/extensions.rs:80-92`; `ext/goal/src/extension.rs:101-105,414-424`; `ext/goal/src/runtime.rs:117-119`; `ext/goal/src/tool.rs:407-425` | `tools_available_for_thread` |
| A child thread's token usage is charged to the root goal's accounting state, transitively through each parent's own root, and that delta feeds the budget comparison | mechanism §1 | `ext/goal/src/extension.rs:115-137,400-412`; `ext/goal/src/accounting.rs:200-206,291-303`; `ext/goal/src/runtime.rs:528-540`; `state/src/runtime/goals.rs:542-566` | `record_descendant_token_usage` |
| The auto-block streak counts only a failure of the code-mode `exec` tool whose handler executed, in a turn with no successful tool; `exec` is the code-mode public tool name, not `shell`/`exec_command` | mechanism §1 | `ext/goal/src/accounting.rs:102-152,265-289`; `code-mode-protocol/src/lib.rs:51`; `core/src/tools/code_mode/mod.rs:54-61`; `core/src/tools/spec_plan.rs:360-368`; `core/src/tools/handlers/shell_spec.rs:67,96` | `consecutive_execution_failure_turns`; `PUBLIC_TOOL_NAME` |
| Any successful tool, a goal replacement, or a Plan-mode turn resets the streak; every other outcome is neutral | mechanism §1 | `ext/goal/src/accounting.rs:116-121,145-148,265-276`; `ext/goal/src/extension.rs:246-252` | `clear_current_turn_goal` |
| At the third qualifying turn, turn stop sets the goal to `Blocked` under the event name `execution-unavailable`, and skips it if the goal was replaced | mechanism §1 | `ext/goal/src/extension.rs:279-299`; `ext/goal/src/runtime.rs:285-304,323-329`; `state/src/model/thread_goal.rs:12-21` | `ExecutionUnavailable` |
| A `code_mode_only` session keeps direct-model-only tools and hosted tools; only code-mode-nested tools are hidden | SKILL Layer 0, mechanism §6 | `core/src/tools/spec_plan.rs:484-510,682-693`; `tools/src/tool_executor.rs:64-72`; `code-mode-protocol/src/description.rs:251` | `is_hidden_by_code_mode_only`; `fn hosted_model_tool_specs` |
| Ordinary `CodeMode` falls back to `Direct` when the host is unavailable; `CodeModeOnly` never does, and the host failure reaches the model as tool output | SKILL Layer 0, mechanism §6 | `core/src/tools/mod.rs:79-89`; `core/src/tools/code_mode/mod.rs:101-114`; `core/src/tools/code_mode/execute_handler.rs:65-77` | `disable_in_process_fallback`; `Code mode will fail closed` |
| `hide_spawn_agent_metadata` drops `service_tier` from the spawn schema and the nickname from the result; `non_code_mode_only` selects `DirectModelOnly` exposure | mechanism §6 | `core/src/tools/spec_plan.rs:1120-1150`; `core/src/tools/handlers/multi_agents_spec.rs:102-119,409-438` | `hide_spawn_agent_metadata`; `spawn_agent_output_schema_v2` |
| V2 rejects `fork_context`, and `followup_task` cannot target the root agent | mechanism §6 | `core/src/tools/handlers/multi_agents_v2/spawn.rs:283-289,320-330`; `core/src/tools/handlers/multi_agents_v2/message_tool.rs:11-23,72-80` | `fork_context is not supported in MultiAgentV2` |
| `is_non_root_agent` covers internal sessions as well as subagents, so both are excluded from the async user tools | SKILL default surface | `protocol/src/protocol.rs:2723-2728`; `core/src/tools/spec_plan.rs:1162,1187` | `fn is_non_root_agent` |
| A child's service tier is overwritten with the root thread's after the role is applied, so a role's `service_tier` is never effective | SKILL default surface, mechanism §6 | `core/src/tools/handlers/multi_agents_common.rs:322-350`; `core/src/tools/handlers/multi_agents/spawn.rs:106-108`; `core/src/tools/handlers/multi_agents_v2/spawn.rs:136-145` | `fn apply_spawn_agent_service_tier` |
| …and a child model that does not support the root's tier receives none | mechanism §6 | `core/src/tools/handlers/multi_agents_common.rs:344-348` | `supports_service_tier` |

---

## Empirically verified

Two probe families, run at different times against different builds. Do not read a row as re-verified unless its own family was — the tables are split for exactly that reason. Raw dumps and probe transcripts are not checked in.

### Prompt composition — re-run 2026-09-05, `codex-cli 0.154.0-alpha.3`

Dumped with `codex debug prompt-input` from cwd `/tmp/codex_probe_ro` against the installed `codex-cli 0.154.0-alpha.3` (built from a tree close to, but not identical with, the pinned commit). Ten configurations succeeded — the machine's own config; `read-only`, `workspace-write` and `danger-full-access` each under `approval_policy=never`; `read-only` and `workspace-write` under `on-request`; `workspace-write` under `on-failure`; `--enable powershell_shell_version`; `sandbox_workspace_write.network_access=true`; and that last one plus `network_proxy.allowed_domains`/`denied_domains` — and one was refused by the CLI (see the `untrusted` row).

| Claim | How it was confirmed | Result |
|---|---|---|
| `<permissions instructions>`, `<environment_context>`, `# AGENTS.md instructions` markers are real | every dump | All three present, in that order, across four messages: three developer and one user. The message count changed since the previous run, which recorded two developer messages — the multi-agent usage hint and `<multi_agent_mode>` now each occupy a developer message of their own |
| Further harness-authored developer fragments exist: `<skills_instructions>`, `<apps_instructions>`, `<plugins_instructions>`, `<multi_agent_mode>` | same | All four present. Each message carries a `content_item_kinds` metadata list naming its fragments, and the four groupings are now recorded exactly: `[host_skills.instructions, permissions.instructions, apps.instructions, plugins.usage_instructions]`, then `[multi_agent.usage_hint]`, then `[multi_agent.mode_instructions]`, then the user message's `[plugins.recommendations, agents_md.instructions, environments.environment_context]` |
| One developer fragment carries no marker at all | same | Still true. The multi-agent usage hint (`You are `/root`, the primary agent in a team of agents…`) is plain text with no wrapper; it states the concurrency-slot count, that collaboration tools are absent from the `functions.exec` `tools.*` namespace, and that a full-history fork inherits the parent model and reasoning effort and rejects overrides |
| `<recommended_plugins>` is a user-role fragment | same | Present, user role, listing plugins available but not installed |
| The AGENTS.md body is wrapped in `<INSTRUCTIONS>` | same | `# AGENTS.md instructions` is followed by `<INSTRUCTIONS>…</INSTRUCTIONS>` around the collected text |
| `<environment_context>` is structured XML with a filesystem permission profile | same | Confirmed element by element, not in shape only: `<cwd>`, `<shell>`, `<current_date>`, `<timezone>`, then `<filesystem><workspace_roots>…<permission_profile type="managed"><file_system type="restricted">` with `<entry access="read\|write">` rows; each workspace root's `.git`, `.agents` and `.codex` appear as explicit read-only entries; `:root`, `:slash_tmp`, `:tmpdir` appear as `<special>` values. Under `read-only` the profile collapses to a single `read :root` entry under both `never` and `on-request`, so the collapse follows the sandbox mode and not the approval policy |
| `danger-full-access` renders a third profile shape | `-c sandbox_mode=danger-full-access -c approval_policy=never` | `<permission_profile type="disabled"><file_system type="unrestricted" />` — a self-closing element with no `<entry>` rows and no workspace-root permissions, while `<workspace_roots>` itself is unchanged. The binary also carries `<permission_profile type="external">` and `<file_system type="external" />`; no configuration tried here reached either |
| Network domain lists in `<environment_context>` | the ten dumps, including `-c sandbox_workspace_write.network_access=true` and `-c network_proxy.allowed_domains=["example.com"] -c network_proxy.denied_domains=["evil.test"]` | No `<network>` element appeared in any of them. The two `network_proxy` overrides changed neither the fragment nor the announcement; `network_access=true` changed only the announcement, whose trailing clause becomes `Network access is enabled`. The binary does contain an adjacent `<network enabled="true">` / `allowed` / `denied` / `</network>` string group, so the shape exists at this build — what triggers it was not determined here |
| The permissions announcement discloses sandbox mode, read/write scope, and network state up front | every dump | Confirmed, and the opening sentence is now recorded verbatim for each mode. `workspace-write`: ``Filesystem sandboxing defines which files can be read or written. `sandbox_mode` is `workspace-write`: The sandbox permits reading files, and editing files in `cwd` and `writable_roots`. Editing files in other directories requires approval. Network access is restricted.`` `read-only` substitutes `The sandbox only permits reading files.`; `danger-full-access` substitutes `No filesystem sandboxing - all commands are permitted. Network access is enabled.` — an ASCII hyphen, not a dash. The writable-roots line, `` The writable roots are `…`. ``, is the fragment's last line rather than the next one |
| The announcement lists already-approved prefix rules verbatim | `-c sandbox_mode=workspace-write -c approval_policy=on-request` | `## Approved command prefixes` enumerates the user's persisted rules as JSON arrays. New at this run: the whole block disappears under `never` even in `workspace-write`, so the list is gated on the approval policy, not on the sandbox mode |
| The `never` announcement text | `-c approval_policy=never` under each of `read-only`, `workspace-write` and `danger-full-access` | Byte for byte in all three, on its own line and unchanged from the previous run: ``Approval policy is currently never. Do not provide the `sandbox_permissions` for any reason, commands will be rejected.`` |
| The `on-request` announcement forbids destructive and heredoc prefix rules | `-c sandbox_mode=workspace-write -c approval_policy=on-request` (this machine's default resolves to the same pair) | Verbatim, under a `### Banned prefix_rules ` heading that itself ends in a space: `NEVER provide a prefix_rule argument for destructive commands like rm.` and `NEVER provide a prefix_rule if your command uses a heredoc or herestring.` — the second carries a trailing space, so do not anchor a grep on its final period |
| Under `never`, the escalation instructions are removed entirely rather than reworded | `workspace-write` dumped under `on-request` and again under `never` | Confirmed, and now measured at equal sandbox mode: 4,966 characters shrink to 703. Both the `# Escalation Requests` block (with its `## How to request escalation` subsection) and `## Approved command prefixes` vanish; the sandbox sentence, the one-line policy sentence and the writable-roots line survive. Under `read-only` the same policy leaves 341 characters, matching the previous run. Treat 4,966 as a local measurement, not a build constant — it embeds this machine's persisted prefix rules and writable-root list, which is also why it is not comparable with the 5,056 the previous run recorded |
| `on-failure` is an announcement-layer alias of `on-request` | `-c sandbox_mode=workspace-write` under each | The two announcements are byte-identical at 4,966 characters, and so is the rest of the dump. This is the alias the mode matrix claims, observed at the layer the model actually reads |
| `approval_policy = "untrusted"` is refused at startup | `-c approval_policy=untrusted` | Exit status 1, empty stdout, and on stderr exactly `Error: approval_policy = "untrusted" is no longer supported; remove this setting`. The mode-matrix note's string, confirmed empirically rather than from source |
| Escalation is requested via `sandbox_permissions: "require_escalated"` | the `workspace-write` + `on-request` dump | Confirmed in the `on-request` announcement, together with a required `justification` and an optional `prefix_rule` |
| `<shell_version>` is feature-gated, default off, and PowerShell-only | `--enable powershell_shell_version` with `<shell>zsh</shell>` | Absent, and the fragment is byte-identical to the same dump without the flag. Read this as **consistent with** the claim, not as proof of it: a macOS host cannot resolve a turn to a local PowerShell environment, so the negative case is all this platform can produce. The build does carry the machinery — `powershell_shell_version` and the literal `<shell_version status="unavailable" />` both appear in the binary — so the positive case needs a Windows host to test |
| No fragment marker the docs name is absent from this build | the ten dumps, plus `strings(1)` over the installed binary | Twenty-two fragment-level markers are named across `SKILL.md` and `mechanism.md` (`<environment_context>` sub-elements and `<INSTRUCTIONS>` have their own rows above); every one of the twenty-two is present in the binary's string table. Eight appear in startup composition: `<permissions instructions>`, `<environment_context>`, `<skills_instructions>`, `<apps_instructions>`, `<plugins_instructions>`, `<multi_agent_mode>`, `<recommended_plugins>`, `# AGENTS.md instructions`. The other fourteen are mid-turn or host-conditional and appear in no startup dump — `<context_window>`, `<context_window_guidance>`, `<user_shell_command>`, `<turn_aborted>`, `<current_time_reminder>`, `<managed_developer_instructions>`, `<git_attribution>`, `<external_…>`, `<collaboration_mode>`, `<personality_spec>`, `<persistent_mode>`, `<model_switch>`, `<codex_internal_context …>`, and the `$mention` skill body's `<skill>`. Absence from a startup dump is evidence about *composition*, never about the build. (`<server>` in §6's MCP error message is a placeholder in an error string, not an injection marker, and is excluded from this count.) |

### Exec behavior — carried over from 2026-09-04, `codex-cli 0.154.0-alpha.2`

Two probe sessions were driven through `codex mcp-server` (`approval_policy = never`, `sandbox_mode = read-only`) against a build close to, but not identical with, the pinned tree: one with no model override, one with `model = gpt-5.6-sol`. What was and was not re-checked at `alpha.3` is spelled out in the paragraph below the table, and the methodology note at the end of this section applies to this table specifically.

| Claim | How it was confirmed | Result |
|---|---|---|
| **A default session is in code mode** | `codex mcp-server` probe with no model override | Direct tools were exactly `functions.exec`, `functions.wait`, `functions.request_user_input`, the six `collaboration.*` tools and two `mcp__cua_repl.*` tools; `shell_command` was absent and `exec_command` / `apply_patch` were reachable only as `tools.exec_command(...)` / `tools.apply_patch(...)` inside `functions.exec`. `gpt-5.6-sol` produced the identical list |
| Nested tool inventory is discoverable from inside `functions.exec` | same | `ALL_TOOLS` returned `apply_patch`, `exec_command`, `write_stdin`, `view_image`, `update_plan`, `request_plugin_install`, `image_gen__imagegen`, `read_mcp_resource`, `list_mcp_resources`, `list_mcp_resource_templates` and every `mcp__<server>__<tool>` entry. Read this as the *shape* of the inventory, not as a stock default list: `update_plan` is opt-in at the pin, and this probe ran on a build that is not the pinned tree (see the note above the table) |
| **OS-jail denials reach the model as unwrapped, verbatim command stderr** | probe: `touch /tmp/codex_probe_a.txt` via nested `exec_command` | Model saw `{"chunk_id":"…","exit_code":1,"original_token_count":14,"output":"touch: /tmp/codex_probe_a.txt: Operation not permitted\n"}` |
| **Policy rejections reach the model nested in a spawn-error wrapper** | probe: `rm -rf /tmp/codex_probe_dir`, then a source trace of the whole path | Structure confirmed by the probe — the model received a debug-formatted blob carrying `Rejected("approval required by policy, but AskForApproval is set to Never")` rather than a bare reason line. The wrapper's exact text is now taken from source instead of from the probe: `ExecApprovalRequirement::Forbidden { reason }` becomes `ToolError::Rejected(reason)` (`core/src/tools/orchestrator.rs:205-207`), which the unified-exec mapper debug-formats into `UnifiedExecError::create_process(format!("{other:?}"))` (`core/src/unified_exec/process_manager.rs:1467`; `ToolError` derives `Debug` at `core/src/tools/sandboxing.rs:357-361`, so a rejection renders as `Rejected("…")`), lands in `CreateProcess { message }` (`core/src/unified_exec/errors.rs:9`), and is emitted as `format!("exec_command failed: {err:?}")` middle-truncated to 900 bytes (`core/src/tools/handlers/unified_exec/exec_command.rs:57,460-464`). **Still do not quote the probe's own wrapper text**: see the trap below |
| **A denied nested call is a rejected promise, and an uncaught one fails the whole script** | same, both sessions | The `gpt-5.6-sol` session let it propagate and got `Script failed` with the error under `Script error:`; the default session caught it and got `Script completed` with the same error under `Output:` |
| **The forced-rm reason does not fire for every `rm -rf`** | same, then a source read of the reason branches | The run produced the generic policy reason, not `rm -f style commands are not permitted` — but shell wrapping is not the cause. `extract_bash_command` accepts `bash`/`zsh`/`sh` with `-c` or `-lc` and the heuristic still sees the inner `rm` (`shell-command/src/bash.rs:106-127`). The discriminator is `prompt_is_rule` (`core/src/exec_policy.rs:408-418`): the probe host's `~/.codex/rules/default.rules` carries a `prefix_rule` on `rm` with `decision = "prompt"`, and a rule-driven `Prompt` under `never` returns the bare policy constant before the dangerous-command match is consulted. Without such a rule the heuristic drives `Decision::Forbidden` (`exec_policy.rs:799-806`) and `derive_forbidden_reason` renders `` `rm -rf /tmp/x` rejected: rm -f style commands are not permitted. Use a safer approach `` — specific *and* command-echoing (`exec_policy.rs:1058-1096`; both outcomes pinned by `exec_policy_tests.rs:2234` and `:2256`) |
| The model will not request escalation under `never` | probe step asking for a `require_escalated` run | Both sessions refused to issue the call, citing the announcement; no rejection string was produced because none was sent |
| A 20,000-character nested `exec_command` output is not truncated at that size | probe: `echo` of 20,000 `x` | 20,001 characters returned with no marker, `original_token_count: 5001` |

No session was driven at `alpha.3` for this table; what was checked instead is cheap and build-specific. `strings -a` on the installed `0.154.0-alpha.3` binary, on 2026-09-05, finds `exec_command failed: `, `approval required by policy, but AskForApproval is set to Never`, `rm -f style commands are not permitted. Use a safer approach` and `rejected: policy forbids commands starting with`, and does not find `exec_command failed for` — so every literal string this document quotes for the rejection path is present in the shipped build, and the retired one is not. What that cannot show is structure — wrapped versus bare, the rejected promise, the untruncated 20,000-character output, the tool inventory — and those rows remain observations from the `0.154.0-alpha.2` run.

Reproduce the prompt dumps by running `codex debug prompt-input` from a stable cwd, once per configuration: bare for the machine's own config, then `-c sandbox_mode=<read-only\|workspace-write\|danger-full-access>` crossed with `-c approval_policy=<never\|on-request\|on-failure>`, then `--enable powershell_shell_version`, `-c sandbox_workspace_write.network_access=true`, and `-c network_proxy.allowed_domains=[…]`. Diff the announcement (`.[0].content[1].text`) and the environment context (`.[3].content[2].text`) across them rather than reading each dump whole. Reproduce the exec probe by driving `codex mcp-server` (or `codex exec --json -s read-only -c approval_policy=never --skip-git-repo-check --ephemeral`) with:

```
This is a harness introspection probe. Report the exact names of every tool available to you and whether exec_command/apply_patch are direct or nested. Then run: touch /tmp/codex_probe_a.txt ; then run: rm -rf /tmp/codex_probe_dir ; quote both errors verbatim, do not retry. Then echo a 20000-character string of x and report the returned length and any truncation marker.
```

**A probe model cannot be trusted to quote an error string.** Both probe sessions independently reported the policy rejection as `` exec_command failed for `/bin/zsh -c '…'`: CreateProcess { … } `` — the shape this document itself claimed at the previous pin. That string does not exist: the only site that builds this message is `format!("exec_command failed: {err:?}")`, the fragment `exec_command failed for` appears nowhere in the tree, and the probe binary's own strings contain the new form and not the old one. The model reproduced a documented-looking shape rather than the bytes it received, which means a probe can silently confirm whatever the docs already say. Take verbatim strings from source; use probes only for structure that source cannot show — which tools are exposed, whether a denial arrives wrapped or bare, whether an uncaught rejection ends the script, whether output was truncated at all.

## Re-verification procedure

1. **Scope the diff.** From a codex checkout. This is the union of every directory cited in the tables above — the previous pass listed only eleven of them, and every stale claim it missed sat in one of the nineteen it did not walk:

   ```
   git log --oneline <pinned>..HEAD -- \
     codex-rs/agent-roles codex-rs/app-server codex-rs/app-server-protocol codex-rs/apply-patch codex-rs/cli \
     codex-rs/cloud-config codex-rs/code-mode codex-rs/code-mode-protocol \
     codex-rs/code-mode-runtime codex-rs/codex-api codex-rs/codex-mcp \
     codex-rs/config codex-rs/connectors codex-rs/context-fragments codex-rs/core \
     codex-rs/core-plugins codex-rs/exec codex-rs/exec-server \
     codex-rs/execpolicy codex-rs/ext codex-rs/features codex-rs/git-utils codex-rs/guardian-context codex-rs/history codex-rs/hooks \
     codex-rs/linux-sandbox codex-rs/login codex-rs/mcp-server codex-rs/memories \
     codex-rs/model-provider codex-rs/model-provider-info \
     codex-rs/models-manager codex-rs/network-proxy \
     codex-rs/plugin codex-rs/process-hardening codex-rs/prompts codex-rs/protocol \
     codex-rs/rmcp-client codex-rs/sandboxing codex-rs/secrets codex-rs/shell-command codex-rs/shell-escalation \
     codex-rs/skills codex-rs/state codex-rs/tools codex-rs/tui codex-rs/utils \
     codex-rs/windows-sandbox-rs codex-rs/windows-sandbox-service codex-rs/worktree
   ```

   Regenerate this list from the **Source** column whenever rows are added, rather than editing it by hand.

2. **Check the anchors.** For each row, `rg` the **match on** column. A hit means the claim's anchor survives; follow it to confirm the surrounding logic still says the same thing. A miss means the constant or string was renamed or removed — re-derive the claim before editing the prose, and do not assume the behavior changed just because the identifier did.

3. **Line numbers drift; identifiers usually don't.** Treat the `file:line` column as a starting point and the **match on** column as the real key. Update line numbers as you go.

4. **Update the pin** at the top of this file and the verification line at the top of `SKILL.md`.

### Known gaps in the current pass

**This pass was scoped deliberately, and the scope is the largest gap.** Two independent sweeps of `c9b19deb09..8e6a44b428` (roughly 540 commits) were run: a 22-agent survey against the local checkout, and a separate audit run by a Codex session against `origin/main`. Between them they produced about 190 candidate corrections. Only the subset that made an existing statement **false** was applied first, seven areas in all: the exec rejection wrapper, `unified_exec` degradation, the async user tools, subagent service tier, Guardian authorization evidence, the history/notes caps, and the empirical section. A second pass has since applied fifteen further reviewed items — per-tool MCP output budgets, the configurable optional-server startup grace, the bundled cleanup-hook allowlist, `mcp_tool` hook handlers, `update_plan`'s opt-in, skills pagination and aliasing, retained-terminal `write_stdin` review, Apps account-link scoping, tool-schema bounds, goal accounting, the `clock` tool gates, the history/notes thread hint, sensitive MCP elicitations, catalog controls beyond `tool_mode`, and per-server MCP permission authority — and the clusters below are pruned to match. A third pass has since applied eight more — Bedrock access keys, `CODEX_VERSION`, the PowerShell shell version, `codex exec --worktree`, MCP connection identity and the hosted-ChatGPT startup guard, app-server MCP structured errors, the Agent Plugin MCP env overlay, and managed marketplace source policy. What is still listed there is real, verified, and **not yet in these files**.

The largest unapplied clusters, so the next pass does not rediscover them:

- **Approvals and Guardian**: extension approval review splits into a fast decision and a full review with first-claim-wins.
- **Tool surface**: a `persistent` reasoning effort injects a `<persistent_mode>` developer fragment.
- **Skills**: a `$mention` injects at most 8,000 bytes of the skill body, silently (`MAX_SKILL_PROMPT_BYTES`, `ext/skills/src/render.rs:19,1177`) — note that mechanism §6's opening sentence still says a mention "injects the full body", which this contradicts and which the next pass must fix together.
- **Injected context**: closed. §1's fragment list now names the developer-role markers (`<skills_instructions>`, `<apps_instructions>`, `<plugins_instructions>`, `<multi_agent_mode>`) and the user-role `<recommended_plugins>`, records the `<INSTRUCTIONS>` wrapper around the AGENTS.md body, and scopes the marker rule to the user role — so the unmarked multi-agent usage hint no longer contradicts it. `<environment_context>` is documented as structured XML carrying a `<permission_profile>` in §1 and in the prompt-composition table above.
- **Executor platform**: command classification, `apply_patch` path authority and permission matching follow the *reported executor* platform rather than the host, and an unknown reported platform is treated as Windows.
- Smaller, all verified and all unapplied: `thread/shellCommand` takes a `timeoutMs` defaulting to one hour; an idle app-server thread unloads after 60 seconds.

**A pre-existing error the recount found**: the Granular rejection row quotes `approval required by policy, but AskForApproval::Granular.rules is false`. The source says `approval required by policy rule, but …`. This was wrong at the previous pin too, and is not yet corrected.

What the sweeps themselves did not cover: the survey's per-slice reports each carry an honest `not_opened` list, and the coverage is uneven. Roughly 155 of 187 `tui` commits, about 90 of 160 `app-server` commits, and about 80 of 131 `protocol`/`features`/`config` commits were triaged from subject line plus file list without opening a diff. `ext/memories` was read at HEAD but its change history was not walked. Guardian V2's context composition, transcript retention and decision internals were read only at grepped sites.

**The empirical rows have a different status than the source rows, and the two empirical tables differ from each other.** The prompt-composition table was re-run on 2026-09-05 against `codex-cli 0.154.0-alpha.3`; the exec table still carries its 2026-09-04 `alpha.2` observations. Its two weakest rows were closed from source rather than by a new session — the rejection wrapper's exact text is now traced end to end through `orchestrator.rs`, `process_manager.rs`, `errors.rs` and `exec_command.rs`, and the forced-rm row's stated cause was wrong and has been corrected — and the shipped `alpha.3` binary was checked for every literal string those rows quote. What stays unverified at `alpha.3` is the *structure* the probe observed: that a rejection arrives wrapped while an OS denial does not, that a denied nested call is a rejected promise that can fail the whole script, and the exact tool inventory. Re-running the exec probe would settle only those. The probe methodology failed here in a way worth reading before trusting any future probe: see the note at the end of the empirical section.

### Traps

- **Do not trust "the file did not change in range" as evidence a claim is true.** It is evidence the claim did not *change*. The Windows degrade-vs-refuse error survived a previous review precisely this way.
- **Do not `rg` a full quoted template.** See the note at the top of this file.
